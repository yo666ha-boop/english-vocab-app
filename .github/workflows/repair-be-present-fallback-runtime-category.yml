name: Repair be-present fallback runtime category

on:
  push:
    branches:
      - fix-vocab-grammar-range-20260825
    paths:
      - tools/repair_be_present_fallback_runtime_category.mjs
      - .github/workflows/repair-be-present-fallback-runtime-category.yml
      - audit/VOCAB_SAFE_FALLBACK_BE_PRESENT_CATEGORY_TRIGGER.md

permissions:
  contents: write

concurrency:
  group: repair-be-present-fallback-category-${{ github.ref }}
  cancel-in-progress: false

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix-vocab-grammar-range-20260825
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Sync branch
        run: |
          git fetch origin fix-vocab-grammar-range-20260825
          git reset --hard origin/fix-vocab-grammar-range-20260825
      - name: Repair display-stage/runtime-category connection
        run: node tools/repair_be_present_fallback_runtime_category.mjs
      - name: Validate embedded JSON and runtime syntax
        run: |
          node - <<'NODE'
          const fs=require('fs'),vm=require('vm');
          const s=fs.readFileSync('problem-app/index.html','utf8');
          for(const id of ['qb-data','meta-data']){const m=new RegExp(`<script\\s+id=["']${id}["'][^>]*>([\\s\\S]*?)<\\/script>`,'i').exec(s);if(!m)throw new Error(id+' missing');JSON.parse(m[1]);}
          const scripts=[...s.matchAll(/<script(?![^>]*type=["']application\/json["'])[^>]*>([\s\S]*?)<\/script>/gi)].map(m=>m[1]).filter(x=>x.trim());
          for(const code of scripts)new vm.Script(code);
          console.log('JSON + runtime syntax PASS');
          NODE
      - name: Commit repair
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add problem-app/index.html tools/install_vocab_safe_fallback_v1.mjs tools/extend_vocab_safe_fallback_core_grammar.mjs audit/VOCAB_SAFE_FALLBACK_BE_PRESENT_CATEGORY_REPAIR.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'fix: map be-present fallback to runtime category [skip ci]'
          git pull --rebase origin fix-vocab-grammar-range-20260825
          git push
