name: Repair problem content core families

on:
  workflow_dispatch:
  push:
    branches:
      - fix-problem-content-quality-20260828
    paths:
      - 'tools/repair_problem_content_quality_core_families.mjs'
      - '.github/workflows/repair-problem-content-core-families.yml'

permissions:
  contents: write

concurrency:
  group: repair-problem-content-core-families
  cancel-in-progress: false

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix-problem-content-quality-20260828
          fetch-depth: 0
      - name: Sync latest branch
        run: |
          git fetch origin fix-problem-content-quality-20260828
          git reset --hard origin/fix-problem-content-quality-20260828
      - name: Repair high-confidence content families
        run: node tools/repair_problem_content_quality_core_families.mjs
      - name: Refresh content-quality evidence
        shell: bash
        run: |
          set +e
          node tools/audit_problem_content_quality.mjs
          echo "audit_exit=$?"
          node tools/summarize_problem_content_templates.mjs
          exit 0
      - name: Commit repaired bank and evidence
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add problem-app/index.html audit/PROBLEM_APP_CONTENT_CORE_REPAIR.json audit/PROBLEM_APP_CONTENT_QUALITY.json audit/PROBLEM_APP_CONTENT_TEMPLATE_SUMMARY.json
          if git diff --cached --quiet; then
            echo 'No repair changes'
          else
            git commit -m 'fix: repair high-confidence problem content families [skip ci]'
            git pull --rebase origin fix-problem-content-quality-20260828
            git push origin HEAD:fix-problem-content-quality-20260828
          fi
