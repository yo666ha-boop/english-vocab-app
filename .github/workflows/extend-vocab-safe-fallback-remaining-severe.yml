name: Extend vocab-safe fallback remaining severe

on:
  push:
    branches:
      - fix-vocab-grammar-range-20260825
    paths:
      - tools/extend_vocab_safe_fallback_remaining_severe.mjs
      - .github/workflows/extend-vocab-safe-fallback-remaining-severe.yml
      - audit/VOCAB_SAFE_FALLBACK_REMAINING_SEVERE_TRIGGER.md

permissions:
  contents: write

concurrency:
  group: extend-vocab-safe-fallback-remaining-severe-${{ github.ref }}
  cancel-in-progress: false

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix-vocab-grammar-range-20260825
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Sync branch
        run: |
          git fetch origin fix-vocab-grammar-range-20260825
          git reset --hard origin/fix-vocab-grammar-range-20260825
      - name: Extend remaining severe safe fallback coverage
        run: node tools/extend_vocab_safe_fallback_remaining_severe.mjs
      - name: Validate embedded JSON and runtime syntax
        run: |
          node - <<'NODE'
          const fs=require('fs'),vm=require('vm');
          const s=fs.readFileSync('problem-app/index.html','utf8');
          for(const id of ['qb-data','meta-data']){const m=new RegExp(`<script\\s+id=["']${id}["'][^>]*>([\\s\\S]*?)<\\/script>`,'i').exec(s);if(!m)throw new Error(id+' missing');JSON.parse(m[1]);}
          const scripts=[...s.matchAll(/<script(?![^>]*type=["']application\/json["'])[^>]*>([\s\S]*?)<\/script>/gi)].map(m=>m[1]).filter(x=>x.trim());
          for(const code of scripts)new vm.Script(code);
          console.log('JSON + runtime syntax PASS');
          NODE
      - name: Commit repair
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add problem-app/index.html tools/install_vocab_safe_fallback_v1.mjs audit/VOCAB_SAFE_FALLBACK_REMAINING_SEVERE_REPAIR.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'fix: add generic fallbacks for remaining severe grammar [skip ci]'
          git pull --rebase origin fix-vocab-grammar-range-20260825
          git push
