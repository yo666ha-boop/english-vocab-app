# Trigger learned-only zeroed-row audit

Identify all chronology-aware rows where OFF > 0 and ON = 0, then record their exact section/category and unresolved passMeta evidence. No problem-ID or count hard-code.
