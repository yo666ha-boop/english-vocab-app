{
  "generated_at": "2026-08-25T12:26:39.432Z",
  "html_bytes": 3794529,
  "hits": [
    {
      "needle": "function passesVocab",
      "index": 2582944,
      "context": "blob);\n  const a = document.createElement('a');\n  a.href = url;\n  a.download = 'mikamijuku_app_state_latest.json';\n  document.body.appendChild(a);\n  a.click();\n  a.remove();\n  setTimeout(() => URL.revokeObjectURL(url), 3000);\n  if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを書き出しました。';\n}\n\nfunction importStateFile(file) {\n  const fr = new FileReader();\n  fr.onload = () => {\n    try {\n      const state = JSON.parse(fr.result);\n      applyState(state);\n      saveState(true);\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを読み込みました。';\n    } catch (e) {\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データの読み込みに失敗しました。';\n    }\n  };\n  fr.readAsText(file, 'utf-8');\n}\n\nconst currentSubject = () => el('subjectSelect').value;\nconst currentGrade = () => el('gradeSelect').value;\nconst currentTextbook = () => el('textbookSelect').value;\nconst currentSectionIndex = () => Math.max(0, el('sectionSelect').selectedIndex);\nconst useVocabGate = () => el('vocabMode').value === 'on';\n\nfunction renderTabs() {\n  document.querySelectorAll('.tab').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.tab').forEach(x => x.classList.remove('active'));\n      document.querySelectorAll('.screen').forEach(x => x.classList.remove('active'));\n      btn.classList.add('active');\n      const target = document.getElementById('screen-' + btn.dataset.screen);\n      if (target) target.classList.add('active');\n    });\n  });\n}\n\nfunction renderTextbookOptions() {\n  el('textbookSelect').innerHTML = meta.textbooks.map(tb => `<option>${tb}</option>`).join('');\n}\n\nfunction renderSectionOptions() {\n  const subject = currentSubject();\n  const grade = currentGrade().replace('中','');\n  if (subject !== '英語') {\n    el('textbookWrap').classList.add('hidden');\n    el('sectionWrap').classList.add('hidden');\n    return;\n  }\n  el('textbookWrap').classList.remove('hidden');\n  el('sectionWrap').classList.remove('hidden');\n  const tb = currentTextbook() || meta.textbooks[0];\n  const sections = ((meta.sections[tb] || {})[grade] || []);\n  el('sectionSelect').innerHTML = sections.map(sec => `<option>${sec}</option>`).join('');\n}\n\nfunction renderStageOptions() {\n  const stages = subjectConfig[currentSubject()].stagesByGrade[currentGrade()] || [];\n  el('stageArea').innerHTML = stages.map(s => `<label class=\"ck\"><input type=\"checkbox\" data-stage value=\"${s}\" checked> <span>${s}</span></label>`).join('');\n}\n\nfunction renderTypeOptions() {\n  const types = subjectConfig[currentSubject()].types;\n  el('typeArea').innerHTML = types.map(t => `<label class=\"ck\"><input type=\"checkbox\" data-type value=\"${t}\" checked> <span>${t}</span></label>`).join('');\n}\n\nfunction selectedValues(selector) {\n  return Array.from(document.querySelectorAll(selector + ':checked')).map(x => x.value);\n}\n\nfunction selectedCategories() {\n  const stages = selectedValues('input[data-stage]');\n  if (!stages.length) return [];\n  const map = subjectConfig[currentSubject()].stageMap[currentGrade()] || {};\n  const set = new Set();\n  stages.forEach(stage => (map[stage] || [stage]).forEach(c => set.add(c)));\n  return Array.from(set);\n}\n\nfunction passesVocab(item, overrideMode) {\n  if (item.subject !== '英語') return true;\n  const mode = overrideMode || (useVocabGate() ? 'on' : 'off');\n  if (mode === 'off') return true;\n  if (item.grade !== currentGrade()) return true;\n  const rec = meta.passMeta[item.id] || {};\n  const minIdx = rec[currentTextbook()];\n  if (meta.vocabCoordinateVersion !== 'v7-2026-08-18-1based') return false;\n  if (minIdx === -2) return true;\n  if (!Number.isInteger(minIdx) || minIdx <= 0) return false;\n  const selectedOrdinal = currentSectionIndex() + 1;\n  return minIdx <= selectedOrdinal;\n}\n\n// ---- Mikami prerequisite grammar + runtime quality gates ----\nfunction selectedStageCeiling() {\n  if (currentSubject() !== '英語') return Number.MAX_SAFE_INTEGER;\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  const selected = selectedValues('input[data-stage]');\n  if (!selected.length) return -1;\n  return selected.reduce((max, s) => Math.max(max, stages.indexOf(s)), -1);\n}\n\nfunction stageIndexByNameContains(...needles) {\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  for (let i = 0; i < stages.length; i++) {\n    const s = stages[i];\n    if (needles.some(n => s.includes(n))) return i;\n  }\n  return -1;\n}\n\nfunction targetStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const cfg = subjectConfig['英語'];\n  const stages = cfg.stagesByGrade[item.grade] || [];\n  const map = cfg.stageMap[item.grade] || {};\n  for (let i = 0; i < stages.length; i++) {\n    const cats = map[stages[i]] || [stages[i]];\n    if (cats.includes(item.category)) return i;\n  }\n  return -1;\n}\n\nfunction requiredGrammarStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const text = String(item.q || '') + ' ' + String(item.a || '');\n  let req = Math.max(0, targetStageIndex(item));\n  const bump = i => { if (i >= 0) req = Math.max(req, i); };\n\n  // Detect only structures that can make an otherwise earlier problem depend on a later grammar unit.\n  if (/\\b(will|won't)\\b|\\b(am|is|are) going to\\b/i.test(text)) bump(stageIndexByNameContains('未来'));\n  if (/\\b(was|were)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('過去進行'));\n  if (/\\b(am|is|are)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('進行形'));\n  if (/\\b(can|must|may|should)\\b/i.test(text)) bump(stageIndexByNameContains('助動詞','can'));\n  if (/\\bto\\s+(?:be|have|do|play|study|read|write|watch|visit|see|eat|drink|go|come|make|use|help|learn|practice|speak|listen|buy|call|join|take|work|live|want|need|try|start|finish)\\b/i.test(text)) bump(stageIndexByNameContains('不定詞①','不定詞②','不定詞'));\n  if (/\\b(?:enjoy|enjoys|enjoyed|finish|finishes|finished|stop|stops|stopped)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('動名詞','不定詞①，動名詞'));\n  if (/\\b(?:than|as\\s+\\w+\\s+as|more\\s+\\w+\\s+than|most\\s+\\w+)\\b/i.test(text)) bump(stageIndexByNameContains('比較'));\n  if (/\\b(am|is|are|was|were)\\s+(?:made|used|written|spoken|known|called|played|studied|visited|built|seen|loved)\\b/i.test(text)) bump(stageIndexByNameContains('受動態'));\n  if (/\\b(?:have|has)\\s+(?:ever\\s+|never\\s+|already\\s+|just\\s+)?(?:been|visited|finished|lost|lived|studied|seen|done|written|read|known|used|played|gone|made|eaten|had)\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形'));\n  if (/\\b(?:have|has) been [A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形（継続）'));\n  if (/\\b(?:who|which|that)\\s+(?:is|are|was|were|has|have|can|will|[A-Za-z]+s?)(?:\\s|\\?)/i.test(text) && /\\b(?:boy|girl|man|woman|teacher|student|person|book|bag|picture|pen|flower|thing)\\b/i.test(text)) bump(stageIndexByNameContains('関係代名詞'));\n  if (/\\bif\\b[\\s\\S]*\\bwould\\b|\\bI wish\\b/i.test(text)) bump(stageIndexByNameContains('仮定法'));\n  return req;\n}\n\nfunction passesPrereqGrammar(item) {\n  if (item.subject !== '英語') return true;\n  const ceiling = selectedStageCeiling();\n  if (ceiling < 0) return false;\n  const required = requiredGrammarStageIndex(item);\n  return required < 0 || required <= ceiling;\n}\n\nfunction passesQualityGate(item) {\n  if (!item || !String(item.q || '').trim() || !String(item.a || '').trim()) return false;\n  if (item.subject !== '英語') return true;\n  const q = String(item.q || '');\n  const a = String(item.a || '');\n  const both = q + ' ' + a;\n\n  // Known invalid / non-unique generation families.\n  if (/This is (?:he|she|we)\\b/i.test(both)) return false;\n  if (/\\bthan He\\b/.test(both)) return false;\n  if (/\\b(?:Mika|She) (?:love|begin|stop)\\b/.test(both)) return false;\n  if (/Do you have (?:visited|been|finished|lost|lived|studied|seen|done)/i.test(both)) return false;\n  if (/Did You ne\\b|Do You\\b|Do They\\b|Does She\\b|Does He\\b/.test(both)) return false;\n  if (/否定文または疑問文/.test(q)) return false;\n  if (/日本語で説明しなさい/.test(q) && /^[A-Za-z]/.test(a)) return false;\n  if (/\\(\\s*she\\s*\\/\\s*her\\s*\\/\\s*her\\s*\\)/i.test(q)) return false;\n  if (/\\(\\s*he\\s*\\/\\s*him\\s*\\/\\s*him\\s*\\)/i.test(q)) return false;\n  if (String(item.id || '').startsWith('M2-INF2-')) {\n    if (item.type === '空所補充' && /^to$/i.test(a) && /\\bto\\s*\\(\\s*\\)\\s*(?=[A-Za-z])/i.test(q)) return false;\n    if (/^(?:He|She|Yuki|Mika|Takumi|Ken|Emi|Tom|The student|The teacher|This boy|This girl|My mother|My father|My brother|My sister|My friend) (?:want|need|like) to\\b/i.test(a)) return false;\n  }\n\n  // Target-grammar evidence: do not let a mislabeled vocabulary-only item through.\n  if (item.category === '動名詞' && !/[A-Za-z]+ing\\b/.test(both)) return false;\n  if (item.category === '比較' && !/\\bthan\\b|\\bas\\s+\\w+\\s+as\\b|\\bmost\\b|\\w+est\\b/i.test(both)) return false;\n  const infinitiveBlank = item.type === '空所補充' && /^to$/i.test(a) && /\\(\\s*\\)\\s*[A-Za-z]+\\b/.test(q);\n  if ((item.category === '不定詞' || item.category === '不定詞①' || item.category === '不定詞②') && !/\\bto\\s+[A-Za-z]+\\b/i.test(both) && !infinitiveBlank) return false;\n  if (String(item.category).startsWith('現在完了形') && !/\\b(?:have|has)\\b/i.test(both)) return false;\n  if (item.category === '関係代名詞') {\n    if (!/\\b(?:who|which|that)\\b/i.test(both)) return false;\n    if (item.type === '空所補充' && /\\(\\s*\\)/.test(q)) {\n      if (!/who\\s*\\/\\s*which/i.test(q)) return false;\n      if (!/^(?:who|which)$/i.test(a)) return false;\n    }\n  }\n  return true;\n}\n\nfunction baseFiltered(overrideMode) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const categories = selectedCategories();\n  const types = selectedValues('input[data-type]');\n  return qb.filter(item => {\n    if (item.subject !== subj) return false;\n    if (item.grade !== grade) return false;\n    if (categories.length && !categories.includes(item.category)) return false;\n    if (types.length && !types.includes(item.type)) return false;\n    if (!passesVocab(item, overrideMode)) return false;\n    if (!passesPrereqGrammar(item)) return false;\n    if (!passesQualityGate(item)) return false;\n    return true;\n  });\n}\n\nfunction shuffle(arr) {\n  const a = arr.slice();\n  for (let i=a.length-1;i>0;i--) {\n    const j = Math.floor(Math.random() * (i+1));\n    const tmp = a[i]; a[i] = a[j]; a[j] = tmp;\n  }\n  return a;\n}\n\nfunction updateBuilderStatus(candidates) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const sameGradeCount = qb.filter(x => x.subject === subj && x.grade === grade)."
    },
    {
      "needle": "function passesPrereqGrammar",
      "index": 2586684,
      "context": " 1;\n  return minIdx <= selectedOrdinal;\n}\n\n// ---- Mikami prerequisite grammar + runtime quality gates ----\nfunction selectedStageCeiling() {\n  if (currentSubject() !== '英語') return Number.MAX_SAFE_INTEGER;\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  const selected = selectedValues('input[data-stage]');\n  if (!selected.length) return -1;\n  return selected.reduce((max, s) => Math.max(max, stages.indexOf(s)), -1);\n}\n\nfunction stageIndexByNameContains(...needles) {\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  for (let i = 0; i < stages.length; i++) {\n    const s = stages[i];\n    if (needles.some(n => s.includes(n))) return i;\n  }\n  return -1;\n}\n\nfunction targetStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const cfg = subjectConfig['英語'];\n  const stages = cfg.stagesByGrade[item.grade] || [];\n  const map = cfg.stageMap[item.grade] || {};\n  for (let i = 0; i < stages.length; i++) {\n    const cats = map[stages[i]] || [stages[i]];\n    if (cats.includes(item.category)) return i;\n  }\n  return -1;\n}\n\nfunction requiredGrammarStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const text = String(item.q || '') + ' ' + String(item.a || '');\n  let req = Math.max(0, targetStageIndex(item));\n  const bump = i => { if (i >= 0) req = Math.max(req, i); };\n\n  // Detect only structures that can make an otherwise earlier problem depend on a later grammar unit.\n  if (/\\b(will|won't)\\b|\\b(am|is|are) going to\\b/i.test(text)) bump(stageIndexByNameContains('未来'));\n  if (/\\b(was|were)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('過去進行'));\n  if (/\\b(am|is|are)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('進行形'));\n  if (/\\b(can|must|may|should)\\b/i.test(text)) bump(stageIndexByNameContains('助動詞','can'));\n  if (/\\bto\\s+(?:be|have|do|play|study|read|write|watch|visit|see|eat|drink|go|come|make|use|help|learn|practice|speak|listen|buy|call|join|take|work|live|want|need|try|start|finish)\\b/i.test(text)) bump(stageIndexByNameContains('不定詞①','不定詞②','不定詞'));\n  if (/\\b(?:enjoy|enjoys|enjoyed|finish|finishes|finished|stop|stops|stopped)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('動名詞','不定詞①，動名詞'));\n  if (/\\b(?:than|as\\s+\\w+\\s+as|more\\s+\\w+\\s+than|most\\s+\\w+)\\b/i.test(text)) bump(stageIndexByNameContains('比較'));\n  if (/\\b(am|is|are|was|were)\\s+(?:made|used|written|spoken|known|called|played|studied|visited|built|seen|loved)\\b/i.test(text)) bump(stageIndexByNameContains('受動態'));\n  if (/\\b(?:have|has)\\s+(?:ever\\s+|never\\s+|already\\s+|just\\s+)?(?:been|visited|finished|lost|lived|studied|seen|done|written|read|known|used|played|gone|made|eaten|had)\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形'));\n  if (/\\b(?:have|has) been [A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形（継続）'));\n  if (/\\b(?:who|which|that)\\s+(?:is|are|was|were|has|have|can|will|[A-Za-z]+s?)(?:\\s|\\?)/i.test(text) && /\\b(?:boy|girl|man|woman|teacher|student|person|book|bag|picture|pen|flower|thing)\\b/i.test(text)) bump(stageIndexByNameContains('関係代名詞'));\n  if (/\\bif\\b[\\s\\S]*\\bwould\\b|\\bI wish\\b/i.test(text)) bump(stageIndexByNameContains('仮定法'));\n  return req;\n}\n\nfunction passesPrereqGrammar(item) {\n  if (item.subject !== '英語') return true;\n  const ceiling = selectedStageCeiling();\n  if (ceiling < 0) return false;\n  const required = requiredGrammarStageIndex(item);\n  return required < 0 || required <= ceiling;\n}\n\nfunction passesQualityGate(item) {\n  if (!item || !String(item.q || '').trim() || !String(item.a || '').trim()) return false;\n  if (item.subject !== '英語') return true;\n  const q = String(item.q || '');\n  const a = String(item.a || '');\n  const both = q + ' ' + a;\n\n  // Known invalid / non-unique generation families.\n  if (/This is (?:he|she|we)\\b/i.test(both)) return false;\n  if (/\\bthan He\\b/.test(both)) return false;\n  if (/\\b(?:Mika|She) (?:love|begin|stop)\\b/.test(both)) return false;\n  if (/Do you have (?:visited|been|finished|lost|lived|studied|seen|done)/i.test(both)) return false;\n  if (/Did You ne\\b|Do You\\b|Do They\\b|Does She\\b|Does He\\b/.test(both)) return false;\n  if (/否定文または疑問文/.test(q)) return false;\n  if (/日本語で説明しなさい/.test(q) && /^[A-Za-z]/.test(a)) return false;\n  if (/\\(\\s*she\\s*\\/\\s*her\\s*\\/\\s*her\\s*\\)/i.test(q)) return false;\n  if (/\\(\\s*he\\s*\\/\\s*him\\s*\\/\\s*him\\s*\\)/i.test(q)) return false;\n  if (String(item.id || '').startsWith('M2-INF2-')) {\n    if (item.type === '空所補充' && /^to$/i.test(a) && /\\bto\\s*\\(\\s*\\)\\s*(?=[A-Za-z])/i.test(q)) return false;\n    if (/^(?:He|She|Yuki|Mika|Takumi|Ken|Emi|Tom|The student|The teacher|This boy|This girl|My mother|My father|My brother|My sister|My friend) (?:want|need|like) to\\b/i.test(a)) return false;\n  }\n\n  // Target-grammar evidence: do not let a mislabeled vocabulary-only item through.\n  if (item.category === '動名詞' && !/[A-Za-z]+ing\\b/.test(both)) return false;\n  if (item.category === '比較' && !/\\bthan\\b|\\bas\\s+\\w+\\s+as\\b|\\bmost\\b|\\w+est\\b/i.test(both)) return false;\n  const infinitiveBlank = item.type === '空所補充' && /^to$/i.test(a) && /\\(\\s*\\)\\s*[A-Za-z]+\\b/.test(q);\n  if ((item.category === '不定詞' || item.category === '不定詞①' || item.category === '不定詞②') && !/\\bto\\s+[A-Za-z]+\\b/i.test(both) && !infinitiveBlank) return false;\n  if (String(item.category).startsWith('現在完了形') && !/\\b(?:have|has)\\b/i.test(both)) return false;\n  if (item.category === '関係代名詞') {\n    if (!/\\b(?:who|which|that)\\b/i.test(both)) return false;\n    if (item.type === '空所補充' && /\\(\\s*\\)/.test(q)) {\n      if (!/who\\s*\\/\\s*which/i.test(q)) return false;\n      if (!/^(?:who|which)$/i.test(a)) return false;\n    }\n  }\n  return true;\n}\n\nfunction baseFiltered(overrideMode) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const categories = selectedCategories();\n  const types = selectedValues('input[data-type]');\n  return qb.filter(item => {\n    if (item.subject !== subj) return false;\n    if (item.grade !== grade) return false;\n    if (categories.length && !categories.includes(item.category)) return false;\n    if (types.length && !types.includes(item.type)) return false;\n    if (!passesVocab(item, overrideMode)) return false;\n    if (!passesPrereqGrammar(item)) return false;\n    if (!passesQualityGate(item)) return false;\n    return true;\n  });\n}\n\nfunction shuffle(arr) {\n  const a = arr.slice();\n  for (let i=a.length-1;i>0;i--) {\n    const j = Math.floor(Math.random() * (i+1));\n    const tmp = a[i]; a[i] = a[j]; a[j] = tmp;\n  }\n  return a;\n}\n\nfunction updateBuilderStatus(candidates) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const sameGradeCount = qb.filter(x => x.subject === subj && x.grade === grade).length;\n  let msg = '全問題 ' + meta.counts.all + '件 / ' + grade + 'の' + subj + '元データ ' + sameGradeCount + '件 / この条件で使える候補 ' + candidates.length + '件';\n  if (subj === '英語') {\n    msg += ' / 教科書: ' + currentTextbook() + ' / 単語はここまで: ' + (el('sectionSelect').value || '未選択');\n  }\n  el('builderStatus').textContent = msg;\n  el('builderStatus').classList.toggle('good', candidates.length > 0);\n}\n\nfunction renderQuestions() {\n  const count = parseInt(el('questionCount').value, 10);\n  const pickMode = el('pickMode').value;\n  const candidates = baseFiltered();\n  updateBuilderStatus(candidates);\n  if (!candidates.length) {\n    currentQuestions = [];\n    el('questionPreview').innerHTML = 'この条件では問題が見つかりませんでした。単語制限を外すか、範囲や問題タイプを広げてください。';\n    return;\n  }\n  const picked = (pickMode === 'random' ? shuffle(candidates) : candidates).slice(0, count);\n  currentQuestions = picked;\n  el('questionPreview').classList.toggle('showAnswers', showAnswers);\n  el('questionPreview').innerHTML = picked.map((q, i) => `\n    <div class=\"q\">\n      <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n      <div>${q.q}</div>\n      <div class=\"ans\">答え: ${q.a}</div>\n    </div>\n  `).join('');\n}\n\nfunction printQuestions() {\n  if (!currentQuestions.length) return;\n  const escapeHtml = value => String(value ?? '').replace(/[&<>\"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[char]));\n  const renderQuestion = (q, index) => `<div class=\"q\"><div class=\"n\">Q${index + 1}　${escapeHtml(q.id)}</div><div class=\"body\">${escapeHtml(q.q)}</div>${showAnswers ? `<div class=\"a\">答え: ${escapeHtml(q.a)}</div>` : '<div class=\"answer-space\" aria-label=\"解答欄\"></div>'}</div>`;\n  const pages = [];\n  for (let startIndex = 0; startIndex < currentQuestions.length; startIndex += 10) {\n    const pageQuestions = currentQuestions.slice(startIndex, startIndex + 10);\n    pages.push({\n      html: pageQuestions.map((q, offset) => renderQuestion(q, startIndex + offset)).join(''),\n      rows: Math.ceil(pageQuestions.length / 2)\n    });\n  }\n  const printHtml = `<!doctype html><html lang=\"ja\"><head><meta charset=\"utf-8\"><title>問題プリント</title><style>\n  @page{size:A4 portrait;margin:8mm}\n  *{box-sizing:border-box}\n  body{font-family:-apple-system,BlinkMacSystemFont,\"Segoe UI\",\"Yu Gothic\",sans-serif;margin:0;color:#111;font-size:9pt;line-height:1.3}\n  .page{height:281mm;display:flex;flex-direction:column;overflow:hidden;page-break-after:always;break-after:page}\n  .page:last-child{page-break-after:auto;break-after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.on"
    },
    {
      "needle": "function requiredGrammarStageIndex",
      "index": 2584567,
      "context": "ption>${tb}</option>`).join('');\n}\n\nfunction renderSectionOptions() {\n  const subject = currentSubject();\n  const grade = currentGrade().replace('中','');\n  if (subject !== '英語') {\n    el('textbookWrap').classList.add('hidden');\n    el('sectionWrap').classList.add('hidden');\n    return;\n  }\n  el('textbookWrap').classList.remove('hidden');\n  el('sectionWrap').classList.remove('hidden');\n  const tb = currentTextbook() || meta.textbooks[0];\n  const sections = ((meta.sections[tb] || {})[grade] || []);\n  el('sectionSelect').innerHTML = sections.map(sec => `<option>${sec}</option>`).join('');\n}\n\nfunction renderStageOptions() {\n  const stages = subjectConfig[currentSubject()].stagesByGrade[currentGrade()] || [];\n  el('stageArea').innerHTML = stages.map(s => `<label class=\"ck\"><input type=\"checkbox\" data-stage value=\"${s}\" checked> <span>${s}</span></label>`).join('');\n}\n\nfunction renderTypeOptions() {\n  const types = subjectConfig[currentSubject()].types;\n  el('typeArea').innerHTML = types.map(t => `<label class=\"ck\"><input type=\"checkbox\" data-type value=\"${t}\" checked> <span>${t}</span></label>`).join('');\n}\n\nfunction selectedValues(selector) {\n  return Array.from(document.querySelectorAll(selector + ':checked')).map(x => x.value);\n}\n\nfunction selectedCategories() {\n  const stages = selectedValues('input[data-stage]');\n  if (!stages.length) return [];\n  const map = subjectConfig[currentSubject()].stageMap[currentGrade()] || {};\n  const set = new Set();\n  stages.forEach(stage => (map[stage] || [stage]).forEach(c => set.add(c)));\n  return Array.from(set);\n}\n\nfunction passesVocab(item, overrideMode) {\n  if (item.subject !== '英語') return true;\n  const mode = overrideMode || (useVocabGate() ? 'on' : 'off');\n  if (mode === 'off') return true;\n  if (item.grade !== currentGrade()) return true;\n  const rec = meta.passMeta[item.id] || {};\n  const minIdx = rec[currentTextbook()];\n  if (meta.vocabCoordinateVersion !== 'v7-2026-08-18-1based') return false;\n  if (minIdx === -2) return true;\n  if (!Number.isInteger(minIdx) || minIdx <= 0) return false;\n  const selectedOrdinal = currentSectionIndex() + 1;\n  return minIdx <= selectedOrdinal;\n}\n\n// ---- Mikami prerequisite grammar + runtime quality gates ----\nfunction selectedStageCeiling() {\n  if (currentSubject() !== '英語') return Number.MAX_SAFE_INTEGER;\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  const selected = selectedValues('input[data-stage]');\n  if (!selected.length) return -1;\n  return selected.reduce((max, s) => Math.max(max, stages.indexOf(s)), -1);\n}\n\nfunction stageIndexByNameContains(...needles) {\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  for (let i = 0; i < stages.length; i++) {\n    const s = stages[i];\n    if (needles.some(n => s.includes(n))) return i;\n  }\n  return -1;\n}\n\nfunction targetStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const cfg = subjectConfig['英語'];\n  const stages = cfg.stagesByGrade[item.grade] || [];\n  const map = cfg.stageMap[item.grade] || {};\n  for (let i = 0; i < stages.length; i++) {\n    const cats = map[stages[i]] || [stages[i]];\n    if (cats.includes(item.category)) return i;\n  }\n  return -1;\n}\n\nfunction requiredGrammarStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const text = String(item.q || '') + ' ' + String(item.a || '');\n  let req = Math.max(0, targetStageIndex(item));\n  const bump = i => { if (i >= 0) req = Math.max(req, i); };\n\n  // Detect only structures that can make an otherwise earlier problem depend on a later grammar unit.\n  if (/\\b(will|won't)\\b|\\b(am|is|are) going to\\b/i.test(text)) bump(stageIndexByNameContains('未来'));\n  if (/\\b(was|were)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('過去進行'));\n  if (/\\b(am|is|are)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('進行形'));\n  if (/\\b(can|must|may|should)\\b/i.test(text)) bump(stageIndexByNameContains('助動詞','can'));\n  if (/\\bto\\s+(?:be|have|do|play|study|read|write|watch|visit|see|eat|drink|go|come|make|use|help|learn|practice|speak|listen|buy|call|join|take|work|live|want|need|try|start|finish)\\b/i.test(text)) bump(stageIndexByNameContains('不定詞①','不定詞②','不定詞'));\n  if (/\\b(?:enjoy|enjoys|enjoyed|finish|finishes|finished|stop|stops|stopped)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('動名詞','不定詞①，動名詞'));\n  if (/\\b(?:than|as\\s+\\w+\\s+as|more\\s+\\w+\\s+than|most\\s+\\w+)\\b/i.test(text)) bump(stageIndexByNameContains('比較'));\n  if (/\\b(am|is|are|was|were)\\s+(?:made|used|written|spoken|known|called|played|studied|visited|built|seen|loved)\\b/i.test(text)) bump(stageIndexByNameContains('受動態'));\n  if (/\\b(?:have|has)\\s+(?:ever\\s+|never\\s+|already\\s+|just\\s+)?(?:been|visited|finished|lost|lived|studied|seen|done|written|read|known|used|played|gone|made|eaten|had)\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形'));\n  if (/\\b(?:have|has) been [A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形（継続）'));\n  if (/\\b(?:who|which|that)\\s+(?:is|are|was|were|has|have|can|will|[A-Za-z]+s?)(?:\\s|\\?)/i.test(text) && /\\b(?:boy|girl|man|woman|teacher|student|person|book|bag|picture|pen|flower|thing)\\b/i.test(text)) bump(stageIndexByNameContains('関係代名詞'));\n  if (/\\bif\\b[\\s\\S]*\\bwould\\b|\\bI wish\\b/i.test(text)) bump(stageIndexByNameContains('仮定法'));\n  return req;\n}\n\nfunction passesPrereqGrammar(item) {\n  if (item.subject !== '英語') return true;\n  const ceiling = selectedStageCeiling();\n  if (ceiling < 0) return false;\n  const required = requiredGrammarStageIndex(item);\n  return required < 0 || required <= ceiling;\n}\n\nfunction passesQualityGate(item) {\n  if (!item || !String(item.q || '').trim() || !String(item.a || '').trim()) return false;\n  if (item.subject !== '英語') return true;\n  const q = String(item.q || '');\n  const a = String(item.a || '');\n  const both = q + ' ' + a;\n\n  // Known invalid / non-unique generation families.\n  if (/This is (?:he|she|we)\\b/i.test(both)) return false;\n  if (/\\bthan He\\b/.test(both)) return false;\n  if (/\\b(?:Mika|She) (?:love|begin|stop)\\b/.test(both)) return false;\n  if (/Do you have (?:visited|been|finished|lost|lived|studied|seen|done)/i.test(both)) return false;\n  if (/Did You ne\\b|Do You\\b|Do They\\b|Does She\\b|Does He\\b/.test(both)) return false;\n  if (/否定文または疑問文/.test(q)) return false;\n  if (/日本語で説明しなさい/.test(q) && /^[A-Za-z]/.test(a)) return false;\n  if (/\\(\\s*she\\s*\\/\\s*her\\s*\\/\\s*her\\s*\\)/i.test(q)) return false;\n  if (/\\(\\s*he\\s*\\/\\s*him\\s*\\/\\s*him\\s*\\)/i.test(q)) return false;\n  if (String(item.id || '').startsWith('M2-INF2-')) {\n    if (item.type === '空所補充' && /^to$/i.test(a) && /\\bto\\s*\\(\\s*\\)\\s*(?=[A-Za-z])/i.test(q)) return false;\n    if (/^(?:He|She|Yuki|Mika|Takumi|Ken|Emi|Tom|The student|The teacher|This boy|This girl|My mother|My father|My brother|My sister|My friend) (?:want|need|like) to\\b/i.test(a)) return false;\n  }\n\n  // Target-grammar evidence: do not let a mislabeled vocabulary-only item through.\n  if (item.category === '動名詞' && !/[A-Za-z]+ing\\b/.test(both)) return false;\n  if (item.category === '比較' && !/\\bthan\\b|\\bas\\s+\\w+\\s+as\\b|\\bmost\\b|\\w+est\\b/i.test(both)) return false;\n  const infinitiveBlank = item.type === '空所補充' && /^to$/i.test(a) && /\\(\\s*\\)\\s*[A-Za-z]+\\b/.test(q);\n  if ((item.category === '不定詞' || item.category === '不定詞①' || item.category === '不定詞②') && !/\\bto\\s+[A-Za-z]+\\b/i.test(both) && !infinitiveBlank) return false;\n  if (String(item.category).startsWith('現在完了形') && !/\\b(?:have|has)\\b/i.test(both)) return false;\n  if (item.category === '関係代名詞') {\n    if (!/\\b(?:who|which|that)\\b/i.test(both)) return false;\n    if (item.type === '空所補充' && /\\(\\s*\\)/.test(q)) {\n      if (!/who\\s*\\/\\s*which/i.test(q)) return false;\n      if (!/^(?:who|which)$/i.test(a)) return false;\n    }\n  }\n  return true;\n}\n\nfunction baseFiltered(overrideMode) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const categories = selectedCategories();\n  const types = selectedValues('input[data-type]');\n  return qb.filter(item => {\n    if (item.subject !== subj) return false;\n    if (item.grade !== grade) return false;\n    if (categories.length && !categories.includes(item.category)) return false;\n    if (types.length && !types.includes(item.type)) return false;\n    if (!passesVocab(item, overrideMode)) return false;\n    if (!passesPrereqGrammar(item)) return false;\n    if (!passesQualityGate(item)) return false;\n    return true;\n  });\n}\n\nfunction shuffle(arr) {\n  const a = arr.slice();\n  for (let i=a.length-1;i>0;i--) {\n    const j = Math.floor(Math.random() * (i+1));\n    const tmp = a[i]; a[i] = a[j]; a[j] = tmp;\n  }\n  return a;\n}\n\nfunction updateBuilderStatus(candidates) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const sameGradeCount = qb.filter(x => x.subject === subj && x.grade === grade).length;\n  let msg = '全問題 ' + meta.counts.all + '件 / ' + grade + 'の' + subj + '元データ ' + sameGradeCount + '件 / この条件で使える候補 ' + candidates.length + '件';\n  if (subj === '英語') {\n    msg += ' / 教科書: ' + currentTextbook() + ' / 単語はここまで: ' + (el('sectionSelect').value || '未選択');\n  }\n  el('builderStatus').textContent = msg;\n  el('builderStatus').classList.toggle('good', candidates.length > 0);\n}\n\nfunction renderQuestions() {\n  const count = parseInt(el('questionCount').value, 10);\n  const pickMode = el('pickMode').value;\n  const candidates = baseFiltered();\n  updateBuilderStatus(candidates);\n  if (!candidates.length) {\n    currentQuestions = [];\n    el('questionPreview').innerHTML = 'この条件では問題が見つかりませんでした。単語制限を外すか、範囲や問題タイプを広げてください。';\n    return;\n  }\n  const picked = (pickMode === 'random' ? shuffle(candidates) : candidates).slice(0, count);\n  currentQuestions = picked;\n  el('questionPreview').classList.toggle('showAnswers', showAnswers);\n  el('questionPreview').innerHTML = picked.map((q, i) => `\n    <div class=\"q\">\n      <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n      <div>${q.q}</div>\n      <div class=\"ans\">答え: ${q.a}</div>\n    </div>\n  `).join('');\n}\n\nfunction printQuestions() {\n  if (!currentQuestions.length) return;\n  const escapeHtml = value => String(value ?? '').replace(/[&<>\"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[char]));\n  const renderQuestion = (q, index) => `<div class=\"q\"><div class=\"n\">Q${index + 1}　${escapeHtml(q.id)}</div><div class=\"body\">${escapeHtml(q.q)}</div>${showAnswers ? `<div class=\"a\">答え: ${escapeHtml(q.a)}</div"
    },
    {
      "needle": "function baseFiltered",
      "index": 2589134,
      "context": "een|loved)\\b/i.test(text)) bump(stageIndexByNameContains('受動態'));\n  if (/\\b(?:have|has)\\s+(?:ever\\s+|never\\s+|already\\s+|just\\s+)?(?:been|visited|finished|lost|lived|studied|seen|done|written|read|known|used|played|gone|made|eaten|had)\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形'));\n  if (/\\b(?:have|has) been [A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形（継続）'));\n  if (/\\b(?:who|which|that)\\s+(?:is|are|was|were|has|have|can|will|[A-Za-z]+s?)(?:\\s|\\?)/i.test(text) && /\\b(?:boy|girl|man|woman|teacher|student|person|book|bag|picture|pen|flower|thing)\\b/i.test(text)) bump(stageIndexByNameContains('関係代名詞'));\n  if (/\\bif\\b[\\s\\S]*\\bwould\\b|\\bI wish\\b/i.test(text)) bump(stageIndexByNameContains('仮定法'));\n  return req;\n}\n\nfunction passesPrereqGrammar(item) {\n  if (item.subject !== '英語') return true;\n  const ceiling = selectedStageCeiling();\n  if (ceiling < 0) return false;\n  const required = requiredGrammarStageIndex(item);\n  return required < 0 || required <= ceiling;\n}\n\nfunction passesQualityGate(item) {\n  if (!item || !String(item.q || '').trim() || !String(item.a || '').trim()) return false;\n  if (item.subject !== '英語') return true;\n  const q = String(item.q || '');\n  const a = String(item.a || '');\n  const both = q + ' ' + a;\n\n  // Known invalid / non-unique generation families.\n  if (/This is (?:he|she|we)\\b/i.test(both)) return false;\n  if (/\\bthan He\\b/.test(both)) return false;\n  if (/\\b(?:Mika|She) (?:love|begin|stop)\\b/.test(both)) return false;\n  if (/Do you have (?:visited|been|finished|lost|lived|studied|seen|done)/i.test(both)) return false;\n  if (/Did You ne\\b|Do You\\b|Do They\\b|Does She\\b|Does He\\b/.test(both)) return false;\n  if (/否定文または疑問文/.test(q)) return false;\n  if (/日本語で説明しなさい/.test(q) && /^[A-Za-z]/.test(a)) return false;\n  if (/\\(\\s*she\\s*\\/\\s*her\\s*\\/\\s*her\\s*\\)/i.test(q)) return false;\n  if (/\\(\\s*he\\s*\\/\\s*him\\s*\\/\\s*him\\s*\\)/i.test(q)) return false;\n  if (String(item.id || '').startsWith('M2-INF2-')) {\n    if (item.type === '空所補充' && /^to$/i.test(a) && /\\bto\\s*\\(\\s*\\)\\s*(?=[A-Za-z])/i.test(q)) return false;\n    if (/^(?:He|She|Yuki|Mika|Takumi|Ken|Emi|Tom|The student|The teacher|This boy|This girl|My mother|My father|My brother|My sister|My friend) (?:want|need|like) to\\b/i.test(a)) return false;\n  }\n\n  // Target-grammar evidence: do not let a mislabeled vocabulary-only item through.\n  if (item.category === '動名詞' && !/[A-Za-z]+ing\\b/.test(both)) return false;\n  if (item.category === '比較' && !/\\bthan\\b|\\bas\\s+\\w+\\s+as\\b|\\bmost\\b|\\w+est\\b/i.test(both)) return false;\n  const infinitiveBlank = item.type === '空所補充' && /^to$/i.test(a) && /\\(\\s*\\)\\s*[A-Za-z]+\\b/.test(q);\n  if ((item.category === '不定詞' || item.category === '不定詞①' || item.category === '不定詞②') && !/\\bto\\s+[A-Za-z]+\\b/i.test(both) && !infinitiveBlank) return false;\n  if (String(item.category).startsWith('現在完了形') && !/\\b(?:have|has)\\b/i.test(both)) return false;\n  if (item.category === '関係代名詞') {\n    if (!/\\b(?:who|which|that)\\b/i.test(both)) return false;\n    if (item.type === '空所補充' && /\\(\\s*\\)/.test(q)) {\n      if (!/who\\s*\\/\\s*which/i.test(q)) return false;\n      if (!/^(?:who|which)$/i.test(a)) return false;\n    }\n  }\n  return true;\n}\n\nfunction baseFiltered(overrideMode) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const categories = selectedCategories();\n  const types = selectedValues('input[data-type]');\n  return qb.filter(item => {\n    if (item.subject !== subj) return false;\n    if (item.grade !== grade) return false;\n    if (categories.length && !categories.includes(item.category)) return false;\n    if (types.length && !types.includes(item.type)) return false;\n    if (!passesVocab(item, overrideMode)) return false;\n    if (!passesPrereqGrammar(item)) return false;\n    if (!passesQualityGate(item)) return false;\n    return true;\n  });\n}\n\nfunction shuffle(arr) {\n  const a = arr.slice();\n  for (let i=a.length-1;i>0;i--) {\n    const j = Math.floor(Math.random() * (i+1));\n    const tmp = a[i]; a[i] = a[j]; a[j] = tmp;\n  }\n  return a;\n}\n\nfunction updateBuilderStatus(candidates) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const sameGradeCount = qb.filter(x => x.subject === subj && x.grade === grade).length;\n  let msg = '全問題 ' + meta.counts.all + '件 / ' + grade + 'の' + subj + '元データ ' + sameGradeCount + '件 / この条件で使える候補 ' + candidates.length + '件';\n  if (subj === '英語') {\n    msg += ' / 教科書: ' + currentTextbook() + ' / 単語はここまで: ' + (el('sectionSelect').value || '未選択');\n  }\n  el('builderStatus').textContent = msg;\n  el('builderStatus').classList.toggle('good', candidates.length > 0);\n}\n\nfunction renderQuestions() {\n  const count = parseInt(el('questionCount').value, 10);\n  const pickMode = el('pickMode').value;\n  const candidates = baseFiltered();\n  updateBuilderStatus(candidates);\n  if (!candidates.length) {\n    currentQuestions = [];\n    el('questionPreview').innerHTML = 'この条件では問題が見つかりませんでした。単語制限を外すか、範囲や問題タイプを広げてください。';\n    return;\n  }\n  const picked = (pickMode === 'random' ? shuffle(candidates) : candidates).slice(0, count);\n  currentQuestions = picked;\n  el('questionPreview').classList.toggle('showAnswers', showAnswers);\n  el('questionPreview').innerHTML = picked.map((q, i) => `\n    <div class=\"q\">\n      <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n      <div>${q.q}</div>\n      <div class=\"ans\">答え: ${q.a}</div>\n    </div>\n  `).join('');\n}\n\nfunction printQuestions() {\n  if (!currentQuestions.length) return;\n  const escapeHtml = value => String(value ?? '').replace(/[&<>\"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[char]));\n  const renderQuestion = (q, index) => `<div class=\"q\"><div class=\"n\">Q${index + 1}　${escapeHtml(q.id)}</div><div class=\"body\">${escapeHtml(q.q)}</div>${showAnswers ? `<div class=\"a\">答え: ${escapeHtml(q.a)}</div>` : '<div class=\"answer-space\" aria-label=\"解答欄\"></div>'}</div>`;\n  const pages = [];\n  for (let startIndex = 0; startIndex < currentQuestions.length; startIndex += 10) {\n    const pageQuestions = currentQuestions.slice(startIndex, startIndex + 10);\n    pages.push({\n      html: pageQuestions.map((q, offset) => renderQuestion(q, startIndex + offset)).join(''),\n      rows: Math.ceil(pageQuestions.length / 2)\n    });\n  }\n  const printHtml = `<!doctype html><html lang=\"ja\"><head><meta charset=\"utf-8\"><title>問題プリント</title><style>\n  @page{size:A4 portrait;margin:8mm}\n  *{box-sizing:border-box}\n  body{font-family:-apple-system,BlinkMacSystemFont,\"Segoe UI\",\"Yu Gothic\",sans-serif;margin:0;color:#111;font-size:9pt;line-height:1.3}\n  .page{height:281mm;display:flex;flex-direction:column;overflow:hidden;page-break-after:always;break-after:page}\n  .page:last-child{page-break-after:auto;break-after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventList"
    },
    {
      "needle": "baseFiltered(",
      "index": 2579566,
      "context": "ct')?.value || '英語',\n    grade: el('gradeSelect')?.value || '中1',\n    textbook: el('textbookSelect')?.value || '',\n    section: el('sectionSelect')?.value || '',\n    questionCount: el('questionCount')?.value || '10',\n    vocabMode: el('vocabMode')?.value || 'on',\n    pickMode: el('pickMode')?.value || 'random',\n    stages: selectedValues('input[data-stage]'),\n    types: selectedValues('input[data-type]'),\n    listSubject: el('listSubject')?.value || '英語',\n    listGrade: el('listGrade')?.value || 'all',\n    listLimit: el('listLimit')?.value || '50',\n    listKeyword: el('listKeyword')?.value || '',\n    listVocabMode: el('listVocabMode')?.value || 'same',\n    currentQuestionIds: currentQuestions.map(q => q.id),\n    showAnswers\n  };\n}\n\nfunction saveState(silent = true) {\n  try {\n    localStorage.setItem(STORAGE_KEY, JSON.stringify(captureState()));\n    if (!silent && el('globalSaveStatus')) {\n      el('globalSaveStatus').textContent = 'この端末に保存しました。';\n    }\n  } catch (e) {\n    if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存に失敗しました。';\n  }\n}\n\nfunction restoreQuestionSetByIds(ids) {\n  const map = new Map(qb.map(q => [q.id, q]));\n  const restored = (ids || []).map(id => map.get(id)).filter(Boolean);\n  if (restored.length) {\n    currentQuestions = restored;\n    el('questionPreview').classList.toggle('showAnswers', showAnswers);\n    el('questionPreview').innerHTML = restored.map((q, i) => `\n      <div class=\"q\">\n        <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n        <div>${q.q}</div>\n        <div class=\"ans\">答え: ${q.a}</div>\n      </div>\n    `).join('');\n    el('builderStatus').textContent = '保存されていた問題セットを復元しました。';\n    el('builderStatus').classList.add('good');\n  }\n}\n\nfunction safeSetValue(id, value) {\n  const node = el(id);\n  if (!node || value == null) return;\n  const has = Array.from(node.options || []).some(op => op.value === value || op.textContent === value);\n  if (has) node.value = value;\n}\n\nfunction setCheckedValues(selector, values) {\n  const set = new Set(values || []);\n  document.querySelectorAll(selector).forEach(ch => {\n    ch.checked = set.size ? set.has(ch.value) : true;\n  });\n}\n\nfunction applyState(state) {\n  if (!state) return;\n  safeSetValue('subjectSelect', state.subject);\n  safeSetValue('gradeSelect', state.grade);\n  renderSectionOptions();\n  renderStageOptions();\n  renderTypeOptions();\n  safeSetValue('textbookSelect', state.textbook);\n  renderSectionOptions();\n  safeSetValue('sectionSelect', state.section);\n  safeSetValue('questionCount', state.questionCount);\n  safeSetValue('vocabMode', state.vocabMode);\n  safeSetValue('pickMode', state.pickMode);\n  setCheckedValues('input[data-stage]', state.stages);\n  setCheckedValues('input[data-type]', state.types);\n  syncListSelectors();\n  safeSetValue('listSubject', state.listSubject);\n  safeSetValue('listGrade', state.listGrade);\n  safeSetValue('listLimit', state.listLimit);\n  safeSetValue('listVocabMode', state.listVocabMode);\n  if (el('listKeyword')) el('listKeyword').value = state.listKeyword || '';\n  showAnswers = !!state.showAnswers;\n  restoreQuestionSetByIds(state.currentQuestionIds || []);\n  updateBuilderStatus(baseFiltered());\n}\n\nfunction exportStateFile() {\n  const blob = new Blob([JSON.stringify(captureState(), null, 2)], {type:'application/json'});\n  const url = URL.createObjectURL(blob);\n  const a = document.createElement('a');\n  a.href = url;\n  a.download = 'mikamijuku_app_state_latest.json';\n  document.body.appendChild(a);\n  a.click();\n  a.remove();\n  setTimeout(() => URL.revokeObjectURL(url), 3000);\n  if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを書き出しました。';\n}\n\nfunction importStateFile(file) {\n  const fr = new FileReader();\n  fr.onload = () => {\n    try {\n      const state = JSON.parse(fr.result);\n      applyState(state);\n      saveState(true);\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを読み込みました。';\n    } catch (e) {\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データの読み込みに失敗しました。';\n    }\n  };\n  fr.readAsText(file, 'utf-8');\n}\n\nconst currentSubject = () => el('subjectSelect').value;\nconst currentGrade = () => el('gradeSelect').value;\nconst currentTextbook = () => el('textbookSelect').value;\nconst currentSectionIndex = () => Math.max(0, el('sectionSelect').selectedIndex);\nconst useVocabGate = () => el('vocabMode').value === 'on';\n\nfunction renderTabs() {\n  document.querySelectorAll('.tab').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.tab').forEach(x => x.classList.remove('active'));\n      document.querySelectorAll('.screen').forEach(x => x.classList.remove('active'));\n      btn.classList.add('active');\n      const target = document.getElementById('screen-' + btn.dataset.screen);\n      if (target) target.classList.add('active');\n    });\n  });\n}\n\nfunction renderTextbookOptions() {\n  el('textbookSelect').innerHTML = meta.textbooks.map(tb => `<option>${tb}</option>`).join('');\n}\n\nfunction renderSectionOptions() {\n  const subject = currentSubject();\n  const grade = currentGrade().replace('中','');\n  if (subject !== '英語') {\n    el('textbookWrap').classList.add('hidden');\n    el('sectionWrap').classList.add('hidden');\n    return;\n  }\n  el('textbookWrap').classList.remove('hidden');\n  el('sectionWrap').classList.remove('hidden');\n  const tb = currentTextbook() || meta.textbooks[0];\n  const sections = ((meta.sections[tb] || {})[grade] || []);\n  el('sectionSelect').innerHTML = sections.map(sec => `<option>${sec}</option>`).join('');\n}\n\nfunction renderStageOptions() {\n  const stages = subjectConfig[currentSubject()].stagesByGrade[currentGrade()] || [];\n  el('stageArea').innerHTML = stages.map(s => `<label class=\"ck\"><input type=\"checkbox\" data-stage value=\"${s}\" checked> <span>${s}</span></label>`).join('');\n}\n\nfunction renderTypeOptions() {\n  const types = subjectConfig[currentSubject()].types;\n  el('typeArea').innerHTML = types.map(t => `<label class=\"ck\"><input type=\"checkbox\" data-type value=\"${t}\" checked> <span>${t}</span></label>`).join('');\n}\n\nfunction selectedValues(selector) {\n  return Array.from(document.querySelectorAll(selector + ':checked')).map(x => x.value);\n}\n\nfunction selectedCategories() {\n  const stages = selectedValues('input[data-stage]');\n  if (!stages.length) return [];\n  const map = subjectConfig[currentSubject()].stageMap[currentGrade()] || {};\n  const set = new Set();\n  stages.forEach(stage => (map[stage] || [stage]).forEach(c => set.add(c)));\n  return Array.from(set);\n}\n\nfunction passesVocab(item, overrideMode) {\n  if (item.subject !== '英語') return true;\n  const mode = overrideMode || (useVocabGate() ? 'on' : 'off');\n  if (mode === 'off') return true;\n  if (item.grade !== currentGrade()) return true;\n  const rec = meta.passMeta[item.id] || {};\n  const minIdx = rec[currentTextbook()];\n  if (meta.vocabCoordinateVersion !== 'v7-2026-08-18-1based') return false;\n  if (minIdx === -2) return true;\n  if (!Number.isInteger(minIdx) || minIdx <= 0) return false;\n  const selectedOrdinal = currentSectionIndex() + 1;\n  return minIdx <= selectedOrdinal;\n}\n\n// ---- Mikami prerequisite grammar + runtime quality gates ----\nfunction selectedStageCeiling() {\n  if (currentSubject() !== '英語') return Number.MAX_SAFE_INTEGER;\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  const selected = selectedValues('input[data-stage]');\n  if (!selected.length) return -1;\n  return selected.reduce((max, s) => Math.max(max, stages.indexOf(s)), -1);\n}\n\nfunction stageIndexByNameContains(...needles) {\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  for (let i = 0; i < stages.length; i++) {\n    const s = stages[i];\n    if (needles.some(n => s.includes(n))) return i;\n  }\n  return -1;\n}\n\nfunction targetStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const cfg = subjectConfig['英語'];\n  const stages = cfg.stagesByGrade[item.grade] || [];\n  const map = cfg.stageMap[item.grade] || {};\n  for (let i = 0; i < stages.length; i++) {\n    const cats = map[stages[i]] || [stages[i]];\n    if (cats.includes(item.category)) return i;\n  }\n  return -1;\n}\n\nfunction requiredGrammarStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const text = String(item.q || '') + ' ' + String(item.a || '');\n  let req = Math.max(0, targetStageIndex(item));\n  const bump = i => { if (i >= 0) req = Math.max(req, i); };\n\n  // Detect only structures that can make an otherwise earlier problem depend on a later grammar unit.\n  if (/\\b(will|won't)\\b|\\b(am|is|are) going to\\b/i.test(text)) bump(stageIndexByNameContains('未来'));\n  if (/\\b(was|were)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('過去進行'));\n  if (/\\b(am|is|are)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('進行形'));\n  if (/\\b(can|must|may|should)\\b/i.test(text)) bump(stageIndexByNameContains('助動詞','can'));\n  if (/\\bto\\s+(?:be|have|do|play|study|read|write|watch|visit|see|eat|drink|go|come|make|use|help|learn|practice|speak|listen|buy|call|join|take|work|live|want|need|try|start|finish)\\b/i.test(text)) bump(stageIndexByNameContains('不定詞①','不定詞②','不定詞'));\n  if (/\\b(?:enjoy|enjoys|enjoyed|finish|finishes|finished|stop|stops|stopped)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('動名詞','不定詞①，動名詞'));\n  if (/\\b(?:than|as\\s+\\w+\\s+as|more\\s+\\w+\\s+than|most\\s+\\w+)\\b/i.test(text)) bump(stageIndexByNameContains('比較'));\n  if (/\\b(am|is|are|was|were)\\s+(?:made|used|written|spoken|known|called|played|studied|visited|built|seen|loved)\\b/i.test(text)) bump(stageIndexByNameContains('受動態'));\n  if (/\\b(?:have|has)\\s+(?:ever\\s+|never\\s+|already\\s+|just\\s+)?(?:been|visited|finished|lost|lived|studied|seen|done|written|read|known|used|played|gone|made|eaten|had)\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形'));\n  if (/\\b(?:have|has) been [A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形（継続）'));\n  if (/\\b(?:who|which|that)\\s+(?:is|are|was|were|has|have|can|will|[A-Za-z]+s?)(?:\\s|\\?)/i.test(text) && /\\b(?:boy|girl|man|woman|teacher|student|person|book|bag|picture|pen|flower|thing)\\b/i.test(text)) bump(stageIndexByNameContains('関係代名詞'));\n  if (/\\bif\\b[\\s\\S]*\\bwould\\b|\\bI wish\\b/i.test(text)) bump(stageIndexByNameContains('仮定法'));\n  return req;\n}\n\nfunction passesPrereqGrammar(item) {\n  if (item.subject !== '英語') return true;\n  const ceiling "
    },
    {
      "needle": "baseFiltered(",
      "index": 2589143,
      "context": ")\\b/i.test(text)) bump(stageIndexByNameContains('受動態'));\n  if (/\\b(?:have|has)\\s+(?:ever\\s+|never\\s+|already\\s+|just\\s+)?(?:been|visited|finished|lost|lived|studied|seen|done|written|read|known|used|played|gone|made|eaten|had)\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形'));\n  if (/\\b(?:have|has) been [A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形（継続）'));\n  if (/\\b(?:who|which|that)\\s+(?:is|are|was|were|has|have|can|will|[A-Za-z]+s?)(?:\\s|\\?)/i.test(text) && /\\b(?:boy|girl|man|woman|teacher|student|person|book|bag|picture|pen|flower|thing)\\b/i.test(text)) bump(stageIndexByNameContains('関係代名詞'));\n  if (/\\bif\\b[\\s\\S]*\\bwould\\b|\\bI wish\\b/i.test(text)) bump(stageIndexByNameContains('仮定法'));\n  return req;\n}\n\nfunction passesPrereqGrammar(item) {\n  if (item.subject !== '英語') return true;\n  const ceiling = selectedStageCeiling();\n  if (ceiling < 0) return false;\n  const required = requiredGrammarStageIndex(item);\n  return required < 0 || required <= ceiling;\n}\n\nfunction passesQualityGate(item) {\n  if (!item || !String(item.q || '').trim() || !String(item.a || '').trim()) return false;\n  if (item.subject !== '英語') return true;\n  const q = String(item.q || '');\n  const a = String(item.a || '');\n  const both = q + ' ' + a;\n\n  // Known invalid / non-unique generation families.\n  if (/This is (?:he|she|we)\\b/i.test(both)) return false;\n  if (/\\bthan He\\b/.test(both)) return false;\n  if (/\\b(?:Mika|She) (?:love|begin|stop)\\b/.test(both)) return false;\n  if (/Do you have (?:visited|been|finished|lost|lived|studied|seen|done)/i.test(both)) return false;\n  if (/Did You ne\\b|Do You\\b|Do They\\b|Does She\\b|Does He\\b/.test(both)) return false;\n  if (/否定文または疑問文/.test(q)) return false;\n  if (/日本語で説明しなさい/.test(q) && /^[A-Za-z]/.test(a)) return false;\n  if (/\\(\\s*she\\s*\\/\\s*her\\s*\\/\\s*her\\s*\\)/i.test(q)) return false;\n  if (/\\(\\s*he\\s*\\/\\s*him\\s*\\/\\s*him\\s*\\)/i.test(q)) return false;\n  if (String(item.id || '').startsWith('M2-INF2-')) {\n    if (item.type === '空所補充' && /^to$/i.test(a) && /\\bto\\s*\\(\\s*\\)\\s*(?=[A-Za-z])/i.test(q)) return false;\n    if (/^(?:He|She|Yuki|Mika|Takumi|Ken|Emi|Tom|The student|The teacher|This boy|This girl|My mother|My father|My brother|My sister|My friend) (?:want|need|like) to\\b/i.test(a)) return false;\n  }\n\n  // Target-grammar evidence: do not let a mislabeled vocabulary-only item through.\n  if (item.category === '動名詞' && !/[A-Za-z]+ing\\b/.test(both)) return false;\n  if (item.category === '比較' && !/\\bthan\\b|\\bas\\s+\\w+\\s+as\\b|\\bmost\\b|\\w+est\\b/i.test(both)) return false;\n  const infinitiveBlank = item.type === '空所補充' && /^to$/i.test(a) && /\\(\\s*\\)\\s*[A-Za-z]+\\b/.test(q);\n  if ((item.category === '不定詞' || item.category === '不定詞①' || item.category === '不定詞②') && !/\\bto\\s+[A-Za-z]+\\b/i.test(both) && !infinitiveBlank) return false;\n  if (String(item.category).startsWith('現在完了形') && !/\\b(?:have|has)\\b/i.test(both)) return false;\n  if (item.category === '関係代名詞') {\n    if (!/\\b(?:who|which|that)\\b/i.test(both)) return false;\n    if (item.type === '空所補充' && /\\(\\s*\\)/.test(q)) {\n      if (!/who\\s*\\/\\s*which/i.test(q)) return false;\n      if (!/^(?:who|which)$/i.test(a)) return false;\n    }\n  }\n  return true;\n}\n\nfunction baseFiltered(overrideMode) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const categories = selectedCategories();\n  const types = selectedValues('input[data-type]');\n  return qb.filter(item => {\n    if (item.subject !== subj) return false;\n    if (item.grade !== grade) return false;\n    if (categories.length && !categories.includes(item.category)) return false;\n    if (types.length && !types.includes(item.type)) return false;\n    if (!passesVocab(item, overrideMode)) return false;\n    if (!passesPrereqGrammar(item)) return false;\n    if (!passesQualityGate(item)) return false;\n    return true;\n  });\n}\n\nfunction shuffle(arr) {\n  const a = arr.slice();\n  for (let i=a.length-1;i>0;i--) {\n    const j = Math.floor(Math.random() * (i+1));\n    const tmp = a[i]; a[i] = a[j]; a[j] = tmp;\n  }\n  return a;\n}\n\nfunction updateBuilderStatus(candidates) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const sameGradeCount = qb.filter(x => x.subject === subj && x.grade === grade).length;\n  let msg = '全問題 ' + meta.counts.all + '件 / ' + grade + 'の' + subj + '元データ ' + sameGradeCount + '件 / この条件で使える候補 ' + candidates.length + '件';\n  if (subj === '英語') {\n    msg += ' / 教科書: ' + currentTextbook() + ' / 単語はここまで: ' + (el('sectionSelect').value || '未選択');\n  }\n  el('builderStatus').textContent = msg;\n  el('builderStatus').classList.toggle('good', candidates.length > 0);\n}\n\nfunction renderQuestions() {\n  const count = parseInt(el('questionCount').value, 10);\n  const pickMode = el('pickMode').value;\n  const candidates = baseFiltered();\n  updateBuilderStatus(candidates);\n  if (!candidates.length) {\n    currentQuestions = [];\n    el('questionPreview').innerHTML = 'この条件では問題が見つかりませんでした。単語制限を外すか、範囲や問題タイプを広げてください。';\n    return;\n  }\n  const picked = (pickMode === 'random' ? shuffle(candidates) : candidates).slice(0, count);\n  currentQuestions = picked;\n  el('questionPreview').classList.toggle('showAnswers', showAnswers);\n  el('questionPreview').innerHTML = picked.map((q, i) => `\n    <div class=\"q\">\n      <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n      <div>${q.q}</div>\n      <div class=\"ans\">答え: ${q.a}</div>\n    </div>\n  `).join('');\n}\n\nfunction printQuestions() {\n  if (!currentQuestions.length) return;\n  const escapeHtml = value => String(value ?? '').replace(/[&<>\"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[char]));\n  const renderQuestion = (q, index) => `<div class=\"q\"><div class=\"n\">Q${index + 1}　${escapeHtml(q.id)}</div><div class=\"body\">${escapeHtml(q.q)}</div>${showAnswers ? `<div class=\"a\">答え: ${escapeHtml(q.a)}</div>` : '<div class=\"answer-space\" aria-label=\"解答欄\"></div>'}</div>`;\n  const pages = [];\n  for (let startIndex = 0; startIndex < currentQuestions.length; startIndex += 10) {\n    const pageQuestions = currentQuestions.slice(startIndex, startIndex + 10);\n    pages.push({\n      html: pageQuestions.map((q, offset) => renderQuestion(q, startIndex + offset)).join(''),\n      rows: Math.ceil(pageQuestions.length / 2)\n    });\n  }\n  const printHtml = `<!doctype html><html lang=\"ja\"><head><meta charset=\"utf-8\"><title>問題プリント</title><style>\n  @page{size:A4 portrait;margin:8mm}\n  *{box-sizing:border-box}\n  body{font-family:-apple-system,BlinkMacSystemFont,\"Segoe UI\",\"Yu Gothic\",sans-serif;margin:0;color:#111;font-size:9pt;line-height:1.3}\n  .page{height:281mm;display:flex;flex-direction:column;overflow:hidden;page-break-after:always;break-after:page}\n  .page:last-child{page-break-after:auto;break-after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListe"
    },
    {
      "needle": "baseFiltered(",
      "index": 2590702,
      "context": "one)/i.test(both)) return false;\n  if (/Did You ne\\b|Do You\\b|Do They\\b|Does She\\b|Does He\\b/.test(both)) return false;\n  if (/否定文または疑問文/.test(q)) return false;\n  if (/日本語で説明しなさい/.test(q) && /^[A-Za-z]/.test(a)) return false;\n  if (/\\(\\s*she\\s*\\/\\s*her\\s*\\/\\s*her\\s*\\)/i.test(q)) return false;\n  if (/\\(\\s*he\\s*\\/\\s*him\\s*\\/\\s*him\\s*\\)/i.test(q)) return false;\n  if (String(item.id || '').startsWith('M2-INF2-')) {\n    if (item.type === '空所補充' && /^to$/i.test(a) && /\\bto\\s*\\(\\s*\\)\\s*(?=[A-Za-z])/i.test(q)) return false;\n    if (/^(?:He|She|Yuki|Mika|Takumi|Ken|Emi|Tom|The student|The teacher|This boy|This girl|My mother|My father|My brother|My sister|My friend) (?:want|need|like) to\\b/i.test(a)) return false;\n  }\n\n  // Target-grammar evidence: do not let a mislabeled vocabulary-only item through.\n  if (item.category === '動名詞' && !/[A-Za-z]+ing\\b/.test(both)) return false;\n  if (item.category === '比較' && !/\\bthan\\b|\\bas\\s+\\w+\\s+as\\b|\\bmost\\b|\\w+est\\b/i.test(both)) return false;\n  const infinitiveBlank = item.type === '空所補充' && /^to$/i.test(a) && /\\(\\s*\\)\\s*[A-Za-z]+\\b/.test(q);\n  if ((item.category === '不定詞' || item.category === '不定詞①' || item.category === '不定詞②') && !/\\bto\\s+[A-Za-z]+\\b/i.test(both) && !infinitiveBlank) return false;\n  if (String(item.category).startsWith('現在完了形') && !/\\b(?:have|has)\\b/i.test(both)) return false;\n  if (item.category === '関係代名詞') {\n    if (!/\\b(?:who|which|that)\\b/i.test(both)) return false;\n    if (item.type === '空所補充' && /\\(\\s*\\)/.test(q)) {\n      if (!/who\\s*\\/\\s*which/i.test(q)) return false;\n      if (!/^(?:who|which)$/i.test(a)) return false;\n    }\n  }\n  return true;\n}\n\nfunction baseFiltered(overrideMode) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const categories = selectedCategories();\n  const types = selectedValues('input[data-type]');\n  return qb.filter(item => {\n    if (item.subject !== subj) return false;\n    if (item.grade !== grade) return false;\n    if (categories.length && !categories.includes(item.category)) return false;\n    if (types.length && !types.includes(item.type)) return false;\n    if (!passesVocab(item, overrideMode)) return false;\n    if (!passesPrereqGrammar(item)) return false;\n    if (!passesQualityGate(item)) return false;\n    return true;\n  });\n}\n\nfunction shuffle(arr) {\n  const a = arr.slice();\n  for (let i=a.length-1;i>0;i--) {\n    const j = Math.floor(Math.random() * (i+1));\n    const tmp = a[i]; a[i] = a[j]; a[j] = tmp;\n  }\n  return a;\n}\n\nfunction updateBuilderStatus(candidates) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const sameGradeCount = qb.filter(x => x.subject === subj && x.grade === grade).length;\n  let msg = '全問題 ' + meta.counts.all + '件 / ' + grade + 'の' + subj + '元データ ' + sameGradeCount + '件 / この条件で使える候補 ' + candidates.length + '件';\n  if (subj === '英語') {\n    msg += ' / 教科書: ' + currentTextbook() + ' / 単語はここまで: ' + (el('sectionSelect').value || '未選択');\n  }\n  el('builderStatus').textContent = msg;\n  el('builderStatus').classList.toggle('good', candidates.length > 0);\n}\n\nfunction renderQuestions() {\n  const count = parseInt(el('questionCount').value, 10);\n  const pickMode = el('pickMode').value;\n  const candidates = baseFiltered();\n  updateBuilderStatus(candidates);\n  if (!candidates.length) {\n    currentQuestions = [];\n    el('questionPreview').innerHTML = 'この条件では問題が見つかりませんでした。単語制限を外すか、範囲や問題タイプを広げてください。';\n    return;\n  }\n  const picked = (pickMode === 'random' ? shuffle(candidates) : candidates).slice(0, count);\n  currentQuestions = picked;\n  el('questionPreview').classList.toggle('showAnswers', showAnswers);\n  el('questionPreview').innerHTML = picked.map((q, i) => `\n    <div class=\"q\">\n      <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n      <div>${q.q}</div>\n      <div class=\"ans\">答え: ${q.a}</div>\n    </div>\n  `).join('');\n}\n\nfunction printQuestions() {\n  if (!currentQuestions.length) return;\n  const escapeHtml = value => String(value ?? '').replace(/[&<>\"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[char]));\n  const renderQuestion = (q, index) => `<div class=\"q\"><div class=\"n\">Q${index + 1}　${escapeHtml(q.id)}</div><div class=\"body\">${escapeHtml(q.q)}</div>${showAnswers ? `<div class=\"a\">答え: ${escapeHtml(q.a)}</div>` : '<div class=\"answer-space\" aria-label=\"解答欄\"></div>'}</div>`;\n  const pages = [];\n  for (let startIndex = 0; startIndex < currentQuestions.length; startIndex += 10) {\n    const pageQuestions = currentQuestions.slice(startIndex, startIndex + 10);\n    pages.push({\n      html: pageQuestions.map((q, offset) => renderQuestion(q, startIndex + offset)).join(''),\n      rows: Math.ceil(pageQuestions.length / 2)\n    });\n  }\n  const printHtml = `<!doctype html><html lang=\"ja\"><head><meta charset=\"utf-8\"><title>問題プリント</title><style>\n  @page{size:A4 portrait;margin:8mm}\n  *{box-sizing:border-box}\n  body{font-family:-apple-system,BlinkMacSystemFont,\"Segoe UI\",\"Yu Gothic\",sans-serif;margin:0;color:#111;font-size:9pt;line-height:1.3}\n  .page{height:281mm;display:flex;flex-direction:column;overflow:hidden;page-break-after:always;break-after:page}\n  .page:last-child{page-break-after:auto;break-after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(saved));\n} catch (e) {}\nupdateBuilderStatus(baseFiltered());\n})();\n</script>\n\n</body>\n</html>"
    },
    {
      "needle": "baseFiltered(",
      "index": 2595587,
      "context": "der-box}\n  body{font-family:-apple-system,BlinkMacSystemFont,\"Segoe UI\",\"Yu Gothic\",sans-serif;margin:0;color:#111;font-size:9pt;line-height:1.3}\n  .page{height:281mm;display:flex;flex-direction:column;overflow:hidden;page-break-after:always;break-after:page}\n  .page:last-child{page-break-after:auto;break-after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(saved));\n} catch (e) {}\nupdateBuilderStatus(baseFiltered());\n})();\n</script>\n\n</body>\n</html>"
    },
    {
      "needle": "baseFiltered(",
      "index": 2595771,
      "context": "-direction:column;overflow:hidden;page-break-after:always;break-after:page}\n  .page:last-child{page-break-after:auto;break-after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(saved));\n} catch (e) {}\nupdateBuilderStatus(baseFiltered());\n})();\n</script>\n\n</body>\n</html>"
    },
    {
      "needle": "baseFiltered(",
      "index": 2595894,
      "context": "after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(saved));\n} catch (e) {}\nupdateBuilderStatus(baseFiltered());\n})();\n</script>\n\n</body>\n</html>"
    },
    {
      "needle": "baseFiltered(",
      "index": 2595990,
      "context": "argin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(saved));\n} catch (e) {}\nupdateBuilderStatus(baseFiltered());\n})();\n</script>\n\n</body>\n</html>"
    },
    {
      "needle": "baseFiltered(",
      "index": 2596079,
      "context": "to;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(saved));\n} catch (e) {}\nupdateBuilderStatus(baseFiltered());\n})();\n</script>\n\n</body>\n</html>"
    },
    {
      "needle": "baseFiltered(",
      "index": 2596231,
      "context": "eak-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(saved));\n} catch (e) {}\nupdateBuilderStatus(baseFiltered());\n})();\n</script>\n\n</body>\n</html>"
    },
    {
      "needle": "baseFiltered(",
      "index": 2597818,
      "context": "currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(saved));\n} catch (e) {}\nupdateBuilderStatus(baseFiltered());\n})();\n</script>\n\n</body>\n</html>"
    },
    {
      "needle": "function renderQuestions",
      "index": 2590554,
      "context": " false;\n  if (/\\b(?:Mika|She) (?:love|begin|stop)\\b/.test(both)) return false;\n  if (/Do you have (?:visited|been|finished|lost|lived|studied|seen|done)/i.test(both)) return false;\n  if (/Did You ne\\b|Do You\\b|Do They\\b|Does She\\b|Does He\\b/.test(both)) return false;\n  if (/否定文または疑問文/.test(q)) return false;\n  if (/日本語で説明しなさい/.test(q) && /^[A-Za-z]/.test(a)) return false;\n  if (/\\(\\s*she\\s*\\/\\s*her\\s*\\/\\s*her\\s*\\)/i.test(q)) return false;\n  if (/\\(\\s*he\\s*\\/\\s*him\\s*\\/\\s*him\\s*\\)/i.test(q)) return false;\n  if (String(item.id || '').startsWith('M2-INF2-')) {\n    if (item.type === '空所補充' && /^to$/i.test(a) && /\\bto\\s*\\(\\s*\\)\\s*(?=[A-Za-z])/i.test(q)) return false;\n    if (/^(?:He|She|Yuki|Mika|Takumi|Ken|Emi|Tom|The student|The teacher|This boy|This girl|My mother|My father|My brother|My sister|My friend) (?:want|need|like) to\\b/i.test(a)) return false;\n  }\n\n  // Target-grammar evidence: do not let a mislabeled vocabulary-only item through.\n  if (item.category === '動名詞' && !/[A-Za-z]+ing\\b/.test(both)) return false;\n  if (item.category === '比較' && !/\\bthan\\b|\\bas\\s+\\w+\\s+as\\b|\\bmost\\b|\\w+est\\b/i.test(both)) return false;\n  const infinitiveBlank = item.type === '空所補充' && /^to$/i.test(a) && /\\(\\s*\\)\\s*[A-Za-z]+\\b/.test(q);\n  if ((item.category === '不定詞' || item.category === '不定詞①' || item.category === '不定詞②') && !/\\bto\\s+[A-Za-z]+\\b/i.test(both) && !infinitiveBlank) return false;\n  if (String(item.category).startsWith('現在完了形') && !/\\b(?:have|has)\\b/i.test(both)) return false;\n  if (item.category === '関係代名詞') {\n    if (!/\\b(?:who|which|that)\\b/i.test(both)) return false;\n    if (item.type === '空所補充' && /\\(\\s*\\)/.test(q)) {\n      if (!/who\\s*\\/\\s*which/i.test(q)) return false;\n      if (!/^(?:who|which)$/i.test(a)) return false;\n    }\n  }\n  return true;\n}\n\nfunction baseFiltered(overrideMode) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const categories = selectedCategories();\n  const types = selectedValues('input[data-type]');\n  return qb.filter(item => {\n    if (item.subject !== subj) return false;\n    if (item.grade !== grade) return false;\n    if (categories.length && !categories.includes(item.category)) return false;\n    if (types.length && !types.includes(item.type)) return false;\n    if (!passesVocab(item, overrideMode)) return false;\n    if (!passesPrereqGrammar(item)) return false;\n    if (!passesQualityGate(item)) return false;\n    return true;\n  });\n}\n\nfunction shuffle(arr) {\n  const a = arr.slice();\n  for (let i=a.length-1;i>0;i--) {\n    const j = Math.floor(Math.random() * (i+1));\n    const tmp = a[i]; a[i] = a[j]; a[j] = tmp;\n  }\n  return a;\n}\n\nfunction updateBuilderStatus(candidates) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const sameGradeCount = qb.filter(x => x.subject === subj && x.grade === grade).length;\n  let msg = '全問題 ' + meta.counts.all + '件 / ' + grade + 'の' + subj + '元データ ' + sameGradeCount + '件 / この条件で使える候補 ' + candidates.length + '件';\n  if (subj === '英語') {\n    msg += ' / 教科書: ' + currentTextbook() + ' / 単語はここまで: ' + (el('sectionSelect').value || '未選択');\n  }\n  el('builderStatus').textContent = msg;\n  el('builderStatus').classList.toggle('good', candidates.length > 0);\n}\n\nfunction renderQuestions() {\n  const count = parseInt(el('questionCount').value, 10);\n  const pickMode = el('pickMode').value;\n  const candidates = baseFiltered();\n  updateBuilderStatus(candidates);\n  if (!candidates.length) {\n    currentQuestions = [];\n    el('questionPreview').innerHTML = 'この条件では問題が見つかりませんでした。単語制限を外すか、範囲や問題タイプを広げてください。';\n    return;\n  }\n  const picked = (pickMode === 'random' ? shuffle(candidates) : candidates).slice(0, count);\n  currentQuestions = picked;\n  el('questionPreview').classList.toggle('showAnswers', showAnswers);\n  el('questionPreview').innerHTML = picked.map((q, i) => `\n    <div class=\"q\">\n      <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n      <div>${q.q}</div>\n      <div class=\"ans\">答え: ${q.a}</div>\n    </div>\n  `).join('');\n}\n\nfunction printQuestions() {\n  if (!currentQuestions.length) return;\n  const escapeHtml = value => String(value ?? '').replace(/[&<>\"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[char]));\n  const renderQuestion = (q, index) => `<div class=\"q\"><div class=\"n\">Q${index + 1}　${escapeHtml(q.id)}</div><div class=\"body\">${escapeHtml(q.q)}</div>${showAnswers ? `<div class=\"a\">答え: ${escapeHtml(q.a)}</div>` : '<div class=\"answer-space\" aria-label=\"解答欄\"></div>'}</div>`;\n  const pages = [];\n  for (let startIndex = 0; startIndex < currentQuestions.length; startIndex += 10) {\n    const pageQuestions = currentQuestions.slice(startIndex, startIndex + 10);\n    pages.push({\n      html: pageQuestions.map((q, offset) => renderQuestion(q, startIndex + offset)).join(''),\n      rows: Math.ceil(pageQuestions.length / 2)\n    });\n  }\n  const printHtml = `<!doctype html><html lang=\"ja\"><head><meta charset=\"utf-8\"><title>問題プリント</title><style>\n  @page{size:A4 portrait;margin:8mm}\n  *{box-sizing:border-box}\n  body{font-family:-apple-system,BlinkMacSystemFont,\"Segoe UI\",\"Yu Gothic\",sans-serif;margin:0;color:#111;font-size:9pt;line-height:1.3}\n  .page{height:281mm;display:flex;flex-direction:column;overflow:hidden;page-break-after:always;break-after:page}\n  .page:last-child{page-break-after:auto;break-after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(save"
    },
    {
      "needle": "function updateBuilderStatus",
      "index": 2589975,
      "context": "ing = selectedStageCeiling();\n  if (ceiling < 0) return false;\n  const required = requiredGrammarStageIndex(item);\n  return required < 0 || required <= ceiling;\n}\n\nfunction passesQualityGate(item) {\n  if (!item || !String(item.q || '').trim() || !String(item.a || '').trim()) return false;\n  if (item.subject !== '英語') return true;\n  const q = String(item.q || '');\n  const a = String(item.a || '');\n  const both = q + ' ' + a;\n\n  // Known invalid / non-unique generation families.\n  if (/This is (?:he|she|we)\\b/i.test(both)) return false;\n  if (/\\bthan He\\b/.test(both)) return false;\n  if (/\\b(?:Mika|She) (?:love|begin|stop)\\b/.test(both)) return false;\n  if (/Do you have (?:visited|been|finished|lost|lived|studied|seen|done)/i.test(both)) return false;\n  if (/Did You ne\\b|Do You\\b|Do They\\b|Does She\\b|Does He\\b/.test(both)) return false;\n  if (/否定文または疑問文/.test(q)) return false;\n  if (/日本語で説明しなさい/.test(q) && /^[A-Za-z]/.test(a)) return false;\n  if (/\\(\\s*she\\s*\\/\\s*her\\s*\\/\\s*her\\s*\\)/i.test(q)) return false;\n  if (/\\(\\s*he\\s*\\/\\s*him\\s*\\/\\s*him\\s*\\)/i.test(q)) return false;\n  if (String(item.id || '').startsWith('M2-INF2-')) {\n    if (item.type === '空所補充' && /^to$/i.test(a) && /\\bto\\s*\\(\\s*\\)\\s*(?=[A-Za-z])/i.test(q)) return false;\n    if (/^(?:He|She|Yuki|Mika|Takumi|Ken|Emi|Tom|The student|The teacher|This boy|This girl|My mother|My father|My brother|My sister|My friend) (?:want|need|like) to\\b/i.test(a)) return false;\n  }\n\n  // Target-grammar evidence: do not let a mislabeled vocabulary-only item through.\n  if (item.category === '動名詞' && !/[A-Za-z]+ing\\b/.test(both)) return false;\n  if (item.category === '比較' && !/\\bthan\\b|\\bas\\s+\\w+\\s+as\\b|\\bmost\\b|\\w+est\\b/i.test(both)) return false;\n  const infinitiveBlank = item.type === '空所補充' && /^to$/i.test(a) && /\\(\\s*\\)\\s*[A-Za-z]+\\b/.test(q);\n  if ((item.category === '不定詞' || item.category === '不定詞①' || item.category === '不定詞②') && !/\\bto\\s+[A-Za-z]+\\b/i.test(both) && !infinitiveBlank) return false;\n  if (String(item.category).startsWith('現在完了形') && !/\\b(?:have|has)\\b/i.test(both)) return false;\n  if (item.category === '関係代名詞') {\n    if (!/\\b(?:who|which|that)\\b/i.test(both)) return false;\n    if (item.type === '空所補充' && /\\(\\s*\\)/.test(q)) {\n      if (!/who\\s*\\/\\s*which/i.test(q)) return false;\n      if (!/^(?:who|which)$/i.test(a)) return false;\n    }\n  }\n  return true;\n}\n\nfunction baseFiltered(overrideMode) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const categories = selectedCategories();\n  const types = selectedValues('input[data-type]');\n  return qb.filter(item => {\n    if (item.subject !== subj) return false;\n    if (item.grade !== grade) return false;\n    if (categories.length && !categories.includes(item.category)) return false;\n    if (types.length && !types.includes(item.type)) return false;\n    if (!passesVocab(item, overrideMode)) return false;\n    if (!passesPrereqGrammar(item)) return false;\n    if (!passesQualityGate(item)) return false;\n    return true;\n  });\n}\n\nfunction shuffle(arr) {\n  const a = arr.slice();\n  for (let i=a.length-1;i>0;i--) {\n    const j = Math.floor(Math.random() * (i+1));\n    const tmp = a[i]; a[i] = a[j]; a[j] = tmp;\n  }\n  return a;\n}\n\nfunction updateBuilderStatus(candidates) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const sameGradeCount = qb.filter(x => x.subject === subj && x.grade === grade).length;\n  let msg = '全問題 ' + meta.counts.all + '件 / ' + grade + 'の' + subj + '元データ ' + sameGradeCount + '件 / この条件で使える候補 ' + candidates.length + '件';\n  if (subj === '英語') {\n    msg += ' / 教科書: ' + currentTextbook() + ' / 単語はここまで: ' + (el('sectionSelect').value || '未選択');\n  }\n  el('builderStatus').textContent = msg;\n  el('builderStatus').classList.toggle('good', candidates.length > 0);\n}\n\nfunction renderQuestions() {\n  const count = parseInt(el('questionCount').value, 10);\n  const pickMode = el('pickMode').value;\n  const candidates = baseFiltered();\n  updateBuilderStatus(candidates);\n  if (!candidates.length) {\n    currentQuestions = [];\n    el('questionPreview').innerHTML = 'この条件では問題が見つかりませんでした。単語制限を外すか、範囲や問題タイプを広げてください。';\n    return;\n  }\n  const picked = (pickMode === 'random' ? shuffle(candidates) : candidates).slice(0, count);\n  currentQuestions = picked;\n  el('questionPreview').classList.toggle('showAnswers', showAnswers);\n  el('questionPreview').innerHTML = picked.map((q, i) => `\n    <div class=\"q\">\n      <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n      <div>${q.q}</div>\n      <div class=\"ans\">答え: ${q.a}</div>\n    </div>\n  `).join('');\n}\n\nfunction printQuestions() {\n  if (!currentQuestions.length) return;\n  const escapeHtml = value => String(value ?? '').replace(/[&<>\"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[char]));\n  const renderQuestion = (q, index) => `<div class=\"q\"><div class=\"n\">Q${index + 1}　${escapeHtml(q.id)}</div><div class=\"body\">${escapeHtml(q.q)}</div>${showAnswers ? `<div class=\"a\">答え: ${escapeHtml(q.a)}</div>` : '<div class=\"answer-space\" aria-label=\"解答欄\"></div>'}</div>`;\n  const pages = [];\n  for (let startIndex = 0; startIndex < currentQuestions.length; startIndex += 10) {\n    const pageQuestions = currentQuestions.slice(startIndex, startIndex + 10);\n    pages.push({\n      html: pageQuestions.map((q, offset) => renderQuestion(q, startIndex + offset)).join(''),\n      rows: Math.ceil(pageQuestions.length / 2)\n    });\n  }\n  const printHtml = `<!doctype html><html lang=\"ja\"><head><meta charset=\"utf-8\"><title>問題プリント</title><style>\n  @page{size:A4 portrait;margin:8mm}\n  *{box-sizing:border-box}\n  body{font-family:-apple-system,BlinkMacSystemFont,\"Segoe UI\",\"Yu Gothic\",sans-serif;margin:0;color:#111;font-size:9pt;line-height:1.3}\n  .page{height:281mm;display:flex;flex-direction:column;overflow:hidden;page-break-after:always;break-after:page}\n  .page:last-child{page-break-after:auto;break-after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput')."
    },
    {
      "needle": "function refreshList",
      "index": 2594096,
      "context": "    return;\n  }\n  const picked = (pickMode === 'random' ? shuffle(candidates) : candidates).slice(0, count);\n  currentQuestions = picked;\n  el('questionPreview').classList.toggle('showAnswers', showAnswers);\n  el('questionPreview').innerHTML = picked.map((q, i) => `\n    <div class=\"q\">\n      <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n      <div>${q.q}</div>\n      <div class=\"ans\">答え: ${q.a}</div>\n    </div>\n  `).join('');\n}\n\nfunction printQuestions() {\n  if (!currentQuestions.length) return;\n  const escapeHtml = value => String(value ?? '').replace(/[&<>\"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[char]));\n  const renderQuestion = (q, index) => `<div class=\"q\"><div class=\"n\">Q${index + 1}　${escapeHtml(q.id)}</div><div class=\"body\">${escapeHtml(q.q)}</div>${showAnswers ? `<div class=\"a\">答え: ${escapeHtml(q.a)}</div>` : '<div class=\"answer-space\" aria-label=\"解答欄\"></div>'}</div>`;\n  const pages = [];\n  for (let startIndex = 0; startIndex < currentQuestions.length; startIndex += 10) {\n    const pageQuestions = currentQuestions.slice(startIndex, startIndex + 10);\n    pages.push({\n      html: pageQuestions.map((q, offset) => renderQuestion(q, startIndex + offset)).join(''),\n      rows: Math.ceil(pageQuestions.length / 2)\n    });\n  }\n  const printHtml = `<!doctype html><html lang=\"ja\"><head><meta charset=\"utf-8\"><title>問題プリント</title><style>\n  @page{size:A4 portrait;margin:8mm}\n  *{box-sizing:border-box}\n  body{font-family:-apple-system,BlinkMacSystemFont,\"Segoe UI\",\"Yu Gothic\",sans-serif;margin:0;color:#111;font-size:9pt;line-height:1.3}\n  .page{height:281mm;display:flex;flex-direction:column;overflow:hidden;page-break-after:always;break-after:page}\n  .page:last-child{page-break-after:auto;break-after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(saved));\n} catch (e) {}\nupdateBuilderStatus(baseFiltered());\n})();\n</script>\n\n</body>\n</html>"
    },
    {
      "needle": "function printQuestions",
      "index": 2591374,
      "context": "t|need|like) to\\b/i.test(a)) return false;\n  }\n\n  // Target-grammar evidence: do not let a mislabeled vocabulary-only item through.\n  if (item.category === '動名詞' && !/[A-Za-z]+ing\\b/.test(both)) return false;\n  if (item.category === '比較' && !/\\bthan\\b|\\bas\\s+\\w+\\s+as\\b|\\bmost\\b|\\w+est\\b/i.test(both)) return false;\n  const infinitiveBlank = item.type === '空所補充' && /^to$/i.test(a) && /\\(\\s*\\)\\s*[A-Za-z]+\\b/.test(q);\n  if ((item.category === '不定詞' || item.category === '不定詞①' || item.category === '不定詞②') && !/\\bto\\s+[A-Za-z]+\\b/i.test(both) && !infinitiveBlank) return false;\n  if (String(item.category).startsWith('現在完了形') && !/\\b(?:have|has)\\b/i.test(both)) return false;\n  if (item.category === '関係代名詞') {\n    if (!/\\b(?:who|which|that)\\b/i.test(both)) return false;\n    if (item.type === '空所補充' && /\\(\\s*\\)/.test(q)) {\n      if (!/who\\s*\\/\\s*which/i.test(q)) return false;\n      if (!/^(?:who|which)$/i.test(a)) return false;\n    }\n  }\n  return true;\n}\n\nfunction baseFiltered(overrideMode) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const categories = selectedCategories();\n  const types = selectedValues('input[data-type]');\n  return qb.filter(item => {\n    if (item.subject !== subj) return false;\n    if (item.grade !== grade) return false;\n    if (categories.length && !categories.includes(item.category)) return false;\n    if (types.length && !types.includes(item.type)) return false;\n    if (!passesVocab(item, overrideMode)) return false;\n    if (!passesPrereqGrammar(item)) return false;\n    if (!passesQualityGate(item)) return false;\n    return true;\n  });\n}\n\nfunction shuffle(arr) {\n  const a = arr.slice();\n  for (let i=a.length-1;i>0;i--) {\n    const j = Math.floor(Math.random() * (i+1));\n    const tmp = a[i]; a[i] = a[j]; a[j] = tmp;\n  }\n  return a;\n}\n\nfunction updateBuilderStatus(candidates) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const sameGradeCount = qb.filter(x => x.subject === subj && x.grade === grade).length;\n  let msg = '全問題 ' + meta.counts.all + '件 / ' + grade + 'の' + subj + '元データ ' + sameGradeCount + '件 / この条件で使える候補 ' + candidates.length + '件';\n  if (subj === '英語') {\n    msg += ' / 教科書: ' + currentTextbook() + ' / 単語はここまで: ' + (el('sectionSelect').value || '未選択');\n  }\n  el('builderStatus').textContent = msg;\n  el('builderStatus').classList.toggle('good', candidates.length > 0);\n}\n\nfunction renderQuestions() {\n  const count = parseInt(el('questionCount').value, 10);\n  const pickMode = el('pickMode').value;\n  const candidates = baseFiltered();\n  updateBuilderStatus(candidates);\n  if (!candidates.length) {\n    currentQuestions = [];\n    el('questionPreview').innerHTML = 'この条件では問題が見つかりませんでした。単語制限を外すか、範囲や問題タイプを広げてください。';\n    return;\n  }\n  const picked = (pickMode === 'random' ? shuffle(candidates) : candidates).slice(0, count);\n  currentQuestions = picked;\n  el('questionPreview').classList.toggle('showAnswers', showAnswers);\n  el('questionPreview').innerHTML = picked.map((q, i) => `\n    <div class=\"q\">\n      <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n      <div>${q.q}</div>\n      <div class=\"ans\">答え: ${q.a}</div>\n    </div>\n  `).join('');\n}\n\nfunction printQuestions() {\n  if (!currentQuestions.length) return;\n  const escapeHtml = value => String(value ?? '').replace(/[&<>\"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[char]));\n  const renderQuestion = (q, index) => `<div class=\"q\"><div class=\"n\">Q${index + 1}　${escapeHtml(q.id)}</div><div class=\"body\">${escapeHtml(q.q)}</div>${showAnswers ? `<div class=\"a\">答え: ${escapeHtml(q.a)}</div>` : '<div class=\"answer-space\" aria-label=\"解答欄\"></div>'}</div>`;\n  const pages = [];\n  for (let startIndex = 0; startIndex < currentQuestions.length; startIndex += 10) {\n    const pageQuestions = currentQuestions.slice(startIndex, startIndex + 10);\n    pages.push({\n      html: pageQuestions.map((q, offset) => renderQuestion(q, startIndex + offset)).join(''),\n      rows: Math.ceil(pageQuestions.length / 2)\n    });\n  }\n  const printHtml = `<!doctype html><html lang=\"ja\"><head><meta charset=\"utf-8\"><title>問題プリント</title><style>\n  @page{size:A4 portrait;margin:8mm}\n  *{box-sizing:border-box}\n  body{font-family:-apple-system,BlinkMacSystemFont,\"Segoe UI\",\"Yu Gothic\",sans-serif;margin:0;color:#111;font-size:9pt;line-height:1.3}\n  .page{height:281mm;display:flex;flex-direction:column;overflow:hidden;page-break-after:always;break-after:page}\n  .page:last-child{page-break-after:auto;break-after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(saved));\n} catch (e) {}\nupdateBuilderStatus(baseFiltered());\n})();\n</script>\n\n</body>\n</html>"
    },
    {
      "needle": "currentQuestions",
      "index": 2576203,
      "context": "\"both\":1942,\"reconstructed_only\":634,\"old_only\":46,\"final_resolved\":2622,\"final_resolved_pct\":90.92},\"中2\":{\"total\":3707,\"reconstructed\":2837,\"reconstructed_pct\":76.53,\"trusted_old\":717,\"trusted_old_pct\":19.34,\"both\":619,\"reconstructed_only\":2218,\"old_only\":98,\"final_resolved\":2935,\"final_resolved_pct\":79.17},\"中3\":{\"total\":3920,\"reconstructed\":2851,\"reconstructed_pct\":72.73,\"trusted_old\":154,\"trusted_old_pct\":3.93,\"both\":126,\"reconstructed_only\":2725,\"old_only\":28,\"final_resolved\":2879,\"final_resolved_pct\":73.44}}}}}</script>\n<script>\n(() => {\n'use strict';\nconst qb = JSON.parse(document.getElementById('qb-data').textContent);\nconst meta = JSON.parse(document.getElementById('meta-data').textContent);\nconst el = id => document.getElementById(id);\n\nconst subjectConfig = {\n  '英語': {\n    types: ['空所補充','並びかえ','英作文','変形','見分け','答え方','間違い直し','読解','選択','答えから質問文'],\n    stagesByGrade: {\n      '中1': ['英語の語順','be動詞（現在形）','一般動詞（現在形）','be動詞と一般動詞','疑問詞','can','人称代名詞','命令文','一般動詞（３単現）','現在進行形','be動詞（過去形）','一般動詞（過去形）','過去進行形','there is ～，look ～ の文','未来の文','接続詞'],\n      '中2': ['英語の語順','be動詞（現在形）','一般動詞（現在形）','be動詞と一般動詞（現在形）','be動詞と一般動詞（過去形）','疑問詞','人称代名詞','命令文','進行形','there is ～ の文','未来の文','助動詞','接続詞','不定詞①','動名詞','文型①（look ～，give A B）','比較','文型②（call A B，make A B）','受動態','現在完了形（完了・経験）','現在完了形（継続），現在完了進行形','不定詞②'],\n      '中3': ['英語の語順','be動詞と一般動詞（現在形）','be動詞と一般動詞（過去形）','疑問詞','人称代名詞','進行形','命令文，there is ～ の文','未来の文','助動詞','接続詞','不定詞①，動名詞','比較','文型','受動態','現在完了形（完了・経験）','現在完了形（継続），現在完了進行形','不定詞②','分詞と間接疑問文','関係代名詞','仮定法']\n    },\n    stageMap: {\n      '中1': {'英語の語順':['並びかえ','英作文'],'be動詞（現在形）':['be動詞'],'一般動詞（現在形）':['一般動詞'],'be動詞と一般動詞':['区別'],'疑問詞':['疑問詞'],'can':['can'],'人称代名詞':['人称代名詞'],'命令文':['命令文'],'一般動詞（３単現）':['三単現'],'現在進行形':['現在進行形'],'be動詞（過去形）':['過去のbe動詞'],'一般動詞（過去形）':['一般動詞の過去形','不規則動詞','過去を表す語'],'過去進行形':['過去進行形'],'there is ～，look ～ の文':['there is ～・look ～ の文'],'未来の文':['未来の文'],'接続詞':['接続詞']},\n      '中2': {'英語の語順':['並びかえ','英作文'],'be動詞（現在形）':['be動詞'],'一般動詞（現在形）':['一般動詞'],'be動詞と一般動詞（現在形）':['be動詞と一般動詞（現在形）'],'be動詞と一般動詞（過去形）':['be動詞と一般動詞（過去形）'],'疑問詞':['疑問詞'],'人称代名詞':['人称代名詞'],'命令文':['命令文'],'進行形':['進行形'],'there is ～ の文':['there is ～ の文'],'未来の文':['未来表現','未来の文'],'助動詞':['助動詞'],'接続詞':['接続詞'],'不定詞①':['不定詞①','不定詞'],'動名詞':['動名詞'],'文型①（look ～，give A B）':['文型①（look ～，give A B）'],'比較':['比較'],'文型②（call A B，make A B）':['文型②（call A B，make A B）'],'受動態':['受動態'],'現在完了形（完了・経験）':['現在完了形（完了・経験）'],'現在完了形（継続），現在完了進行形':['現在完了形（継続），現在完了進行形'],'不定詞②':['不定詞②']},\n      '中3': {'英語の語順':['英語の語順'],'be動詞と一般動詞（現在形）':['be動詞と一般動詞（現在形）'],'be動詞と一般動詞（過去形）':['be動詞と一般動詞（過去形）'],'疑問詞':['疑問詞'],'人称代名詞':['人称代名詞'],'進行形':['進行形'],'命令文，there is ～ の文':['命令文，there is ～ の文'],'未来の文':['未来の文'],'助動詞':['助動詞'],'接続詞':['接続詞'],'不定詞①，動名詞':['不定詞①，動名詞'],'比較':['比較'],'文型':['文型'],'受動態':['受動態'],'現在完了形（完了・経験）':['現在完了形（完了・経験）'],'現在完了形（継続），現在完了進行形':['現在完了形（継続），現在完了進行形'],'不定詞②':['不定詞②'],'分詞と間接疑問文':['分詞と間接疑問文'],'関係代名詞':['関係代名詞'],'仮定法':['仮定法']}\n    }\n  },\n  '数学': {\n    types: ['計算','文章題','変形','グラフ'],\n    stagesByGrade: {'中1':['正負の数','文字式・方程式','比例・反比例'],'中2':['文字式・方程式'],'中3':['文字式・方程式']},\n    stageMap: {'中1':{'正負の数':['正負の数'],'文字式・方程式':['文字式・方程式'],'比例・反比例':['比例・反比例']},'中2':{'文字式・方程式':['文字式・方程式']},'中3':{'文字式・方程式':['文字式・方程式']}}\n  }\n};\n\nlet currentQuestions = [];\nlet showAnswers = false;\nconst STORAGE_KEY = 'mikamijuku_problem_app_v3';\n\nfunction captureState() {\n  return {\n    subject: el('subjectSelect')?.value || '英語',\n    grade: el('gradeSelect')?.value || '中1',\n    textbook: el('textbookSelect')?.value || '',\n    section: el('sectionSelect')?.value || '',\n    questionCount: el('questionCount')?.value || '10',\n    vocabMode: el('vocabMode')?.value || 'on',\n    pickMode: el('pickMode')?.value || 'random',\n    stages: selectedValues('input[data-stage]'),\n    types: selectedValues('input[data-type]'),\n    listSubject: el('listSubject')?.value || '英語',\n    listGrade: el('listGrade')?.value || 'all',\n    listLimit: el('listLimit')?.value || '50',\n    listKeyword: el('listKeyword')?.value || '',\n    listVocabMode: el('listVocabMode')?.value || 'same',\n    currentQuestionIds: currentQuestions.map(q => q.id),\n    showAnswers\n  };\n}\n\nfunction saveState(silent = true) {\n  try {\n    localStorage.setItem(STORAGE_KEY, JSON.stringify(captureState()));\n    if (!silent && el('globalSaveStatus')) {\n      el('globalSaveStatus').textContent = 'この端末に保存しました。';\n    }\n  } catch (e) {\n    if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存に失敗しました。';\n  }\n}\n\nfunction restoreQuestionSetByIds(ids) {\n  const map = new Map(qb.map(q => [q.id, q]));\n  const restored = (ids || []).map(id => map.get(id)).filter(Boolean);\n  if (restored.length) {\n    currentQuestions = restored;\n    el('questionPreview').classList.toggle('showAnswers', showAnswers);\n    el('questionPreview').innerHTML = restored.map((q, i) => `\n      <div class=\"q\">\n        <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n        <div>${q.q}</div>\n        <div class=\"ans\">答え: ${q.a}</div>\n      </div>\n    `).join('');\n    el('builderStatus').textContent = '保存されていた問題セットを復元しました。';\n    el('builderStatus').classList.add('good');\n  }\n}\n\nfunction safeSetValue(id, value) {\n  const node = el(id);\n  if (!node || value == null) return;\n  const has = Array.from(node.options || []).some(op => op.value === value || op.textContent === value);\n  if (has) node.value = value;\n}\n\nfunction setCheckedValues(selector, values) {\n  const set = new Set(values || []);\n  document.querySelectorAll(selector).forEach(ch => {\n    ch.checked = set.size ? set.has(ch.value) : true;\n  });\n}\n\nfunction applyState(state) {\n  if (!state) return;\n  safeSetValue('subjectSelect', state.subject);\n  safeSetValue('gradeSelect', state.grade);\n  renderSectionOptions();\n  renderStageOptions();\n  renderTypeOptions();\n  safeSetValue('textbookSelect', state.textbook);\n  renderSectionOptions();\n  safeSetValue('sectionSelect', state.section);\n  safeSetValue('questionCount', state.questionCount);\n  safeSetValue('vocabMode', state.vocabMode);\n  safeSetValue('pickMode', state.pickMode);\n  setCheckedValues('input[data-stage]', state.stages);\n  setCheckedValues('input[data-type]', state.types);\n  syncListSelectors();\n  safeSetValue('listSubject', state.listSubject);\n  safeSetValue('listGrade', state.listGrade);\n  safeSetValue('listLimit', state.listLimit);\n  safeSetValue('listVocabMode', state.listVocabMode);\n  if (el('listKeyword')) el('listKeyword').value = state.listKeyword || '';\n  showAnswers = !!state.showAnswers;\n  restoreQuestionSetByIds(state.currentQuestionIds || []);\n  updateBuilderStatus(baseFiltered());\n}\n\nfunction exportStateFile() {\n  const blob = new Blob([JSON.stringify(captureState(), null, 2)], {type:'application/json'});\n  const url = URL.createObjectURL(blob);\n  const a = document.createElement('a');\n  a.href = url;\n  a.download = 'mikamijuku_app_state_latest.json';\n  document.body.appendChild(a);\n  a.click();\n  a.remove();\n  setTimeout(() => URL.revokeObjectURL(url), 3000);\n  if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを書き出しました。';\n}\n\nfunction importStateFile(file) {\n  const fr = new FileReader();\n  fr.onload = () => {\n    try {\n      const state = JSON.parse(fr.result);\n      applyState(state);\n      saveState(true);\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを読み込みました。';\n    } catch (e) {\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データの読み込みに失敗しました。';\n    }\n  };\n  fr.readAsText(file, 'utf-8');\n}\n\nconst currentSubject = () => el('subjectSelect').value;\nconst currentGrade = () => el('gradeSelect').value;\nconst currentTextbook = () => el('textbookSelect').value;\nconst currentSectionIndex = () => Math.max(0, el('sectionSelect').selectedIndex);\nconst useVocabGate = () => el('vocabMode').value === 'on';\n\nfunction renderTabs() {\n  document.querySelectorAll('.tab').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.tab').forEach(x => x.classList.remove('active'));\n      document.querySelectorAll('.screen').forEach(x => x.classList.remove('active'));\n      btn.classList.add('active');\n      const target = document.getElementById('screen-' + btn.dataset.screen);\n      if (target) target.classList.add('active');\n    });\n  });\n}\n\nfunction renderTextbookOptions() {\n  el('textbookSelect').innerHTML = meta.textbooks.map(tb => `<option>${tb}</option>`).join('');\n}\n\nfunction renderSectionOptions() {\n  const subject = currentSubject();\n  const grade = currentGrade().replace('中','');\n  if (subject !== '英語') {\n    el('textbookWrap').classList.add('hidden');\n    el('sectionWrap').classList.add('hidden');\n    return;\n  }\n  el('textbookWrap').classList.remove('hidden');\n  el('sectionWrap').classList.remove('hidden');\n  const tb = currentTextbook() || meta.textbooks[0];\n  const sections = ((meta.sections[tb] || {})[grade] || []);\n  el('sectionSelect').innerHTML = sections.map(sec => `<option>${sec}</option>`).join('');\n}\n\nfunction renderStageOptions() {\n  const stages = subjectConfig[currentSubject()].stagesByGrade[currentGrade()] || [];\n  el('stageArea').innerHTML = stages.map(s => `<label class=\"ck\"><input type=\"checkbox\" data-stage value=\"${s}\" checked> <span>${s}</span></label>`).join('');\n}\n\nfunction renderTypeOptions() {\n  const types = subjectConfig[currentSubject()].types;\n  el('typeArea').innerHTML = types.map(t => `<label class=\"ck\"><input type=\"checkbox\" data-type value=\"${t}\" checked> <span>${t}</span></label>`).join('');\n}\n\nfunction selectedValues(selector) {\n  return Array.from(document.querySelectorAll(selector + ':checked')).map(x => x.value);\n}\n\nfunction selectedCategories() {\n  const stages = selectedValues('input[data-stage]');\n  if (!stages.length) return [];\n  const map = subjectConfig[currentSubject()].stageMap[currentGrade()] || {};\n  const set = new Set();\n  stages.forEach(stage => (map[stage] || [stage]).forEach(c => set.add(c)));\n  return Array.from(set);\n}\n\nfunction passesVocab(item, overrideMode) {\n  if (item.subject !== '英語') return true;\n  const mode = overrideMode || (useVocabGate() ? 'on' : 'off');\n  if (mode === 'off') return true;\n  if (item.grade !== currentGrade()) return true;\n  const rec = meta.passMeta[item.id] || {};\n  const minIdx = rec[currentTextbook()];\n  if (meta.vocabCoordinateVersion !== 'v7-2026-08-18-1based') return false;\n  if (minIdx === -2) return true;\n  if (!Number.isInteger(minIdx) || minIdx <= 0"
    },
    {
      "needle": "currentQuestions",
      "index": 2577051,
      "context": "読解','選択','答えから質問文'],\n    stagesByGrade: {\n      '中1': ['英語の語順','be動詞（現在形）','一般動詞（現在形）','be動詞と一般動詞','疑問詞','can','人称代名詞','命令文','一般動詞（３単現）','現在進行形','be動詞（過去形）','一般動詞（過去形）','過去進行形','there is ～，look ～ の文','未来の文','接続詞'],\n      '中2': ['英語の語順','be動詞（現在形）','一般動詞（現在形）','be動詞と一般動詞（現在形）','be動詞と一般動詞（過去形）','疑問詞','人称代名詞','命令文','進行形','there is ～ の文','未来の文','助動詞','接続詞','不定詞①','動名詞','文型①（look ～，give A B）','比較','文型②（call A B，make A B）','受動態','現在完了形（完了・経験）','現在完了形（継続），現在完了進行形','不定詞②'],\n      '中3': ['英語の語順','be動詞と一般動詞（現在形）','be動詞と一般動詞（過去形）','疑問詞','人称代名詞','進行形','命令文，there is ～ の文','未来の文','助動詞','接続詞','不定詞①，動名詞','比較','文型','受動態','現在完了形（完了・経験）','現在完了形（継続），現在完了進行形','不定詞②','分詞と間接疑問文','関係代名詞','仮定法']\n    },\n    stageMap: {\n      '中1': {'英語の語順':['並びかえ','英作文'],'be動詞（現在形）':['be動詞'],'一般動詞（現在形）':['一般動詞'],'be動詞と一般動詞':['区別'],'疑問詞':['疑問詞'],'can':['can'],'人称代名詞':['人称代名詞'],'命令文':['命令文'],'一般動詞（３単現）':['三単現'],'現在進行形':['現在進行形'],'be動詞（過去形）':['過去のbe動詞'],'一般動詞（過去形）':['一般動詞の過去形','不規則動詞','過去を表す語'],'過去進行形':['過去進行形'],'there is ～，look ～ の文':['there is ～・look ～ の文'],'未来の文':['未来の文'],'接続詞':['接続詞']},\n      '中2': {'英語の語順':['並びかえ','英作文'],'be動詞（現在形）':['be動詞'],'一般動詞（現在形）':['一般動詞'],'be動詞と一般動詞（現在形）':['be動詞と一般動詞（現在形）'],'be動詞と一般動詞（過去形）':['be動詞と一般動詞（過去形）'],'疑問詞':['疑問詞'],'人称代名詞':['人称代名詞'],'命令文':['命令文'],'進行形':['進行形'],'there is ～ の文':['there is ～ の文'],'未来の文':['未来表現','未来の文'],'助動詞':['助動詞'],'接続詞':['接続詞'],'不定詞①':['不定詞①','不定詞'],'動名詞':['動名詞'],'文型①（look ～，give A B）':['文型①（look ～，give A B）'],'比較':['比較'],'文型②（call A B，make A B）':['文型②（call A B，make A B）'],'受動態':['受動態'],'現在完了形（完了・経験）':['現在完了形（完了・経験）'],'現在完了形（継続），現在完了進行形':['現在完了形（継続），現在完了進行形'],'不定詞②':['不定詞②']},\n      '中3': {'英語の語順':['英語の語順'],'be動詞と一般動詞（現在形）':['be動詞と一般動詞（現在形）'],'be動詞と一般動詞（過去形）':['be動詞と一般動詞（過去形）'],'疑問詞':['疑問詞'],'人称代名詞':['人称代名詞'],'進行形':['進行形'],'命令文，there is ～ の文':['命令文，there is ～ の文'],'未来の文':['未来の文'],'助動詞':['助動詞'],'接続詞':['接続詞'],'不定詞①，動名詞':['不定詞①，動名詞'],'比較':['比較'],'文型':['文型'],'受動態':['受動態'],'現在完了形（完了・経験）':['現在完了形（完了・経験）'],'現在完了形（継続），現在完了進行形':['現在完了形（継続），現在完了進行形'],'不定詞②':['不定詞②'],'分詞と間接疑問文':['分詞と間接疑問文'],'関係代名詞':['関係代名詞'],'仮定法':['仮定法']}\n    }\n  },\n  '数学': {\n    types: ['計算','文章題','変形','グラフ'],\n    stagesByGrade: {'中1':['正負の数','文字式・方程式','比例・反比例'],'中2':['文字式・方程式'],'中3':['文字式・方程式']},\n    stageMap: {'中1':{'正負の数':['正負の数'],'文字式・方程式':['文字式・方程式'],'比例・反比例':['比例・反比例']},'中2':{'文字式・方程式':['文字式・方程式']},'中3':{'文字式・方程式':['文字式・方程式']}}\n  }\n};\n\nlet currentQuestions = [];\nlet showAnswers = false;\nconst STORAGE_KEY = 'mikamijuku_problem_app_v3';\n\nfunction captureState() {\n  return {\n    subject: el('subjectSelect')?.value || '英語',\n    grade: el('gradeSelect')?.value || '中1',\n    textbook: el('textbookSelect')?.value || '',\n    section: el('sectionSelect')?.value || '',\n    questionCount: el('questionCount')?.value || '10',\n    vocabMode: el('vocabMode')?.value || 'on',\n    pickMode: el('pickMode')?.value || 'random',\n    stages: selectedValues('input[data-stage]'),\n    types: selectedValues('input[data-type]'),\n    listSubject: el('listSubject')?.value || '英語',\n    listGrade: el('listGrade')?.value || 'all',\n    listLimit: el('listLimit')?.value || '50',\n    listKeyword: el('listKeyword')?.value || '',\n    listVocabMode: el('listVocabMode')?.value || 'same',\n    currentQuestionIds: currentQuestions.map(q => q.id),\n    showAnswers\n  };\n}\n\nfunction saveState(silent = true) {\n  try {\n    localStorage.setItem(STORAGE_KEY, JSON.stringify(captureState()));\n    if (!silent && el('globalSaveStatus')) {\n      el('globalSaveStatus').textContent = 'この端末に保存しました。';\n    }\n  } catch (e) {\n    if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存に失敗しました。';\n  }\n}\n\nfunction restoreQuestionSetByIds(ids) {\n  const map = new Map(qb.map(q => [q.id, q]));\n  const restored = (ids || []).map(id => map.get(id)).filter(Boolean);\n  if (restored.length) {\n    currentQuestions = restored;\n    el('questionPreview').classList.toggle('showAnswers', showAnswers);\n    el('questionPreview').innerHTML = restored.map((q, i) => `\n      <div class=\"q\">\n        <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n        <div>${q.q}</div>\n        <div class=\"ans\">答え: ${q.a}</div>\n      </div>\n    `).join('');\n    el('builderStatus').textContent = '保存されていた問題セットを復元しました。';\n    el('builderStatus').classList.add('good');\n  }\n}\n\nfunction safeSetValue(id, value) {\n  const node = el(id);\n  if (!node || value == null) return;\n  const has = Array.from(node.options || []).some(op => op.value === value || op.textContent === value);\n  if (has) node.value = value;\n}\n\nfunction setCheckedValues(selector, values) {\n  const set = new Set(values || []);\n  document.querySelectorAll(selector).forEach(ch => {\n    ch.checked = set.size ? set.has(ch.value) : true;\n  });\n}\n\nfunction applyState(state) {\n  if (!state) return;\n  safeSetValue('subjectSelect', state.subject);\n  safeSetValue('gradeSelect', state.grade);\n  renderSectionOptions();\n  renderStageOptions();\n  renderTypeOptions();\n  safeSetValue('textbookSelect', state.textbook);\n  renderSectionOptions();\n  safeSetValue('sectionSelect', state.section);\n  safeSetValue('questionCount', state.questionCount);\n  safeSetValue('vocabMode', state.vocabMode);\n  safeSetValue('pickMode', state.pickMode);\n  setCheckedValues('input[data-stage]', state.stages);\n  setCheckedValues('input[data-type]', state.types);\n  syncListSelectors();\n  safeSetValue('listSubject', state.listSubject);\n  safeSetValue('listGrade', state.listGrade);\n  safeSetValue('listLimit', state.listLimit);\n  safeSetValue('listVocabMode', state.listVocabMode);\n  if (el('listKeyword')) el('listKeyword').value = state.listKeyword || '';\n  showAnswers = !!state.showAnswers;\n  restoreQuestionSetByIds(state.currentQuestionIds || []);\n  updateBuilderStatus(baseFiltered());\n}\n\nfunction exportStateFile() {\n  const blob = new Blob([JSON.stringify(captureState(), null, 2)], {type:'application/json'});\n  const url = URL.createObjectURL(blob);\n  const a = document.createElement('a');\n  a.href = url;\n  a.download = 'mikamijuku_app_state_latest.json';\n  document.body.appendChild(a);\n  a.click();\n  a.remove();\n  setTimeout(() => URL.revokeObjectURL(url), 3000);\n  if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを書き出しました。';\n}\n\nfunction importStateFile(file) {\n  const fr = new FileReader();\n  fr.onload = () => {\n    try {\n      const state = JSON.parse(fr.result);\n      applyState(state);\n      saveState(true);\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを読み込みました。';\n    } catch (e) {\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データの読み込みに失敗しました。';\n    }\n  };\n  fr.readAsText(file, 'utf-8');\n}\n\nconst currentSubject = () => el('subjectSelect').value;\nconst currentGrade = () => el('gradeSelect').value;\nconst currentTextbook = () => el('textbookSelect').value;\nconst currentSectionIndex = () => Math.max(0, el('sectionSelect').selectedIndex);\nconst useVocabGate = () => el('vocabMode').value === 'on';\n\nfunction renderTabs() {\n  document.querySelectorAll('.tab').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.tab').forEach(x => x.classList.remove('active'));\n      document.querySelectorAll('.screen').forEach(x => x.classList.remove('active'));\n      btn.classList.add('active');\n      const target = document.getElementById('screen-' + btn.dataset.screen);\n      if (target) target.classList.add('active');\n    });\n  });\n}\n\nfunction renderTextbookOptions() {\n  el('textbookSelect').innerHTML = meta.textbooks.map(tb => `<option>${tb}</option>`).join('');\n}\n\nfunction renderSectionOptions() {\n  const subject = currentSubject();\n  const grade = currentGrade().replace('中','');\n  if (subject !== '英語') {\n    el('textbookWrap').classList.add('hidden');\n    el('sectionWrap').classList.add('hidden');\n    return;\n  }\n  el('textbookWrap').classList.remove('hidden');\n  el('sectionWrap').classList.remove('hidden');\n  const tb = currentTextbook() || meta.textbooks[0];\n  const sections = ((meta.sections[tb] || {})[grade] || []);\n  el('sectionSelect').innerHTML = sections.map(sec => `<option>${sec}</option>`).join('');\n}\n\nfunction renderStageOptions() {\n  const stages = subjectConfig[currentSubject()].stagesByGrade[currentGrade()] || [];\n  el('stageArea').innerHTML = stages.map(s => `<label class=\"ck\"><input type=\"checkbox\" data-stage value=\"${s}\" checked> <span>${s}</span></label>`).join('');\n}\n\nfunction renderTypeOptions() {\n  const types = subjectConfig[currentSubject()].types;\n  el('typeArea').innerHTML = types.map(t => `<label class=\"ck\"><input type=\"checkbox\" data-type value=\"${t}\" checked> <span>${t}</span></label>`).join('');\n}\n\nfunction selectedValues(selector) {\n  return Array.from(document.querySelectorAll(selector + ':checked')).map(x => x.value);\n}\n\nfunction selectedCategories() {\n  const stages = selectedValues('input[data-stage]');\n  if (!stages.length) return [];\n  const map = subjectConfig[currentSubject()].stageMap[currentGrade()] || {};\n  const set = new Set();\n  stages.forEach(stage => (map[stage] || [stage]).forEach(c => set.add(c)));\n  return Array.from(set);\n}\n\nfunction passesVocab(item, overrideMode) {\n  if (item.subject !== '英語') return true;\n  const mode = overrideMode || (useVocabGate() ? 'on' : 'off');\n  if (mode === 'off') return true;\n  if (item.grade !== currentGrade()) return true;\n  const rec = meta.passMeta[item.id] || {};\n  const minIdx = rec[currentTextbook()];\n  if (meta.vocabCoordinateVersion !== 'v7-2026-08-18-1based') return false;\n  if (minIdx === -2) return true;\n  if (!Number.isInteger(minIdx) || minIdx <= 0) return false;\n  const selectedOrdinal = currentSectionIndex() + 1;\n  return minIdx <= selectedOrdinal;\n}\n\n// ---- Mikami prerequisite grammar + runtime quality gates ----\nfunction selectedStageCeiling() {\n  if (currentSubject() !== '英語') return Number.MAX_SAFE_INTEGER;\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  const selected = selectedValues('input[data-stage]');\n  if (!selected.length) return -1;\n  return selected.reduce((max, s) => Math.max(max, stages.indexOf(s)), -1);\n}\n\nfunction stageIndexByNameContains(...needles) {\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  for (let i = 0; i < stages.length; i++) {\n    const s = stages[i];\n    if (needles.some(n => s.includes(n))) return i;\n  }\n  return -1;\n}\n\nfunction targetStageIndex(item) {\n  if (item.subject !== '英語') return"
    },
    {
      "needle": "currentQuestions",
      "index": 2577626,
      "context": "動詞','接続詞','不定詞①，動名詞','比較','文型','受動態','現在完了形（完了・経験）','現在完了形（継続），現在完了進行形','不定詞②','分詞と間接疑問文','関係代名詞','仮定法']\n    },\n    stageMap: {\n      '中1': {'英語の語順':['並びかえ','英作文'],'be動詞（現在形）':['be動詞'],'一般動詞（現在形）':['一般動詞'],'be動詞と一般動詞':['区別'],'疑問詞':['疑問詞'],'can':['can'],'人称代名詞':['人称代名詞'],'命令文':['命令文'],'一般動詞（３単現）':['三単現'],'現在進行形':['現在進行形'],'be動詞（過去形）':['過去のbe動詞'],'一般動詞（過去形）':['一般動詞の過去形','不規則動詞','過去を表す語'],'過去進行形':['過去進行形'],'there is ～，look ～ の文':['there is ～・look ～ の文'],'未来の文':['未来の文'],'接続詞':['接続詞']},\n      '中2': {'英語の語順':['並びかえ','英作文'],'be動詞（現在形）':['be動詞'],'一般動詞（現在形）':['一般動詞'],'be動詞と一般動詞（現在形）':['be動詞と一般動詞（現在形）'],'be動詞と一般動詞（過去形）':['be動詞と一般動詞（過去形）'],'疑問詞':['疑問詞'],'人称代名詞':['人称代名詞'],'命令文':['命令文'],'進行形':['進行形'],'there is ～ の文':['there is ～ の文'],'未来の文':['未来表現','未来の文'],'助動詞':['助動詞'],'接続詞':['接続詞'],'不定詞①':['不定詞①','不定詞'],'動名詞':['動名詞'],'文型①（look ～，give A B）':['文型①（look ～，give A B）'],'比較':['比較'],'文型②（call A B，make A B）':['文型②（call A B，make A B）'],'受動態':['受動態'],'現在完了形（完了・経験）':['現在完了形（完了・経験）'],'現在完了形（継続），現在完了進行形':['現在完了形（継続），現在完了進行形'],'不定詞②':['不定詞②']},\n      '中3': {'英語の語順':['英語の語順'],'be動詞と一般動詞（現在形）':['be動詞と一般動詞（現在形）'],'be動詞と一般動詞（過去形）':['be動詞と一般動詞（過去形）'],'疑問詞':['疑問詞'],'人称代名詞':['人称代名詞'],'進行形':['進行形'],'命令文，there is ～ の文':['命令文，there is ～ の文'],'未来の文':['未来の文'],'助動詞':['助動詞'],'接続詞':['接続詞'],'不定詞①，動名詞':['不定詞①，動名詞'],'比較':['比較'],'文型':['文型'],'受動態':['受動態'],'現在完了形（完了・経験）':['現在完了形（完了・経験）'],'現在完了形（継続），現在完了進行形':['現在完了形（継続），現在完了進行形'],'不定詞②':['不定詞②'],'分詞と間接疑問文':['分詞と間接疑問文'],'関係代名詞':['関係代名詞'],'仮定法':['仮定法']}\n    }\n  },\n  '数学': {\n    types: ['計算','文章題','変形','グラフ'],\n    stagesByGrade: {'中1':['正負の数','文字式・方程式','比例・反比例'],'中2':['文字式・方程式'],'中3':['文字式・方程式']},\n    stageMap: {'中1':{'正負の数':['正負の数'],'文字式・方程式':['文字式・方程式'],'比例・反比例':['比例・反比例']},'中2':{'文字式・方程式':['文字式・方程式']},'中3':{'文字式・方程式':['文字式・方程式']}}\n  }\n};\n\nlet currentQuestions = [];\nlet showAnswers = false;\nconst STORAGE_KEY = 'mikamijuku_problem_app_v3';\n\nfunction captureState() {\n  return {\n    subject: el('subjectSelect')?.value || '英語',\n    grade: el('gradeSelect')?.value || '中1',\n    textbook: el('textbookSelect')?.value || '',\n    section: el('sectionSelect')?.value || '',\n    questionCount: el('questionCount')?.value || '10',\n    vocabMode: el('vocabMode')?.value || 'on',\n    pickMode: el('pickMode')?.value || 'random',\n    stages: selectedValues('input[data-stage]'),\n    types: selectedValues('input[data-type]'),\n    listSubject: el('listSubject')?.value || '英語',\n    listGrade: el('listGrade')?.value || 'all',\n    listLimit: el('listLimit')?.value || '50',\n    listKeyword: el('listKeyword')?.value || '',\n    listVocabMode: el('listVocabMode')?.value || 'same',\n    currentQuestionIds: currentQuestions.map(q => q.id),\n    showAnswers\n  };\n}\n\nfunction saveState(silent = true) {\n  try {\n    localStorage.setItem(STORAGE_KEY, JSON.stringify(captureState()));\n    if (!silent && el('globalSaveStatus')) {\n      el('globalSaveStatus').textContent = 'この端末に保存しました。';\n    }\n  } catch (e) {\n    if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存に失敗しました。';\n  }\n}\n\nfunction restoreQuestionSetByIds(ids) {\n  const map = new Map(qb.map(q => [q.id, q]));\n  const restored = (ids || []).map(id => map.get(id)).filter(Boolean);\n  if (restored.length) {\n    currentQuestions = restored;\n    el('questionPreview').classList.toggle('showAnswers', showAnswers);\n    el('questionPreview').innerHTML = restored.map((q, i) => `\n      <div class=\"q\">\n        <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n        <div>${q.q}</div>\n        <div class=\"ans\">答え: ${q.a}</div>\n      </div>\n    `).join('');\n    el('builderStatus').textContent = '保存されていた問題セットを復元しました。';\n    el('builderStatus').classList.add('good');\n  }\n}\n\nfunction safeSetValue(id, value) {\n  const node = el(id);\n  if (!node || value == null) return;\n  const has = Array.from(node.options || []).some(op => op.value === value || op.textContent === value);\n  if (has) node.value = value;\n}\n\nfunction setCheckedValues(selector, values) {\n  const set = new Set(values || []);\n  document.querySelectorAll(selector).forEach(ch => {\n    ch.checked = set.size ? set.has(ch.value) : true;\n  });\n}\n\nfunction applyState(state) {\n  if (!state) return;\n  safeSetValue('subjectSelect', state.subject);\n  safeSetValue('gradeSelect', state.grade);\n  renderSectionOptions();\n  renderStageOptions();\n  renderTypeOptions();\n  safeSetValue('textbookSelect', state.textbook);\n  renderSectionOptions();\n  safeSetValue('sectionSelect', state.section);\n  safeSetValue('questionCount', state.questionCount);\n  safeSetValue('vocabMode', state.vocabMode);\n  safeSetValue('pickMode', state.pickMode);\n  setCheckedValues('input[data-stage]', state.stages);\n  setCheckedValues('input[data-type]', state.types);\n  syncListSelectors();\n  safeSetValue('listSubject', state.listSubject);\n  safeSetValue('listGrade', state.listGrade);\n  safeSetValue('listLimit', state.listLimit);\n  safeSetValue('listVocabMode', state.listVocabMode);\n  if (el('listKeyword')) el('listKeyword').value = state.listKeyword || '';\n  showAnswers = !!state.showAnswers;\n  restoreQuestionSetByIds(state.currentQuestionIds || []);\n  updateBuilderStatus(baseFiltered());\n}\n\nfunction exportStateFile() {\n  const blob = new Blob([JSON.stringify(captureState(), null, 2)], {type:'application/json'});\n  const url = URL.createObjectURL(blob);\n  const a = document.createElement('a');\n  a.href = url;\n  a.download = 'mikamijuku_app_state_latest.json';\n  document.body.appendChild(a);\n  a.click();\n  a.remove();\n  setTimeout(() => URL.revokeObjectURL(url), 3000);\n  if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを書き出しました。';\n}\n\nfunction importStateFile(file) {\n  const fr = new FileReader();\n  fr.onload = () => {\n    try {\n      const state = JSON.parse(fr.result);\n      applyState(state);\n      saveState(true);\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを読み込みました。';\n    } catch (e) {\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データの読み込みに失敗しました。';\n    }\n  };\n  fr.readAsText(file, 'utf-8');\n}\n\nconst currentSubject = () => el('subjectSelect').value;\nconst currentGrade = () => el('gradeSelect').value;\nconst currentTextbook = () => el('textbookSelect').value;\nconst currentSectionIndex = () => Math.max(0, el('sectionSelect').selectedIndex);\nconst useVocabGate = () => el('vocabMode').value === 'on';\n\nfunction renderTabs() {\n  document.querySelectorAll('.tab').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.tab').forEach(x => x.classList.remove('active'));\n      document.querySelectorAll('.screen').forEach(x => x.classList.remove('active'));\n      btn.classList.add('active');\n      const target = document.getElementById('screen-' + btn.dataset.screen);\n      if (target) target.classList.add('active');\n    });\n  });\n}\n\nfunction renderTextbookOptions() {\n  el('textbookSelect').innerHTML = meta.textbooks.map(tb => `<option>${tb}</option>`).join('');\n}\n\nfunction renderSectionOptions() {\n  const subject = currentSubject();\n  const grade = currentGrade().replace('中','');\n  if (subject !== '英語') {\n    el('textbookWrap').classList.add('hidden');\n    el('sectionWrap').classList.add('hidden');\n    return;\n  }\n  el('textbookWrap').classList.remove('hidden');\n  el('sectionWrap').classList.remove('hidden');\n  const tb = currentTextbook() || meta.textbooks[0];\n  const sections = ((meta.sections[tb] || {})[grade] || []);\n  el('sectionSelect').innerHTML = sections.map(sec => `<option>${sec}</option>`).join('');\n}\n\nfunction renderStageOptions() {\n  const stages = subjectConfig[currentSubject()].stagesByGrade[currentGrade()] || [];\n  el('stageArea').innerHTML = stages.map(s => `<label class=\"ck\"><input type=\"checkbox\" data-stage value=\"${s}\" checked> <span>${s}</span></label>`).join('');\n}\n\nfunction renderTypeOptions() {\n  const types = subjectConfig[currentSubject()].types;\n  el('typeArea').innerHTML = types.map(t => `<label class=\"ck\"><input type=\"checkbox\" data-type value=\"${t}\" checked> <span>${t}</span></label>`).join('');\n}\n\nfunction selectedValues(selector) {\n  return Array.from(document.querySelectorAll(selector + ':checked')).map(x => x.value);\n}\n\nfunction selectedCategories() {\n  const stages = selectedValues('input[data-stage]');\n  if (!stages.length) return [];\n  const map = subjectConfig[currentSubject()].stageMap[currentGrade()] || {};\n  const set = new Set();\n  stages.forEach(stage => (map[stage] || [stage]).forEach(c => set.add(c)));\n  return Array.from(set);\n}\n\nfunction passesVocab(item, overrideMode) {\n  if (item.subject !== '英語') return true;\n  const mode = overrideMode || (useVocabGate() ? 'on' : 'off');\n  if (mode === 'off') return true;\n  if (item.grade !== currentGrade()) return true;\n  const rec = meta.passMeta[item.id] || {};\n  const minIdx = rec[currentTextbook()];\n  if (meta.vocabCoordinateVersion !== 'v7-2026-08-18-1based') return false;\n  if (minIdx === -2) return true;\n  if (!Number.isInteger(minIdx) || minIdx <= 0) return false;\n  const selectedOrdinal = currentSectionIndex() + 1;\n  return minIdx <= selectedOrdinal;\n}\n\n// ---- Mikami prerequisite grammar + runtime quality gates ----\nfunction selectedStageCeiling() {\n  if (currentSubject() !== '英語') return Number.MAX_SAFE_INTEGER;\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  const selected = selectedValues('input[data-stage]');\n  if (!selected.length) return -1;\n  return selected.reduce((max, s) => Math.max(max, stages.indexOf(s)), -1);\n}\n\nfunction stageIndexByNameContains(...needles) {\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  for (let i = 0; i < stages.length; i++) {\n    const s = stages[i];\n    if (needles.some(n => s.includes(n))) return i;\n  }\n  return -1;\n}\n\nfunction targetStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const cfg = subjectConfig['英語'];\n  const stages = cfg.stagesByGrade[item.grade] || [];\n  const map = cfg.stageMap[item.grade] || {};\n  for (let i = 0; i < stages.length; i++) {\n    const cats = map[stages[i]] || [stages[i]];\n    if (cats.includes(item.category)) return i;\n  }\n  return -1;\n}\n\nfunction requiredGrammarStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const text = String(item.q || '') + ' ' + String(item.a || '');\n  let req = Math.max(0, targetStageIndex(item));\n  const bump = i => { if (i >= 0) req = Math.max(req, i); };\n\n  // Detect onl"
    },
    {
      "needle": "currentQuestions",
      "index": 2590785,
      "context": "Does He\\b/.test(both)) return false;\n  if (/否定文または疑問文/.test(q)) return false;\n  if (/日本語で説明しなさい/.test(q) && /^[A-Za-z]/.test(a)) return false;\n  if (/\\(\\s*she\\s*\\/\\s*her\\s*\\/\\s*her\\s*\\)/i.test(q)) return false;\n  if (/\\(\\s*he\\s*\\/\\s*him\\s*\\/\\s*him\\s*\\)/i.test(q)) return false;\n  if (String(item.id || '').startsWith('M2-INF2-')) {\n    if (item.type === '空所補充' && /^to$/i.test(a) && /\\bto\\s*\\(\\s*\\)\\s*(?=[A-Za-z])/i.test(q)) return false;\n    if (/^(?:He|She|Yuki|Mika|Takumi|Ken|Emi|Tom|The student|The teacher|This boy|This girl|My mother|My father|My brother|My sister|My friend) (?:want|need|like) to\\b/i.test(a)) return false;\n  }\n\n  // Target-grammar evidence: do not let a mislabeled vocabulary-only item through.\n  if (item.category === '動名詞' && !/[A-Za-z]+ing\\b/.test(both)) return false;\n  if (item.category === '比較' && !/\\bthan\\b|\\bas\\s+\\w+\\s+as\\b|\\bmost\\b|\\w+est\\b/i.test(both)) return false;\n  const infinitiveBlank = item.type === '空所補充' && /^to$/i.test(a) && /\\(\\s*\\)\\s*[A-Za-z]+\\b/.test(q);\n  if ((item.category === '不定詞' || item.category === '不定詞①' || item.category === '不定詞②') && !/\\bto\\s+[A-Za-z]+\\b/i.test(both) && !infinitiveBlank) return false;\n  if (String(item.category).startsWith('現在完了形') && !/\\b(?:have|has)\\b/i.test(both)) return false;\n  if (item.category === '関係代名詞') {\n    if (!/\\b(?:who|which|that)\\b/i.test(both)) return false;\n    if (item.type === '空所補充' && /\\(\\s*\\)/.test(q)) {\n      if (!/who\\s*\\/\\s*which/i.test(q)) return false;\n      if (!/^(?:who|which)$/i.test(a)) return false;\n    }\n  }\n  return true;\n}\n\nfunction baseFiltered(overrideMode) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const categories = selectedCategories();\n  const types = selectedValues('input[data-type]');\n  return qb.filter(item => {\n    if (item.subject !== subj) return false;\n    if (item.grade !== grade) return false;\n    if (categories.length && !categories.includes(item.category)) return false;\n    if (types.length && !types.includes(item.type)) return false;\n    if (!passesVocab(item, overrideMode)) return false;\n    if (!passesPrereqGrammar(item)) return false;\n    if (!passesQualityGate(item)) return false;\n    return true;\n  });\n}\n\nfunction shuffle(arr) {\n  const a = arr.slice();\n  for (let i=a.length-1;i>0;i--) {\n    const j = Math.floor(Math.random() * (i+1));\n    const tmp = a[i]; a[i] = a[j]; a[j] = tmp;\n  }\n  return a;\n}\n\nfunction updateBuilderStatus(candidates) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const sameGradeCount = qb.filter(x => x.subject === subj && x.grade === grade).length;\n  let msg = '全問題 ' + meta.counts.all + '件 / ' + grade + 'の' + subj + '元データ ' + sameGradeCount + '件 / この条件で使える候補 ' + candidates.length + '件';\n  if (subj === '英語') {\n    msg += ' / 教科書: ' + currentTextbook() + ' / 単語はここまで: ' + (el('sectionSelect').value || '未選択');\n  }\n  el('builderStatus').textContent = msg;\n  el('builderStatus').classList.toggle('good', candidates.length > 0);\n}\n\nfunction renderQuestions() {\n  const count = parseInt(el('questionCount').value, 10);\n  const pickMode = el('pickMode').value;\n  const candidates = baseFiltered();\n  updateBuilderStatus(candidates);\n  if (!candidates.length) {\n    currentQuestions = [];\n    el('questionPreview').innerHTML = 'この条件では問題が見つかりませんでした。単語制限を外すか、範囲や問題タイプを広げてください。';\n    return;\n  }\n  const picked = (pickMode === 'random' ? shuffle(candidates) : candidates).slice(0, count);\n  currentQuestions = picked;\n  el('questionPreview').classList.toggle('showAnswers', showAnswers);\n  el('questionPreview').innerHTML = picked.map((q, i) => `\n    <div class=\"q\">\n      <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n      <div>${q.q}</div>\n      <div class=\"ans\">答え: ${q.a}</div>\n    </div>\n  `).join('');\n}\n\nfunction printQuestions() {\n  if (!currentQuestions.length) return;\n  const escapeHtml = value => String(value ?? '').replace(/[&<>\"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[char]));\n  const renderQuestion = (q, index) => `<div class=\"q\"><div class=\"n\">Q${index + 1}　${escapeHtml(q.id)}</div><div class=\"body\">${escapeHtml(q.q)}</div>${showAnswers ? `<div class=\"a\">答え: ${escapeHtml(q.a)}</div>` : '<div class=\"answer-space\" aria-label=\"解答欄\"></div>'}</div>`;\n  const pages = [];\n  for (let startIndex = 0; startIndex < currentQuestions.length; startIndex += 10) {\n    const pageQuestions = currentQuestions.slice(startIndex, startIndex + 10);\n    pages.push({\n      html: pageQuestions.map((q, offset) => renderQuestion(q, startIndex + offset)).join(''),\n      rows: Math.ceil(pageQuestions.length / 2)\n    });\n  }\n  const printHtml = `<!doctype html><html lang=\"ja\"><head><meta charset=\"utf-8\"><title>問題プリント</title><style>\n  @page{size:A4 portrait;margin:8mm}\n  *{box-sizing:border-box}\n  body{font-family:-apple-system,BlinkMacSystemFont,\"Segoe UI\",\"Yu Gothic\",sans-serif;margin:0;color:#111;font-size:9pt;line-height:1.3}\n  .page{height:281mm;display:flex;flex-direction:column;overflow:hidden;page-break-after:always;break-after:page}\n  .page:last-child{page-break-after:auto;break-after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(saved));\n} catch (e) {}\nupdateBuilderStatus(baseFiltered());\n})();\n</script>\n\n</body>\n</html>"
    },
    {
      "needle": "currentQuestions",
      "index": 2591007,
      "context": "*he\\s*\\/\\s*him\\s*\\/\\s*him\\s*\\)/i.test(q)) return false;\n  if (String(item.id || '').startsWith('M2-INF2-')) {\n    if (item.type === '空所補充' && /^to$/i.test(a) && /\\bto\\s*\\(\\s*\\)\\s*(?=[A-Za-z])/i.test(q)) return false;\n    if (/^(?:He|She|Yuki|Mika|Takumi|Ken|Emi|Tom|The student|The teacher|This boy|This girl|My mother|My father|My brother|My sister|My friend) (?:want|need|like) to\\b/i.test(a)) return false;\n  }\n\n  // Target-grammar evidence: do not let a mislabeled vocabulary-only item through.\n  if (item.category === '動名詞' && !/[A-Za-z]+ing\\b/.test(both)) return false;\n  if (item.category === '比較' && !/\\bthan\\b|\\bas\\s+\\w+\\s+as\\b|\\bmost\\b|\\w+est\\b/i.test(both)) return false;\n  const infinitiveBlank = item.type === '空所補充' && /^to$/i.test(a) && /\\(\\s*\\)\\s*[A-Za-z]+\\b/.test(q);\n  if ((item.category === '不定詞' || item.category === '不定詞①' || item.category === '不定詞②') && !/\\bto\\s+[A-Za-z]+\\b/i.test(both) && !infinitiveBlank) return false;\n  if (String(item.category).startsWith('現在完了形') && !/\\b(?:have|has)\\b/i.test(both)) return false;\n  if (item.category === '関係代名詞') {\n    if (!/\\b(?:who|which|that)\\b/i.test(both)) return false;\n    if (item.type === '空所補充' && /\\(\\s*\\)/.test(q)) {\n      if (!/who\\s*\\/\\s*which/i.test(q)) return false;\n      if (!/^(?:who|which)$/i.test(a)) return false;\n    }\n  }\n  return true;\n}\n\nfunction baseFiltered(overrideMode) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const categories = selectedCategories();\n  const types = selectedValues('input[data-type]');\n  return qb.filter(item => {\n    if (item.subject !== subj) return false;\n    if (item.grade !== grade) return false;\n    if (categories.length && !categories.includes(item.category)) return false;\n    if (types.length && !types.includes(item.type)) return false;\n    if (!passesVocab(item, overrideMode)) return false;\n    if (!passesPrereqGrammar(item)) return false;\n    if (!passesQualityGate(item)) return false;\n    return true;\n  });\n}\n\nfunction shuffle(arr) {\n  const a = arr.slice();\n  for (let i=a.length-1;i>0;i--) {\n    const j = Math.floor(Math.random() * (i+1));\n    const tmp = a[i]; a[i] = a[j]; a[j] = tmp;\n  }\n  return a;\n}\n\nfunction updateBuilderStatus(candidates) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const sameGradeCount = qb.filter(x => x.subject === subj && x.grade === grade).length;\n  let msg = '全問題 ' + meta.counts.all + '件 / ' + grade + 'の' + subj + '元データ ' + sameGradeCount + '件 / この条件で使える候補 ' + candidates.length + '件';\n  if (subj === '英語') {\n    msg += ' / 教科書: ' + currentTextbook() + ' / 単語はここまで: ' + (el('sectionSelect').value || '未選択');\n  }\n  el('builderStatus').textContent = msg;\n  el('builderStatus').classList.toggle('good', candidates.length > 0);\n}\n\nfunction renderQuestions() {\n  const count = parseInt(el('questionCount').value, 10);\n  const pickMode = el('pickMode').value;\n  const candidates = baseFiltered();\n  updateBuilderStatus(candidates);\n  if (!candidates.length) {\n    currentQuestions = [];\n    el('questionPreview').innerHTML = 'この条件では問題が見つかりませんでした。単語制限を外すか、範囲や問題タイプを広げてください。';\n    return;\n  }\n  const picked = (pickMode === 'random' ? shuffle(candidates) : candidates).slice(0, count);\n  currentQuestions = picked;\n  el('questionPreview').classList.toggle('showAnswers', showAnswers);\n  el('questionPreview').innerHTML = picked.map((q, i) => `\n    <div class=\"q\">\n      <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n      <div>${q.q}</div>\n      <div class=\"ans\">答え: ${q.a}</div>\n    </div>\n  `).join('');\n}\n\nfunction printQuestions() {\n  if (!currentQuestions.length) return;\n  const escapeHtml = value => String(value ?? '').replace(/[&<>\"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[char]));\n  const renderQuestion = (q, index) => `<div class=\"q\"><div class=\"n\">Q${index + 1}　${escapeHtml(q.id)}</div><div class=\"body\">${escapeHtml(q.q)}</div>${showAnswers ? `<div class=\"a\">答え: ${escapeHtml(q.a)}</div>` : '<div class=\"answer-space\" aria-label=\"解答欄\"></div>'}</div>`;\n  const pages = [];\n  for (let startIndex = 0; startIndex < currentQuestions.length; startIndex += 10) {\n    const pageQuestions = currentQuestions.slice(startIndex, startIndex + 10);\n    pages.push({\n      html: pageQuestions.map((q, offset) => renderQuestion(q, startIndex + offset)).join(''),\n      rows: Math.ceil(pageQuestions.length / 2)\n    });\n  }\n  const printHtml = `<!doctype html><html lang=\"ja\"><head><meta charset=\"utf-8\"><title>問題プリント</title><style>\n  @page{size:A4 portrait;margin:8mm}\n  *{box-sizing:border-box}\n  body{font-family:-apple-system,BlinkMacSystemFont,\"Segoe UI\",\"Yu Gothic\",sans-serif;margin:0;color:#111;font-size:9pt;line-height:1.3}\n  .page{height:281mm;display:flex;flex-direction:column;overflow:hidden;page-break-after:always;break-after:page}\n  .page:last-child{page-break-after:auto;break-after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(saved));\n} catch (e) {}\nupdateBuilderStatus(baseFiltered());\n})();\n</script>\n\n</body>\n</html>"
    },
    {
      "needle": "currentQuestions",
      "index": 2591409,
      "context": " false;\n  }\n\n  // Target-grammar evidence: do not let a mislabeled vocabulary-only item through.\n  if (item.category === '動名詞' && !/[A-Za-z]+ing\\b/.test(both)) return false;\n  if (item.category === '比較' && !/\\bthan\\b|\\bas\\s+\\w+\\s+as\\b|\\bmost\\b|\\w+est\\b/i.test(both)) return false;\n  const infinitiveBlank = item.type === '空所補充' && /^to$/i.test(a) && /\\(\\s*\\)\\s*[A-Za-z]+\\b/.test(q);\n  if ((item.category === '不定詞' || item.category === '不定詞①' || item.category === '不定詞②') && !/\\bto\\s+[A-Za-z]+\\b/i.test(both) && !infinitiveBlank) return false;\n  if (String(item.category).startsWith('現在完了形') && !/\\b(?:have|has)\\b/i.test(both)) return false;\n  if (item.category === '関係代名詞') {\n    if (!/\\b(?:who|which|that)\\b/i.test(both)) return false;\n    if (item.type === '空所補充' && /\\(\\s*\\)/.test(q)) {\n      if (!/who\\s*\\/\\s*which/i.test(q)) return false;\n      if (!/^(?:who|which)$/i.test(a)) return false;\n    }\n  }\n  return true;\n}\n\nfunction baseFiltered(overrideMode) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const categories = selectedCategories();\n  const types = selectedValues('input[data-type]');\n  return qb.filter(item => {\n    if (item.subject !== subj) return false;\n    if (item.grade !== grade) return false;\n    if (categories.length && !categories.includes(item.category)) return false;\n    if (types.length && !types.includes(item.type)) return false;\n    if (!passesVocab(item, overrideMode)) return false;\n    if (!passesPrereqGrammar(item)) return false;\n    if (!passesQualityGate(item)) return false;\n    return true;\n  });\n}\n\nfunction shuffle(arr) {\n  const a = arr.slice();\n  for (let i=a.length-1;i>0;i--) {\n    const j = Math.floor(Math.random() * (i+1));\n    const tmp = a[i]; a[i] = a[j]; a[j] = tmp;\n  }\n  return a;\n}\n\nfunction updateBuilderStatus(candidates) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const sameGradeCount = qb.filter(x => x.subject === subj && x.grade === grade).length;\n  let msg = '全問題 ' + meta.counts.all + '件 / ' + grade + 'の' + subj + '元データ ' + sameGradeCount + '件 / この条件で使える候補 ' + candidates.length + '件';\n  if (subj === '英語') {\n    msg += ' / 教科書: ' + currentTextbook() + ' / 単語はここまで: ' + (el('sectionSelect').value || '未選択');\n  }\n  el('builderStatus').textContent = msg;\n  el('builderStatus').classList.toggle('good', candidates.length > 0);\n}\n\nfunction renderQuestions() {\n  const count = parseInt(el('questionCount').value, 10);\n  const pickMode = el('pickMode').value;\n  const candidates = baseFiltered();\n  updateBuilderStatus(candidates);\n  if (!candidates.length) {\n    currentQuestions = [];\n    el('questionPreview').innerHTML = 'この条件では問題が見つかりませんでした。単語制限を外すか、範囲や問題タイプを広げてください。';\n    return;\n  }\n  const picked = (pickMode === 'random' ? shuffle(candidates) : candidates).slice(0, count);\n  currentQuestions = picked;\n  el('questionPreview').classList.toggle('showAnswers', showAnswers);\n  el('questionPreview').innerHTML = picked.map((q, i) => `\n    <div class=\"q\">\n      <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n      <div>${q.q}</div>\n      <div class=\"ans\">答え: ${q.a}</div>\n    </div>\n  `).join('');\n}\n\nfunction printQuestions() {\n  if (!currentQuestions.length) return;\n  const escapeHtml = value => String(value ?? '').replace(/[&<>\"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[char]));\n  const renderQuestion = (q, index) => `<div class=\"q\"><div class=\"n\">Q${index + 1}　${escapeHtml(q.id)}</div><div class=\"body\">${escapeHtml(q.q)}</div>${showAnswers ? `<div class=\"a\">答え: ${escapeHtml(q.a)}</div>` : '<div class=\"answer-space\" aria-label=\"解答欄\"></div>'}</div>`;\n  const pages = [];\n  for (let startIndex = 0; startIndex < currentQuestions.length; startIndex += 10) {\n    const pageQuestions = currentQuestions.slice(startIndex, startIndex + 10);\n    pages.push({\n      html: pageQuestions.map((q, offset) => renderQuestion(q, startIndex + offset)).join(''),\n      rows: Math.ceil(pageQuestions.length / 2)\n    });\n  }\n  const printHtml = `<!doctype html><html lang=\"ja\"><head><meta charset=\"utf-8\"><title>問題プリント</title><style>\n  @page{size:A4 portrait;margin:8mm}\n  *{box-sizing:border-box}\n  body{font-family:-apple-system,BlinkMacSystemFont,\"Segoe UI\",\"Yu Gothic\",sans-serif;margin:0;color:#111;font-size:9pt;line-height:1.3}\n  .page{height:281mm;display:flex;flex-direction:column;overflow:hidden;page-break-after:always;break-after:page}\n  .page:last-child{page-break-after:auto;break-after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(saved));\n} catch (e) {}\nupdateBuilderStatus(baseFiltered());\n})();\n</script>\n\n</body>\n</html>"
    },
    {
      "needle": "currentQuestions",
      "index": 2591927,
      "context": "tiveBlank) return false;\n  if (String(item.category).startsWith('現在完了形') && !/\\b(?:have|has)\\b/i.test(both)) return false;\n  if (item.category === '関係代名詞') {\n    if (!/\\b(?:who|which|that)\\b/i.test(both)) return false;\n    if (item.type === '空所補充' && /\\(\\s*\\)/.test(q)) {\n      if (!/who\\s*\\/\\s*which/i.test(q)) return false;\n      if (!/^(?:who|which)$/i.test(a)) return false;\n    }\n  }\n  return true;\n}\n\nfunction baseFiltered(overrideMode) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const categories = selectedCategories();\n  const types = selectedValues('input[data-type]');\n  return qb.filter(item => {\n    if (item.subject !== subj) return false;\n    if (item.grade !== grade) return false;\n    if (categories.length && !categories.includes(item.category)) return false;\n    if (types.length && !types.includes(item.type)) return false;\n    if (!passesVocab(item, overrideMode)) return false;\n    if (!passesPrereqGrammar(item)) return false;\n    if (!passesQualityGate(item)) return false;\n    return true;\n  });\n}\n\nfunction shuffle(arr) {\n  const a = arr.slice();\n  for (let i=a.length-1;i>0;i--) {\n    const j = Math.floor(Math.random() * (i+1));\n    const tmp = a[i]; a[i] = a[j]; a[j] = tmp;\n  }\n  return a;\n}\n\nfunction updateBuilderStatus(candidates) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const sameGradeCount = qb.filter(x => x.subject === subj && x.grade === grade).length;\n  let msg = '全問題 ' + meta.counts.all + '件 / ' + grade + 'の' + subj + '元データ ' + sameGradeCount + '件 / この条件で使える候補 ' + candidates.length + '件';\n  if (subj === '英語') {\n    msg += ' / 教科書: ' + currentTextbook() + ' / 単語はここまで: ' + (el('sectionSelect').value || '未選択');\n  }\n  el('builderStatus').textContent = msg;\n  el('builderStatus').classList.toggle('good', candidates.length > 0);\n}\n\nfunction renderQuestions() {\n  const count = parseInt(el('questionCount').value, 10);\n  const pickMode = el('pickMode').value;\n  const candidates = baseFiltered();\n  updateBuilderStatus(candidates);\n  if (!candidates.length) {\n    currentQuestions = [];\n    el('questionPreview').innerHTML = 'この条件では問題が見つかりませんでした。単語制限を外すか、範囲や問題タイプを広げてください。';\n    return;\n  }\n  const picked = (pickMode === 'random' ? shuffle(candidates) : candidates).slice(0, count);\n  currentQuestions = picked;\n  el('questionPreview').classList.toggle('showAnswers', showAnswers);\n  el('questionPreview').innerHTML = picked.map((q, i) => `\n    <div class=\"q\">\n      <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n      <div>${q.q}</div>\n      <div class=\"ans\">答え: ${q.a}</div>\n    </div>\n  `).join('');\n}\n\nfunction printQuestions() {\n  if (!currentQuestions.length) return;\n  const escapeHtml = value => String(value ?? '').replace(/[&<>\"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[char]));\n  const renderQuestion = (q, index) => `<div class=\"q\"><div class=\"n\">Q${index + 1}　${escapeHtml(q.id)}</div><div class=\"body\">${escapeHtml(q.q)}</div>${showAnswers ? `<div class=\"a\">答え: ${escapeHtml(q.a)}</div>` : '<div class=\"answer-space\" aria-label=\"解答欄\"></div>'}</div>`;\n  const pages = [];\n  for (let startIndex = 0; startIndex < currentQuestions.length; startIndex += 10) {\n    const pageQuestions = currentQuestions.slice(startIndex, startIndex + 10);\n    pages.push({\n      html: pageQuestions.map((q, offset) => renderQuestion(q, startIndex + offset)).join(''),\n      rows: Math.ceil(pageQuestions.length / 2)\n    });\n  }\n  const printHtml = `<!doctype html><html lang=\"ja\"><head><meta charset=\"utf-8\"><title>問題プリント</title><style>\n  @page{size:A4 portrait;margin:8mm}\n  *{box-sizing:border-box}\n  body{font-family:-apple-system,BlinkMacSystemFont,\"Segoe UI\",\"Yu Gothic\",sans-serif;margin:0;color:#111;font-size:9pt;line-height:1.3}\n  .page{height:281mm;display:flex;flex-direction:column;overflow:hidden;page-break-after:always;break-after:page}\n  .page:last-child{page-break-after:auto;break-after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(saved));\n} catch (e) {}\nupdateBuilderStatus(baseFiltered());\n})();\n</script>\n\n</body>\n</html>"
    },
    {
      "needle": "currentQuestions",
      "index": 2591998,
      "context": ") && !/\\b(?:have|has)\\b/i.test(both)) return false;\n  if (item.category === '関係代名詞') {\n    if (!/\\b(?:who|which|that)\\b/i.test(both)) return false;\n    if (item.type === '空所補充' && /\\(\\s*\\)/.test(q)) {\n      if (!/who\\s*\\/\\s*which/i.test(q)) return false;\n      if (!/^(?:who|which)$/i.test(a)) return false;\n    }\n  }\n  return true;\n}\n\nfunction baseFiltered(overrideMode) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const categories = selectedCategories();\n  const types = selectedValues('input[data-type]');\n  return qb.filter(item => {\n    if (item.subject !== subj) return false;\n    if (item.grade !== grade) return false;\n    if (categories.length && !categories.includes(item.category)) return false;\n    if (types.length && !types.includes(item.type)) return false;\n    if (!passesVocab(item, overrideMode)) return false;\n    if (!passesPrereqGrammar(item)) return false;\n    if (!passesQualityGate(item)) return false;\n    return true;\n  });\n}\n\nfunction shuffle(arr) {\n  const a = arr.slice();\n  for (let i=a.length-1;i>0;i--) {\n    const j = Math.floor(Math.random() * (i+1));\n    const tmp = a[i]; a[i] = a[j]; a[j] = tmp;\n  }\n  return a;\n}\n\nfunction updateBuilderStatus(candidates) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const sameGradeCount = qb.filter(x => x.subject === subj && x.grade === grade).length;\n  let msg = '全問題 ' + meta.counts.all + '件 / ' + grade + 'の' + subj + '元データ ' + sameGradeCount + '件 / この条件で使える候補 ' + candidates.length + '件';\n  if (subj === '英語') {\n    msg += ' / 教科書: ' + currentTextbook() + ' / 単語はここまで: ' + (el('sectionSelect').value || '未選択');\n  }\n  el('builderStatus').textContent = msg;\n  el('builderStatus').classList.toggle('good', candidates.length > 0);\n}\n\nfunction renderQuestions() {\n  const count = parseInt(el('questionCount').value, 10);\n  const pickMode = el('pickMode').value;\n  const candidates = baseFiltered();\n  updateBuilderStatus(candidates);\n  if (!candidates.length) {\n    currentQuestions = [];\n    el('questionPreview').innerHTML = 'この条件では問題が見つかりませんでした。単語制限を外すか、範囲や問題タイプを広げてください。';\n    return;\n  }\n  const picked = (pickMode === 'random' ? shuffle(candidates) : candidates).slice(0, count);\n  currentQuestions = picked;\n  el('questionPreview').classList.toggle('showAnswers', showAnswers);\n  el('questionPreview').innerHTML = picked.map((q, i) => `\n    <div class=\"q\">\n      <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n      <div>${q.q}</div>\n      <div class=\"ans\">答え: ${q.a}</div>\n    </div>\n  `).join('');\n}\n\nfunction printQuestions() {\n  if (!currentQuestions.length) return;\n  const escapeHtml = value => String(value ?? '').replace(/[&<>\"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[char]));\n  const renderQuestion = (q, index) => `<div class=\"q\"><div class=\"n\">Q${index + 1}　${escapeHtml(q.id)}</div><div class=\"body\">${escapeHtml(q.q)}</div>${showAnswers ? `<div class=\"a\">答え: ${escapeHtml(q.a)}</div>` : '<div class=\"answer-space\" aria-label=\"解答欄\"></div>'}</div>`;\n  const pages = [];\n  for (let startIndex = 0; startIndex < currentQuestions.length; startIndex += 10) {\n    const pageQuestions = currentQuestions.slice(startIndex, startIndex + 10);\n    pages.push({\n      html: pageQuestions.map((q, offset) => renderQuestion(q, startIndex + offset)).join(''),\n      rows: Math.ceil(pageQuestions.length / 2)\n    });\n  }\n  const printHtml = `<!doctype html><html lang=\"ja\"><head><meta charset=\"utf-8\"><title>問題プリント</title><style>\n  @page{size:A4 portrait;margin:8mm}\n  *{box-sizing:border-box}\n  body{font-family:-apple-system,BlinkMacSystemFont,\"Segoe UI\",\"Yu Gothic\",sans-serif;margin:0;color:#111;font-size:9pt;line-height:1.3}\n  .page{height:281mm;display:flex;flex-direction:column;overflow:hidden;page-break-after:always;break-after:page}\n  .page:last-child{page-break-after:auto;break-after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(saved));\n} catch (e) {}\nupdateBuilderStatus(baseFiltered());\n})();\n</script>\n\n</body>\n</html>"
    },
    {
      "needle": "questionCount",
      "index": 5014,
      "context": "font-size:14px}\n.note{font-size:12px;color:var(--muted);line-height:1.7}\n.status{margin-top:12px;padding:12px 14px;border-radius:14px;background:#eff6ff;border:1px solid #bfdbfe;color:#1d4ed8;line-height:1.8}\n.status.good{background:#ecfdf5;border-color:#bbf7d0;color:#166534}\n.checks{display:grid;grid-template-columns:1fr;gap:8px;max-height:260px;overflow:auto;border:1px solid var(--line);border-radius:14px;padding:10px;background:#fafcff}\n.ck{display:flex;gap:8px;align-items:flex-start;font-size:14px}\n.preview{min-height:240px}\n.q{padding:10px 0;border-bottom:1px dashed var(--line)} .q:last-child{border-bottom:0}\n.qnum{font-weight:800;color:var(--blue);margin-bottom:6px}\n.ans{display:none;margin-top:8px;padding-top:8px;border-top:1px dashed var(--line);color:#166534}\n.showAnswers .ans{display:block}\n.tableWrap{overflow:auto;max-height:70vh;border:1px solid var(--line);border-radius:14px}\ntable{width:100%;border-collapse:collapse;background:#fff} th,td{padding:8px 10px;border-bottom:1px solid var(--line);font-size:13px;vertical-align:top;text-align:left} th{position:sticky;top:0;background:#f8fafc}\n.hidden{display:none!important}\n\n.twoCol{display:grid;grid-template-columns:1fr 1fr;gap:16px}\n@media (max-width:1100px){.twoCol{grid-template-columns:1fr}}\n.resultBox{margin-top:12px;padding:14px;border:1px solid var(--line);border-radius:14px;background:#fff;line-height:1.8}\n.pill{display:inline-block;padding:5px 10px;border-radius:999px;background:var(--soft);color:#3730a3;font-size:12px;margin:0 6px 6px 0}\nimg.previewImg{max-width:100%;max-height:320px;border-radius:12px;border:1px solid var(--line)}\n\n@media print{header,.tabs,.builderOnly,.listControls{display:none!important} body{background:#fff} .card{border:none;box-shadow:none;padding:0}}\n</style>\n</head>\n<body>\n<header>\n  <h1>みかみ塾 英語問題アプリ</h1>\n  <div class=\"sub\">問題作成・問題一覧・問題プリント印刷のためのアプリです。答案写真の分析、配布資料、面談資料、印刷用レポートは「みかみ塾 答案分析GPT」で作成します。</div>\n  <div class=\"btns\" style=\"margin-top:12px\">\n    <button id=\"exportJsonBtn\">保存データを書き出す</button>\n    <button id=\"importJsonBtn\">保存データを読み込む</button>\n    <input id=\"importJsonInput\" type=\"file\" accept=\".json,application/json\" class=\"hidden\">\n  </div>\n  <div id=\"globalSaveStatus\" class=\"sub\" style=\"margin-top:8px\">自動保存に加えて、必要な時だけ保存データを書き出せます。</div>\n</header>\n\n<div class=\"wrap\">\n  <div class=\"tabs\">\n    <button class=\"tab active\" data-screen=\"builder\">問題作成</button>\n    <button class=\"tab\" data-screen=\"list\">問題一覧</button>\n  </div>\n\n  <div id=\"screen-builder\" class=\"screen active\">\n    <div class=\"layout\">\n      <div class=\"card\">\n        <div class=\"row2\">\n          <div><label>科目</label><select id=\"subjectSelect\"><option>英語</option><option>数学</option></select></div>\n          <div><label>学年</label><select id=\"gradeSelect\"><option>中1</option><option>中2</option><option>中3</option></select></div>\n        </div>\n        <div class=\"row2\" style=\"margin-top:12px\">\n          <div id=\"textbookWrap\"><label>教科書</label><select id=\"textbookSelect\"></select></div>\n          <div id=\"sectionWrap\"><label>単語はここまで</label><select id=\"sectionSelect\"></select></div>\n        </div>\n        <div class=\"row3\" style=\"margin-top:12px\">\n          <div><label>問題数</label><select id=\"questionCount\"><option>5</option><option selected>10</option><option>15</option><option>20</option></select></div>\n          <div><label>単語制限</label><select id=\"vocabMode\"><option value=\"on\" selected>使う</option><option value=\"off\">使わない</option></select></div>\n          <div><label>候補の選び方</label><select id=\"pickMode\"><option value=\"random\" selected>ランダム</option><option value=\"top\">上から順</option></select></div>\n        </div>\n        <div class=\"note\" style=\"margin-top:10px\">意味: たとえば「中3 / UNIT3まで」は、中1全部 + 中2全部 + 中3のそこまで の単語を使います。</div>\n        <div style=\"margin-top:14px\"><label>習った文法範囲</label><div id=\"stageArea\" class=\"checks\"></div></div>\n        <div style=\"margin-top:14px\"><label>出したい問題タイプ</label><div id=\"typeArea\" class=\"checks\"></div></div>\n        <div class=\"btns builderOnly\" style=\"margin-top:14px\">\n          <button id=\"generateBtn\" class=\"primary\">問題を作成</button>\n          <button id=\"toggleAnswersBtn\">解答表示切替</button>\n          <button id=\"printBtn\">印刷</button>\n          <button id=\"toListBtn\">この条件で問題一覧へ</button>\n        </div>\n        <div id=\"builderStatus\" class=\"status\">準備中…</div>\n      </div>\n      <div class=\"card preview\"><div id=\"questionPreview\">まだ問題を作成していません。</div></div>\n    </div>\n  </div>\n\n  <div id=\"screen-list\" class=\"screen\">\n    <div class=\"card\">\n      <div class=\"row3 listControls\">\n        <div><label>科目</label><select id=\"listSubject\"><option value=\"英語\">英語</option><option value=\"数学\">数学</option><option value=\"all\">すべて</option></select></div>\n        <div><label>学年</label><select id=\"listGrade\"><option value=\"all\">すべて</option><option>中1</option><option>中2</option><option>中3</option></select></div>\n        <div><label>表示件数</label><select id=\"listLimit\"><option>20</option><option selected>50</option><option>100</option><option>200</option></select></div>\n      </div>\n      <div class=\"row2 listControls\" style=\"margin-top:12px\">\n        <div><label>キーワード</label><input id=\"listKeyword\" type=\"text\" placeholder=\"問題文 / 解答 / ID で検索\"></div>\n        <div><label>表示モード</label><select id=\"listVocabMode\"><option value=\"same\">問題作成の単語制限を使う</option><option value=\"off\">単語制限を使わない</option></select></div>\n      </div>\n      <div class=\"btns listControls\" style=\"margin-top:14px\"><button id=\"refreshListBtn\" class=\"primary\">一覧を更新</button></div>\n      <div id=\"listStatus\" class=\"status\">一覧を更新してください。</div>\n      <div class=\"tableWrap\" style=\"margin-top:12px\">\n        <table>\n          <thead><tr><th>ID</th><th>科目</th><th>学年</th><th>親項目</th><th>タイプ</th><th>問題</th><th>解答</th></tr></thead>\n          <tbody id=\"listBody\"></tbody>\n        </table>\n      </div>\n    </div>\n  </div>\n\n  \n  <script id=\"qb-data\" type=\"application/json\">[{\"id\":\"E1-BQ-001\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"be動詞\",\"type\":\"変形\",\"mistake\":\"not の位置ミス\",\"cause\":\"変形ルール未定着\",\"q\":\"He is busy. を否定文にしなさい。\",\"a\":\"He is not busy.\"},{\"id\":\"E1-BQ-002\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"be動詞\",\"type\":\"変形\",\"mistake\":\"be動詞を前に出せない\",\"cause\":\"疑問文の型不足\",\"q\":\"You are happy. を疑問文にしなさい。\",\"a\":\"Are you happy?\"},{\"id\":\"E1-BQ-003\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"be動詞\",\"type\":\"答え方\",\"mistake\":\"Yes/No 応答ミス\",\"cause\":\"答え方の型不足\",\"q\":\"Are you a student? に Yes で答えなさい。\",\"a\":\"Yes, I am.\"},{\"id\":\"E1-BQ-004\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"be動詞\",\"type\":\"並びかえ\",\"mistake\":\"not の位置ミス\",\"cause\":\"語順不安定\",\"q\":\"次の語(句)を正しい順に並べかえなさい。 ( is / she / not / kind )\",\"a\":\"She is not kind.\"},{\"id\":\"E1-BQ-005\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"be動詞\",\"type\":\"間違い直し\",\"mistake\":\"主語に合う be 動詞ミス\",\"cause\":\"主語と動詞の対応不足\",\"q\":\"Are he busy? の誤りを直しなさい。\",\"a\":\"Is he busy?\"},{\"id\":\"E1-BQ-006\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"be動詞\",\"type\":\"変形\",\"mistake\":\"am not の位置ミス\",\"cause\":\"否定文の型不足\",\"q\":\"I am busy. を否定文にしなさい。\",\"a\":\"I am not busy.\"},{\"id\":\"E1-BQ-007\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"be動詞\",\"type\":\"変形\",\"mistake\":\"疑問文の語順ミス\",\"cause\":\"be動詞を前に出せない\",\"q\":\"She is a teacher. を疑問文にしなさい。\",\"a\":\"Is she a teacher?\"},{\"id\":\"E1-BQ-008\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"be動詞\",\"type\":\"答え方\",\"mistake\":\"No 応答ミス\",\"cause\":\"be動詞文の答え方不足\",\"q\":\"Is he from Japan? に No で答えなさい。\",\"a\":\"No, he is not.\"},{\"id\":\"E1-BQ-009\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"be動詞\",\"type\":\"並びかえ\",\"mistake\":\"not の位置ミス\",\"cause\":\"語順不安定\",\"q\":\"次の語(句)を正しい順に並べかえなさい。 ( are / not / they / students )\",\"a\":\"They are not students.\"},{\"id\":\"E1-BQ-010\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"be動詞\",\"type\":\"間違い直し\",\"mistake\":\"主語に合う be 動詞ミス\",\"cause\":\"主語と動詞の対応不足\",\"q\":\"Am you busy? の誤りを直しなさい。\",\"a\":\"Are you busy?\"},{\"id\":\"E1-BQ-011\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"be動詞\",\"type\":\"空所補充\",\"mistake\":\"be動詞選択ミス\",\"cause\":\"主語と be 動詞の対応不足\",\"q\":\"(      ) she from Canada? 「カナダ出身ですか」の意味になるように、空所に入る最も適切な語(句)を書きなさい。\",\"a\":\"Is\"},{\"id\":\"E1-BQ-012\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"be動詞\",\"type\":\"空所補充\",\"mistake\":\"be動詞選択ミス\",\"cause\":\"主語と be 動詞の対応不足\",\"q\":\"We (      ) in the classroom. 「教室にいます」の意味になるように、空所に入る最も適切な語(句)を書きなさい。\",\"a\":\"are\"},{\"id\":\"E1-BQ-013\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"be動詞\",\"type\":\"変形\",\"mistake\":\"否定文ミス\",\"cause\":\"否定文の型不足\",\"q\":\"They are busy. を否定文にしなさい。\",\"a\":\"They are not busy.\"},{\"id\":\"E1-BQ-014\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"be動詞\",\"type\":\"答え方\",\"mistake\":\"Yes/No 応答ミス\",\"cause\":\"答え方の型不足\",\"q\":\"Are they soccer players? に Yes で答えなさい。\",\"a\":\"Yes, they are.\"},{\"id\":\"E1-BQ-015\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"be動詞\",\"type\":\"英作文\",\"mistake\":\"be動詞文の骨組みミス\",\"cause\":\"主語＋be の型不足\",\"q\":\"次の日本語に合う英文を書きなさい。『私は日本出身です。』\",\"a\":\"I am from Japan.\"},{\"id\":\"E1-DQ-001\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"一般動詞\",\"type\":\"変形\",\"mistake\":\"don’t の位置ミス\",\"cause\":\"一般動詞否定文の型不足\",\"q\":\"You play soccer. を否定文にしなさい。\",\"a\":\"You do not play soccer.\"},{\"id\":\"E1-DQ-002\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"一般動詞\",\"type\":\"変形\",\"mistake\":\"be動詞疑問文にしてしまう\",\"cause\":\"文の種類判定不足\",\"q\":\"They study English. を疑問文にしなさい。\",\"a\":\"Do they study English?\"},{\"id\":\"E1-DQ-003\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"一般動詞\",\"type\":\"答え方\",\"mistake\":\"Yes/No 応答ミス\",\"cause\":\"答え方の型不足\",\"q\":\"Do you like music? に No で答えなさい。\",\"a\":\"No, I do not.\"},{\"id\":\"E1-DQ-004\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"一般動詞\",\"type\":\"並びかえ\",\"mistake\":\"do 疑問文の語順ミス\",\"cause\":\"疑問文の型不足\",\"q\":\"次の語(句)を正しい順に並べかえなさい。 ( do / you / tennis / play / ? )\",\"a\":\"Do you play tennis?\"},{\"id\":\"E1-DQ-005\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"一般動詞\",\"type\":\"間違い直し\",\"mistake\":\"原形に戻せない\",\"cause\":\"do の後ろは原形の理解不足\",\"q\":\"You don’t plays baseball. の誤りを直しなさい。\",\"a\":\"You don’t play baseball.\"},{\"id\":\"E1-DQ-006\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"一般動詞\",\"type\":\"変形\",\"mistake\":\"否定文の型ミス\",\"cause\":\"do not の位置が不安定\",\"q\":\"I like math. を否定文にしなさい。\",\"a\":\"I do not like math.\"},{\"id\":\"E1-DQ-007\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"一般動詞\",\"type\":\"変形\",\"mistake\":\"疑問文の型ミス\",\"cause\":\"Do を前に出せない\",\"q\":\"You study English. を疑問文にしなさい。\",\"a\":\"Do you study English?\"},{\"id\":\"E1-DQ-008\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"一般動詞\",\"type\":\"答え方\",\"mistake\":\"Yes 応答ミス\",\"cause\":\"答え方の型不足\",\"q\":\"Do they play tennis? に Yes で答えなさい。\",\"a\":\"Yes, they do.\"},{\"id\":\"E1-DQ-009\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"一般動詞\",\"type\":\"並びかえ\",\"mistake\":\"否定文語順ミス\",\"cause\":\"do not の位置が不安定\",\"q\":\"次の語(句)を正しい順に並べかえなさい。 ( not / we / do / like / dogs )\",\"a\":\"We do not like dogs.\"},{\"id\":\"E1-DQ-010\",\"subject\":\"英語\",\"grade\":\"中1\",\"category\":\"一般動詞\",\"type\":\"間違い直し\",\"mist"
    },
    {
      "needle": "questionCount",
      "index": 2576532,
      "context": "\"reconstructed\":2851,\"reconstructed_pct\":72.73,\"trusted_old\":154,\"trusted_old_pct\":3.93,\"both\":126,\"reconstructed_only\":2725,\"old_only\":28,\"final_resolved\":2879,\"final_resolved_pct\":73.44}}}}}</script>\n<script>\n(() => {\n'use strict';\nconst qb = JSON.parse(document.getElementById('qb-data').textContent);\nconst meta = JSON.parse(document.getElementById('meta-data').textContent);\nconst el = id => document.getElementById(id);\n\nconst subjectConfig = {\n  '英語': {\n    types: ['空所補充','並びかえ','英作文','変形','見分け','答え方','間違い直し','読解','選択','答えから質問文'],\n    stagesByGrade: {\n      '中1': ['英語の語順','be動詞（現在形）','一般動詞（現在形）','be動詞と一般動詞','疑問詞','can','人称代名詞','命令文','一般動詞（３単現）','現在進行形','be動詞（過去形）','一般動詞（過去形）','過去進行形','there is ～，look ～ の文','未来の文','接続詞'],\n      '中2': ['英語の語順','be動詞（現在形）','一般動詞（現在形）','be動詞と一般動詞（現在形）','be動詞と一般動詞（過去形）','疑問詞','人称代名詞','命令文','進行形','there is ～ の文','未来の文','助動詞','接続詞','不定詞①','動名詞','文型①（look ～，give A B）','比較','文型②（call A B，make A B）','受動態','現在完了形（完了・経験）','現在完了形（継続），現在完了進行形','不定詞②'],\n      '中3': ['英語の語順','be動詞と一般動詞（現在形）','be動詞と一般動詞（過去形）','疑問詞','人称代名詞','進行形','命令文，there is ～ の文','未来の文','助動詞','接続詞','不定詞①，動名詞','比較','文型','受動態','現在完了形（完了・経験）','現在完了形（継続），現在完了進行形','不定詞②','分詞と間接疑問文','関係代名詞','仮定法']\n    },\n    stageMap: {\n      '中1': {'英語の語順':['並びかえ','英作文'],'be動詞（現在形）':['be動詞'],'一般動詞（現在形）':['一般動詞'],'be動詞と一般動詞':['区別'],'疑問詞':['疑問詞'],'can':['can'],'人称代名詞':['人称代名詞'],'命令文':['命令文'],'一般動詞（３単現）':['三単現'],'現在進行形':['現在進行形'],'be動詞（過去形）':['過去のbe動詞'],'一般動詞（過去形）':['一般動詞の過去形','不規則動詞','過去を表す語'],'過去進行形':['過去進行形'],'there is ～，look ～ の文':['there is ～・look ～ の文'],'未来の文':['未来の文'],'接続詞':['接続詞']},\n      '中2': {'英語の語順':['並びかえ','英作文'],'be動詞（現在形）':['be動詞'],'一般動詞（現在形）':['一般動詞'],'be動詞と一般動詞（現在形）':['be動詞と一般動詞（現在形）'],'be動詞と一般動詞（過去形）':['be動詞と一般動詞（過去形）'],'疑問詞':['疑問詞'],'人称代名詞':['人称代名詞'],'命令文':['命令文'],'進行形':['進行形'],'there is ～ の文':['there is ～ の文'],'未来の文':['未来表現','未来の文'],'助動詞':['助動詞'],'接続詞':['接続詞'],'不定詞①':['不定詞①','不定詞'],'動名詞':['動名詞'],'文型①（look ～，give A B）':['文型①（look ～，give A B）'],'比較':['比較'],'文型②（call A B，make A B）':['文型②（call A B，make A B）'],'受動態':['受動態'],'現在完了形（完了・経験）':['現在完了形（完了・経験）'],'現在完了形（継続），現在完了進行形':['現在完了形（継続），現在完了進行形'],'不定詞②':['不定詞②']},\n      '中3': {'英語の語順':['英語の語順'],'be動詞と一般動詞（現在形）':['be動詞と一般動詞（現在形）'],'be動詞と一般動詞（過去形）':['be動詞と一般動詞（過去形）'],'疑問詞':['疑問詞'],'人称代名詞':['人称代名詞'],'進行形':['進行形'],'命令文，there is ～ の文':['命令文，there is ～ の文'],'未来の文':['未来の文'],'助動詞':['助動詞'],'接続詞':['接続詞'],'不定詞①，動名詞':['不定詞①，動名詞'],'比較':['比較'],'文型':['文型'],'受動態':['受動態'],'現在完了形（完了・経験）':['現在完了形（完了・経験）'],'現在完了形（継続），現在完了進行形':['現在完了形（継続），現在完了進行形'],'不定詞②':['不定詞②'],'分詞と間接疑問文':['分詞と間接疑問文'],'関係代名詞':['関係代名詞'],'仮定法':['仮定法']}\n    }\n  },\n  '数学': {\n    types: ['計算','文章題','変形','グラフ'],\n    stagesByGrade: {'中1':['正負の数','文字式・方程式','比例・反比例'],'中2':['文字式・方程式'],'中3':['文字式・方程式']},\n    stageMap: {'中1':{'正負の数':['正負の数'],'文字式・方程式':['文字式・方程式'],'比例・反比例':['比例・反比例']},'中2':{'文字式・方程式':['文字式・方程式']},'中3':{'文字式・方程式':['文字式・方程式']}}\n  }\n};\n\nlet currentQuestions = [];\nlet showAnswers = false;\nconst STORAGE_KEY = 'mikamijuku_problem_app_v3';\n\nfunction captureState() {\n  return {\n    subject: el('subjectSelect')?.value || '英語',\n    grade: el('gradeSelect')?.value || '中1',\n    textbook: el('textbookSelect')?.value || '',\n    section: el('sectionSelect')?.value || '',\n    questionCount: el('questionCount')?.value || '10',\n    vocabMode: el('vocabMode')?.value || 'on',\n    pickMode: el('pickMode')?.value || 'random',\n    stages: selectedValues('input[data-stage]'),\n    types: selectedValues('input[data-type]'),\n    listSubject: el('listSubject')?.value || '英語',\n    listGrade: el('listGrade')?.value || 'all',\n    listLimit: el('listLimit')?.value || '50',\n    listKeyword: el('listKeyword')?.value || '',\n    listVocabMode: el('listVocabMode')?.value || 'same',\n    currentQuestionIds: currentQuestions.map(q => q.id),\n    showAnswers\n  };\n}\n\nfunction saveState(silent = true) {\n  try {\n    localStorage.setItem(STORAGE_KEY, JSON.stringify(captureState()));\n    if (!silent && el('globalSaveStatus')) {\n      el('globalSaveStatus').textContent = 'この端末に保存しました。';\n    }\n  } catch (e) {\n    if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存に失敗しました。';\n  }\n}\n\nfunction restoreQuestionSetByIds(ids) {\n  const map = new Map(qb.map(q => [q.id, q]));\n  const restored = (ids || []).map(id => map.get(id)).filter(Boolean);\n  if (restored.length) {\n    currentQuestions = restored;\n    el('questionPreview').classList.toggle('showAnswers', showAnswers);\n    el('questionPreview').innerHTML = restored.map((q, i) => `\n      <div class=\"q\">\n        <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n        <div>${q.q}</div>\n        <div class=\"ans\">答え: ${q.a}</div>\n      </div>\n    `).join('');\n    el('builderStatus').textContent = '保存されていた問題セットを復元しました。';\n    el('builderStatus').classList.add('good');\n  }\n}\n\nfunction safeSetValue(id, value) {\n  const node = el(id);\n  if (!node || value == null) return;\n  const has = Array.from(node.options || []).some(op => op.value === value || op.textContent === value);\n  if (has) node.value = value;\n}\n\nfunction setCheckedValues(selector, values) {\n  const set = new Set(values || []);\n  document.querySelectorAll(selector).forEach(ch => {\n    ch.checked = set.size ? set.has(ch.value) : true;\n  });\n}\n\nfunction applyState(state) {\n  if (!state) return;\n  safeSetValue('subjectSelect', state.subject);\n  safeSetValue('gradeSelect', state.grade);\n  renderSectionOptions();\n  renderStageOptions();\n  renderTypeOptions();\n  safeSetValue('textbookSelect', state.textbook);\n  renderSectionOptions();\n  safeSetValue('sectionSelect', state.section);\n  safeSetValue('questionCount', state.questionCount);\n  safeSetValue('vocabMode', state.vocabMode);\n  safeSetValue('pickMode', state.pickMode);\n  setCheckedValues('input[data-stage]', state.stages);\n  setCheckedValues('input[data-type]', state.types);\n  syncListSelectors();\n  safeSetValue('listSubject', state.listSubject);\n  safeSetValue('listGrade', state.listGrade);\n  safeSetValue('listLimit', state.listLimit);\n  safeSetValue('listVocabMode', state.listVocabMode);\n  if (el('listKeyword')) el('listKeyword').value = state.listKeyword || '';\n  showAnswers = !!state.showAnswers;\n  restoreQuestionSetByIds(state.currentQuestionIds || []);\n  updateBuilderStatus(baseFiltered());\n}\n\nfunction exportStateFile() {\n  const blob = new Blob([JSON.stringify(captureState(), null, 2)], {type:'application/json'});\n  const url = URL.createObjectURL(blob);\n  const a = document.createElement('a');\n  a.href = url;\n  a.download = 'mikamijuku_app_state_latest.json';\n  document.body.appendChild(a);\n  a.click();\n  a.remove();\n  setTimeout(() => URL.revokeObjectURL(url), 3000);\n  if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを書き出しました。';\n}\n\nfunction importStateFile(file) {\n  const fr = new FileReader();\n  fr.onload = () => {\n    try {\n      const state = JSON.parse(fr.result);\n      applyState(state);\n      saveState(true);\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを読み込みました。';\n    } catch (e) {\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データの読み込みに失敗しました。';\n    }\n  };\n  fr.readAsText(file, 'utf-8');\n}\n\nconst currentSubject = () => el('subjectSelect').value;\nconst currentGrade = () => el('gradeSelect').value;\nconst currentTextbook = () => el('textbookSelect').value;\nconst currentSectionIndex = () => Math.max(0, el('sectionSelect').selectedIndex);\nconst useVocabGate = () => el('vocabMode').value === 'on';\n\nfunction renderTabs() {\n  document.querySelectorAll('.tab').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.tab').forEach(x => x.classList.remove('active'));\n      document.querySelectorAll('.screen').forEach(x => x.classList.remove('active'));\n      btn.classList.add('active');\n      const target = document.getElementById('screen-' + btn.dataset.screen);\n      if (target) target.classList.add('active');\n    });\n  });\n}\n\nfunction renderTextbookOptions() {\n  el('textbookSelect').innerHTML = meta.textbooks.map(tb => `<option>${tb}</option>`).join('');\n}\n\nfunction renderSectionOptions() {\n  const subject = currentSubject();\n  const grade = currentGrade().replace('中','');\n  if (subject !== '英語') {\n    el('textbookWrap').classList.add('hidden');\n    el('sectionWrap').classList.add('hidden');\n    return;\n  }\n  el('textbookWrap').classList.remove('hidden');\n  el('sectionWrap').classList.remove('hidden');\n  const tb = currentTextbook() || meta.textbooks[0];\n  const sections = ((meta.sections[tb] || {})[grade] || []);\n  el('sectionSelect').innerHTML = sections.map(sec => `<option>${sec}</option>`).join('');\n}\n\nfunction renderStageOptions() {\n  const stages = subjectConfig[currentSubject()].stagesByGrade[currentGrade()] || [];\n  el('stageArea').innerHTML = stages.map(s => `<label class=\"ck\"><input type=\"checkbox\" data-stage value=\"${s}\" checked> <span>${s}</span></label>`).join('');\n}\n\nfunction renderTypeOptions() {\n  const types = subjectConfig[currentSubject()].types;\n  el('typeArea').innerHTML = types.map(t => `<label class=\"ck\"><input type=\"checkbox\" data-type value=\"${t}\" checked> <span>${t}</span></label>`).join('');\n}\n\nfunction selectedValues(selector) {\n  return Array.from(document.querySelectorAll(selector + ':checked')).map(x => x.value);\n}\n\nfunction selectedCategories() {\n  const stages = selectedValues('input[data-stage]');\n  if (!stages.length) return [];\n  const map = subjectConfig[currentSubject()].stageMap[currentGrade()] || {};\n  const set = new Set();\n  stages.forEach(stage => (map[stage] || [stage]).forEach(c => set.add(c)));\n  return Array.from(set);\n}\n\nfunction passesVocab(item, overrideMode) {\n  if (item.subject !== '英語') return true;\n  const mode = overrideMode || (useVocabGate() ? 'on' : 'off');\n  if (mode === 'off') return true;\n  if (item.grade !== currentGrade()) return true;\n  const rec = meta.passMeta[item.id] || {};\n  const minIdx = rec[currentTextbook()];\n  if (meta.vocabCoordinateVersion !== 'v7-2026-08-18-1based') return false;\n  if (minIdx === -2) return true;\n  if (!Number.isInteger(minIdx) || minIdx <= 0) return false;\n  const selectedOrdinal = currentSectionIndex() + 1;\n  return minIdx <= selectedOrdinal;\n}\n\n// ---- Mikami prerequisite grammar + runtime quality gates ----\nfunction selectedStageCeiling() {\n  if (currentSubject() !== '英語') return Number.MAX_SAFE_INTEGER;\n  const stages = subjectConfig['英語'].stagesByGrade[cur"
    },
    {
      "needle": "questionCount",
      "index": 2576551,
      "context": "1,\"reconstructed_pct\":72.73,\"trusted_old\":154,\"trusted_old_pct\":3.93,\"both\":126,\"reconstructed_only\":2725,\"old_only\":28,\"final_resolved\":2879,\"final_resolved_pct\":73.44}}}}}</script>\n<script>\n(() => {\n'use strict';\nconst qb = JSON.parse(document.getElementById('qb-data').textContent);\nconst meta = JSON.parse(document.getElementById('meta-data').textContent);\nconst el = id => document.getElementById(id);\n\nconst subjectConfig = {\n  '英語': {\n    types: ['空所補充','並びかえ','英作文','変形','見分け','答え方','間違い直し','読解','選択','答えから質問文'],\n    stagesByGrade: {\n      '中1': ['英語の語順','be動詞（現在形）','一般動詞（現在形）','be動詞と一般動詞','疑問詞','can','人称代名詞','命令文','一般動詞（３単現）','現在進行形','be動詞（過去形）','一般動詞（過去形）','過去進行形','there is ～，look ～ の文','未来の文','接続詞'],\n      '中2': ['英語の語順','be動詞（現在形）','一般動詞（現在形）','be動詞と一般動詞（現在形）','be動詞と一般動詞（過去形）','疑問詞','人称代名詞','命令文','進行形','there is ～ の文','未来の文','助動詞','接続詞','不定詞①','動名詞','文型①（look ～，give A B）','比較','文型②（call A B，make A B）','受動態','現在完了形（完了・経験）','現在完了形（継続），現在完了進行形','不定詞②'],\n      '中3': ['英語の語順','be動詞と一般動詞（現在形）','be動詞と一般動詞（過去形）','疑問詞','人称代名詞','進行形','命令文，there is ～ の文','未来の文','助動詞','接続詞','不定詞①，動名詞','比較','文型','受動態','現在完了形（完了・経験）','現在完了形（継続），現在完了進行形','不定詞②','分詞と間接疑問文','関係代名詞','仮定法']\n    },\n    stageMap: {\n      '中1': {'英語の語順':['並びかえ','英作文'],'be動詞（現在形）':['be動詞'],'一般動詞（現在形）':['一般動詞'],'be動詞と一般動詞':['区別'],'疑問詞':['疑問詞'],'can':['can'],'人称代名詞':['人称代名詞'],'命令文':['命令文'],'一般動詞（３単現）':['三単現'],'現在進行形':['現在進行形'],'be動詞（過去形）':['過去のbe動詞'],'一般動詞（過去形）':['一般動詞の過去形','不規則動詞','過去を表す語'],'過去進行形':['過去進行形'],'there is ～，look ～ の文':['there is ～・look ～ の文'],'未来の文':['未来の文'],'接続詞':['接続詞']},\n      '中2': {'英語の語順':['並びかえ','英作文'],'be動詞（現在形）':['be動詞'],'一般動詞（現在形）':['一般動詞'],'be動詞と一般動詞（現在形）':['be動詞と一般動詞（現在形）'],'be動詞と一般動詞（過去形）':['be動詞と一般動詞（過去形）'],'疑問詞':['疑問詞'],'人称代名詞':['人称代名詞'],'命令文':['命令文'],'進行形':['進行形'],'there is ～ の文':['there is ～ の文'],'未来の文':['未来表現','未来の文'],'助動詞':['助動詞'],'接続詞':['接続詞'],'不定詞①':['不定詞①','不定詞'],'動名詞':['動名詞'],'文型①（look ～，give A B）':['文型①（look ～，give A B）'],'比較':['比較'],'文型②（call A B，make A B）':['文型②（call A B，make A B）'],'受動態':['受動態'],'現在完了形（完了・経験）':['現在完了形（完了・経験）'],'現在完了形（継続），現在完了進行形':['現在完了形（継続），現在完了進行形'],'不定詞②':['不定詞②']},\n      '中3': {'英語の語順':['英語の語順'],'be動詞と一般動詞（現在形）':['be動詞と一般動詞（現在形）'],'be動詞と一般動詞（過去形）':['be動詞と一般動詞（過去形）'],'疑問詞':['疑問詞'],'人称代名詞':['人称代名詞'],'進行形':['進行形'],'命令文，there is ～ の文':['命令文，there is ～ の文'],'未来の文':['未来の文'],'助動詞':['助動詞'],'接続詞':['接続詞'],'不定詞①，動名詞':['不定詞①，動名詞'],'比較':['比較'],'文型':['文型'],'受動態':['受動態'],'現在完了形（完了・経験）':['現在完了形（完了・経験）'],'現在完了形（継続），現在完了進行形':['現在完了形（継続），現在完了進行形'],'不定詞②':['不定詞②'],'分詞と間接疑問文':['分詞と間接疑問文'],'関係代名詞':['関係代名詞'],'仮定法':['仮定法']}\n    }\n  },\n  '数学': {\n    types: ['計算','文章題','変形','グラフ'],\n    stagesByGrade: {'中1':['正負の数','文字式・方程式','比例・反比例'],'中2':['文字式・方程式'],'中3':['文字式・方程式']},\n    stageMap: {'中1':{'正負の数':['正負の数'],'文字式・方程式':['文字式・方程式'],'比例・反比例':['比例・反比例']},'中2':{'文字式・方程式':['文字式・方程式']},'中3':{'文字式・方程式':['文字式・方程式']}}\n  }\n};\n\nlet currentQuestions = [];\nlet showAnswers = false;\nconst STORAGE_KEY = 'mikamijuku_problem_app_v3';\n\nfunction captureState() {\n  return {\n    subject: el('subjectSelect')?.value || '英語',\n    grade: el('gradeSelect')?.value || '中1',\n    textbook: el('textbookSelect')?.value || '',\n    section: el('sectionSelect')?.value || '',\n    questionCount: el('questionCount')?.value || '10',\n    vocabMode: el('vocabMode')?.value || 'on',\n    pickMode: el('pickMode')?.value || 'random',\n    stages: selectedValues('input[data-stage]'),\n    types: selectedValues('input[data-type]'),\n    listSubject: el('listSubject')?.value || '英語',\n    listGrade: el('listGrade')?.value || 'all',\n    listLimit: el('listLimit')?.value || '50',\n    listKeyword: el('listKeyword')?.value || '',\n    listVocabMode: el('listVocabMode')?.value || 'same',\n    currentQuestionIds: currentQuestions.map(q => q.id),\n    showAnswers\n  };\n}\n\nfunction saveState(silent = true) {\n  try {\n    localStorage.setItem(STORAGE_KEY, JSON.stringify(captureState()));\n    if (!silent && el('globalSaveStatus')) {\n      el('globalSaveStatus').textContent = 'この端末に保存しました。';\n    }\n  } catch (e) {\n    if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存に失敗しました。';\n  }\n}\n\nfunction restoreQuestionSetByIds(ids) {\n  const map = new Map(qb.map(q => [q.id, q]));\n  const restored = (ids || []).map(id => map.get(id)).filter(Boolean);\n  if (restored.length) {\n    currentQuestions = restored;\n    el('questionPreview').classList.toggle('showAnswers', showAnswers);\n    el('questionPreview').innerHTML = restored.map((q, i) => `\n      <div class=\"q\">\n        <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n        <div>${q.q}</div>\n        <div class=\"ans\">答え: ${q.a}</div>\n      </div>\n    `).join('');\n    el('builderStatus').textContent = '保存されていた問題セットを復元しました。';\n    el('builderStatus').classList.add('good');\n  }\n}\n\nfunction safeSetValue(id, value) {\n  const node = el(id);\n  if (!node || value == null) return;\n  const has = Array.from(node.options || []).some(op => op.value === value || op.textContent === value);\n  if (has) node.value = value;\n}\n\nfunction setCheckedValues(selector, values) {\n  const set = new Set(values || []);\n  document.querySelectorAll(selector).forEach(ch => {\n    ch.checked = set.size ? set.has(ch.value) : true;\n  });\n}\n\nfunction applyState(state) {\n  if (!state) return;\n  safeSetValue('subjectSelect', state.subject);\n  safeSetValue('gradeSelect', state.grade);\n  renderSectionOptions();\n  renderStageOptions();\n  renderTypeOptions();\n  safeSetValue('textbookSelect', state.textbook);\n  renderSectionOptions();\n  safeSetValue('sectionSelect', state.section);\n  safeSetValue('questionCount', state.questionCount);\n  safeSetValue('vocabMode', state.vocabMode);\n  safeSetValue('pickMode', state.pickMode);\n  setCheckedValues('input[data-stage]', state.stages);\n  setCheckedValues('input[data-type]', state.types);\n  syncListSelectors();\n  safeSetValue('listSubject', state.listSubject);\n  safeSetValue('listGrade', state.listGrade);\n  safeSetValue('listLimit', state.listLimit);\n  safeSetValue('listVocabMode', state.listVocabMode);\n  if (el('listKeyword')) el('listKeyword').value = state.listKeyword || '';\n  showAnswers = !!state.showAnswers;\n  restoreQuestionSetByIds(state.currentQuestionIds || []);\n  updateBuilderStatus(baseFiltered());\n}\n\nfunction exportStateFile() {\n  const blob = new Blob([JSON.stringify(captureState(), null, 2)], {type:'application/json'});\n  const url = URL.createObjectURL(blob);\n  const a = document.createElement('a');\n  a.href = url;\n  a.download = 'mikamijuku_app_state_latest.json';\n  document.body.appendChild(a);\n  a.click();\n  a.remove();\n  setTimeout(() => URL.revokeObjectURL(url), 3000);\n  if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを書き出しました。';\n}\n\nfunction importStateFile(file) {\n  const fr = new FileReader();\n  fr.onload = () => {\n    try {\n      const state = JSON.parse(fr.result);\n      applyState(state);\n      saveState(true);\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを読み込みました。';\n    } catch (e) {\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データの読み込みに失敗しました。';\n    }\n  };\n  fr.readAsText(file, 'utf-8');\n}\n\nconst currentSubject = () => el('subjectSelect').value;\nconst currentGrade = () => el('gradeSelect').value;\nconst currentTextbook = () => el('textbookSelect').value;\nconst currentSectionIndex = () => Math.max(0, el('sectionSelect').selectedIndex);\nconst useVocabGate = () => el('vocabMode').value === 'on';\n\nfunction renderTabs() {\n  document.querySelectorAll('.tab').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.tab').forEach(x => x.classList.remove('active'));\n      document.querySelectorAll('.screen').forEach(x => x.classList.remove('active'));\n      btn.classList.add('active');\n      const target = document.getElementById('screen-' + btn.dataset.screen);\n      if (target) target.classList.add('active');\n    });\n  });\n}\n\nfunction renderTextbookOptions() {\n  el('textbookSelect').innerHTML = meta.textbooks.map(tb => `<option>${tb}</option>`).join('');\n}\n\nfunction renderSectionOptions() {\n  const subject = currentSubject();\n  const grade = currentGrade().replace('中','');\n  if (subject !== '英語') {\n    el('textbookWrap').classList.add('hidden');\n    el('sectionWrap').classList.add('hidden');\n    return;\n  }\n  el('textbookWrap').classList.remove('hidden');\n  el('sectionWrap').classList.remove('hidden');\n  const tb = currentTextbook() || meta.textbooks[0];\n  const sections = ((meta.sections[tb] || {})[grade] || []);\n  el('sectionSelect').innerHTML = sections.map(sec => `<option>${sec}</option>`).join('');\n}\n\nfunction renderStageOptions() {\n  const stages = subjectConfig[currentSubject()].stagesByGrade[currentGrade()] || [];\n  el('stageArea').innerHTML = stages.map(s => `<label class=\"ck\"><input type=\"checkbox\" data-stage value=\"${s}\" checked> <span>${s}</span></label>`).join('');\n}\n\nfunction renderTypeOptions() {\n  const types = subjectConfig[currentSubject()].types;\n  el('typeArea').innerHTML = types.map(t => `<label class=\"ck\"><input type=\"checkbox\" data-type value=\"${t}\" checked> <span>${t}</span></label>`).join('');\n}\n\nfunction selectedValues(selector) {\n  return Array.from(document.querySelectorAll(selector + ':checked')).map(x => x.value);\n}\n\nfunction selectedCategories() {\n  const stages = selectedValues('input[data-stage]');\n  if (!stages.length) return [];\n  const map = subjectConfig[currentSubject()].stageMap[currentGrade()] || {};\n  const set = new Set();\n  stages.forEach(stage => (map[stage] || [stage]).forEach(c => set.add(c)));\n  return Array.from(set);\n}\n\nfunction passesVocab(item, overrideMode) {\n  if (item.subject !== '英語') return true;\n  const mode = overrideMode || (useVocabGate() ? 'on' : 'off');\n  if (mode === 'off') return true;\n  if (item.grade !== currentGrade()) return true;\n  const rec = meta.passMeta[item.id] || {};\n  const minIdx = rec[currentTextbook()];\n  if (meta.vocabCoordinateVersion !== 'v7-2026-08-18-1based') return false;\n  if (minIdx === -2) return true;\n  if (!Number.isInteger(minIdx) || minIdx <= 0) return false;\n  const selectedOrdinal = currentSectionIndex() + 1;\n  return minIdx <= selectedOrdinal;\n}\n\n// ---- Mikami prerequisite grammar + runtime quality gates ----\nfunction selectedStageCeiling() {\n  if (currentSubject() !== '英語') return Number.MAX_SAFE_INTEGER;\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];"
    },
    {
      "needle": "questionCount",
      "index": 2578917,
      "context": "詞'],'比較':['比較'],'文型':['文型'],'受動態':['受動態'],'現在完了形（完了・経験）':['現在完了形（完了・経験）'],'現在完了形（継続），現在完了進行形':['現在完了形（継続），現在完了進行形'],'不定詞②':['不定詞②'],'分詞と間接疑問文':['分詞と間接疑問文'],'関係代名詞':['関係代名詞'],'仮定法':['仮定法']}\n    }\n  },\n  '数学': {\n    types: ['計算','文章題','変形','グラフ'],\n    stagesByGrade: {'中1':['正負の数','文字式・方程式','比例・反比例'],'中2':['文字式・方程式'],'中3':['文字式・方程式']},\n    stageMap: {'中1':{'正負の数':['正負の数'],'文字式・方程式':['文字式・方程式'],'比例・反比例':['比例・反比例']},'中2':{'文字式・方程式':['文字式・方程式']},'中3':{'文字式・方程式':['文字式・方程式']}}\n  }\n};\n\nlet currentQuestions = [];\nlet showAnswers = false;\nconst STORAGE_KEY = 'mikamijuku_problem_app_v3';\n\nfunction captureState() {\n  return {\n    subject: el('subjectSelect')?.value || '英語',\n    grade: el('gradeSelect')?.value || '中1',\n    textbook: el('textbookSelect')?.value || '',\n    section: el('sectionSelect')?.value || '',\n    questionCount: el('questionCount')?.value || '10',\n    vocabMode: el('vocabMode')?.value || 'on',\n    pickMode: el('pickMode')?.value || 'random',\n    stages: selectedValues('input[data-stage]'),\n    types: selectedValues('input[data-type]'),\n    listSubject: el('listSubject')?.value || '英語',\n    listGrade: el('listGrade')?.value || 'all',\n    listLimit: el('listLimit')?.value || '50',\n    listKeyword: el('listKeyword')?.value || '',\n    listVocabMode: el('listVocabMode')?.value || 'same',\n    currentQuestionIds: currentQuestions.map(q => q.id),\n    showAnswers\n  };\n}\n\nfunction saveState(silent = true) {\n  try {\n    localStorage.setItem(STORAGE_KEY, JSON.stringify(captureState()));\n    if (!silent && el('globalSaveStatus')) {\n      el('globalSaveStatus').textContent = 'この端末に保存しました。';\n    }\n  } catch (e) {\n    if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存に失敗しました。';\n  }\n}\n\nfunction restoreQuestionSetByIds(ids) {\n  const map = new Map(qb.map(q => [q.id, q]));\n  const restored = (ids || []).map(id => map.get(id)).filter(Boolean);\n  if (restored.length) {\n    currentQuestions = restored;\n    el('questionPreview').classList.toggle('showAnswers', showAnswers);\n    el('questionPreview').innerHTML = restored.map((q, i) => `\n      <div class=\"q\">\n        <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n        <div>${q.q}</div>\n        <div class=\"ans\">答え: ${q.a}</div>\n      </div>\n    `).join('');\n    el('builderStatus').textContent = '保存されていた問題セットを復元しました。';\n    el('builderStatus').classList.add('good');\n  }\n}\n\nfunction safeSetValue(id, value) {\n  const node = el(id);\n  if (!node || value == null) return;\n  const has = Array.from(node.options || []).some(op => op.value === value || op.textContent === value);\n  if (has) node.value = value;\n}\n\nfunction setCheckedValues(selector, values) {\n  const set = new Set(values || []);\n  document.querySelectorAll(selector).forEach(ch => {\n    ch.checked = set.size ? set.has(ch.value) : true;\n  });\n}\n\nfunction applyState(state) {\n  if (!state) return;\n  safeSetValue('subjectSelect', state.subject);\n  safeSetValue('gradeSelect', state.grade);\n  renderSectionOptions();\n  renderStageOptions();\n  renderTypeOptions();\n  safeSetValue('textbookSelect', state.textbook);\n  renderSectionOptions();\n  safeSetValue('sectionSelect', state.section);\n  safeSetValue('questionCount', state.questionCount);\n  safeSetValue('vocabMode', state.vocabMode);\n  safeSetValue('pickMode', state.pickMode);\n  setCheckedValues('input[data-stage]', state.stages);\n  setCheckedValues('input[data-type]', state.types);\n  syncListSelectors();\n  safeSetValue('listSubject', state.listSubject);\n  safeSetValue('listGrade', state.listGrade);\n  safeSetValue('listLimit', state.listLimit);\n  safeSetValue('listVocabMode', state.listVocabMode);\n  if (el('listKeyword')) el('listKeyword').value = state.listKeyword || '';\n  showAnswers = !!state.showAnswers;\n  restoreQuestionSetByIds(state.currentQuestionIds || []);\n  updateBuilderStatus(baseFiltered());\n}\n\nfunction exportStateFile() {\n  const blob = new Blob([JSON.stringify(captureState(), null, 2)], {type:'application/json'});\n  const url = URL.createObjectURL(blob);\n  const a = document.createElement('a');\n  a.href = url;\n  a.download = 'mikamijuku_app_state_latest.json';\n  document.body.appendChild(a);\n  a.click();\n  a.remove();\n  setTimeout(() => URL.revokeObjectURL(url), 3000);\n  if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを書き出しました。';\n}\n\nfunction importStateFile(file) {\n  const fr = new FileReader();\n  fr.onload = () => {\n    try {\n      const state = JSON.parse(fr.result);\n      applyState(state);\n      saveState(true);\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを読み込みました。';\n    } catch (e) {\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データの読み込みに失敗しました。';\n    }\n  };\n  fr.readAsText(file, 'utf-8');\n}\n\nconst currentSubject = () => el('subjectSelect').value;\nconst currentGrade = () => el('gradeSelect').value;\nconst currentTextbook = () => el('textbookSelect').value;\nconst currentSectionIndex = () => Math.max(0, el('sectionSelect').selectedIndex);\nconst useVocabGate = () => el('vocabMode').value === 'on';\n\nfunction renderTabs() {\n  document.querySelectorAll('.tab').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.tab').forEach(x => x.classList.remove('active'));\n      document.querySelectorAll('.screen').forEach(x => x.classList.remove('active'));\n      btn.classList.add('active');\n      const target = document.getElementById('screen-' + btn.dataset.screen);\n      if (target) target.classList.add('active');\n    });\n  });\n}\n\nfunction renderTextbookOptions() {\n  el('textbookSelect').innerHTML = meta.textbooks.map(tb => `<option>${tb}</option>`).join('');\n}\n\nfunction renderSectionOptions() {\n  const subject = currentSubject();\n  const grade = currentGrade().replace('中','');\n  if (subject !== '英語') {\n    el('textbookWrap').classList.add('hidden');\n    el('sectionWrap').classList.add('hidden');\n    return;\n  }\n  el('textbookWrap').classList.remove('hidden');\n  el('sectionWrap').classList.remove('hidden');\n  const tb = currentTextbook() || meta.textbooks[0];\n  const sections = ((meta.sections[tb] || {})[grade] || []);\n  el('sectionSelect').innerHTML = sections.map(sec => `<option>${sec}</option>`).join('');\n}\n\nfunction renderStageOptions() {\n  const stages = subjectConfig[currentSubject()].stagesByGrade[currentGrade()] || [];\n  el('stageArea').innerHTML = stages.map(s => `<label class=\"ck\"><input type=\"checkbox\" data-stage value=\"${s}\" checked> <span>${s}</span></label>`).join('');\n}\n\nfunction renderTypeOptions() {\n  const types = subjectConfig[currentSubject()].types;\n  el('typeArea').innerHTML = types.map(t => `<label class=\"ck\"><input type=\"checkbox\" data-type value=\"${t}\" checked> <span>${t}</span></label>`).join('');\n}\n\nfunction selectedValues(selector) {\n  return Array.from(document.querySelectorAll(selector + ':checked')).map(x => x.value);\n}\n\nfunction selectedCategories() {\n  const stages = selectedValues('input[data-stage]');\n  if (!stages.length) return [];\n  const map = subjectConfig[currentSubject()].stageMap[currentGrade()] || {};\n  const set = new Set();\n  stages.forEach(stage => (map[stage] || [stage]).forEach(c => set.add(c)));\n  return Array.from(set);\n}\n\nfunction passesVocab(item, overrideMode) {\n  if (item.subject !== '英語') return true;\n  const mode = overrideMode || (useVocabGate() ? 'on' : 'off');\n  if (mode === 'off') return true;\n  if (item.grade !== currentGrade()) return true;\n  const rec = meta.passMeta[item.id] || {};\n  const minIdx = rec[currentTextbook()];\n  if (meta.vocabCoordinateVersion !== 'v7-2026-08-18-1based') return false;\n  if (minIdx === -2) return true;\n  if (!Number.isInteger(minIdx) || minIdx <= 0) return false;\n  const selectedOrdinal = currentSectionIndex() + 1;\n  return minIdx <= selectedOrdinal;\n}\n\n// ---- Mikami prerequisite grammar + runtime quality gates ----\nfunction selectedStageCeiling() {\n  if (currentSubject() !== '英語') return Number.MAX_SAFE_INTEGER;\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  const selected = selectedValues('input[data-stage]');\n  if (!selected.length) return -1;\n  return selected.reduce((max, s) => Math.max(max, stages.indexOf(s)), -1);\n}\n\nfunction stageIndexByNameContains(...needles) {\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  for (let i = 0; i < stages.length; i++) {\n    const s = stages[i];\n    if (needles.some(n => s.includes(n))) return i;\n  }\n  return -1;\n}\n\nfunction targetStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const cfg = subjectConfig['英語'];\n  const stages = cfg.stagesByGrade[item.grade] || [];\n  const map = cfg.stageMap[item.grade] || {};\n  for (let i = 0; i < stages.length; i++) {\n    const cats = map[stages[i]] || [stages[i]];\n    if (cats.includes(item.category)) return i;\n  }\n  return -1;\n}\n\nfunction requiredGrammarStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const text = String(item.q || '') + ' ' + String(item.a || '');\n  let req = Math.max(0, targetStageIndex(item));\n  const bump = i => { if (i >= 0) req = Math.max(req, i); };\n\n  // Detect only structures that can make an otherwise earlier problem depend on a later grammar unit.\n  if (/\\b(will|won't)\\b|\\b(am|is|are) going to\\b/i.test(text)) bump(stageIndexByNameContains('未来'));\n  if (/\\b(was|were)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('過去進行'));\n  if (/\\b(am|is|are)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('進行形'));\n  if (/\\b(can|must|may|should)\\b/i.test(text)) bump(stageIndexByNameContains('助動詞','can'));\n  if (/\\bto\\s+(?:be|have|do|play|study|read|write|watch|visit|see|eat|drink|go|come|make|use|help|learn|practice|speak|listen|buy|call|join|take|work|live|want|need|try|start|finish)\\b/i.test(text)) bump(stageIndexByNameContains('不定詞①','不定詞②','不定詞'));\n  if (/\\b(?:enjoy|enjoys|enjoyed|finish|finishes|finished|stop|stops|stopped)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('動名詞','不定詞①，動名詞'));\n  if (/\\b(?:than|as\\s+\\w+\\s+as|more\\s+\\w+\\s+than|most\\s+\\w+)\\b/i.test(text)) bump(stageIndexByNameContains('比較'));\n  if (/\\b(am|is|are|was|were)\\s+(?:made|used|written|spoken|known|called|played|studied|visited|built|seen|loved)\\b/i.test(text)) bump(stageIndexByNameContains('受動態'));\n  if (/\\b(?:have|has)\\s+(?:ever\\s+|never\\s+|already\\s+|just\\s+)?(?:been|visited|finished|lost|lived|studied|seen|done|written|read"
    },
    {
      "needle": "questionCount",
      "index": 2578939,
      "context": "'文型'],'受動態':['受動態'],'現在完了形（完了・経験）':['現在完了形（完了・経験）'],'現在完了形（継続），現在完了進行形':['現在完了形（継続），現在完了進行形'],'不定詞②':['不定詞②'],'分詞と間接疑問文':['分詞と間接疑問文'],'関係代名詞':['関係代名詞'],'仮定法':['仮定法']}\n    }\n  },\n  '数学': {\n    types: ['計算','文章題','変形','グラフ'],\n    stagesByGrade: {'中1':['正負の数','文字式・方程式','比例・反比例'],'中2':['文字式・方程式'],'中3':['文字式・方程式']},\n    stageMap: {'中1':{'正負の数':['正負の数'],'文字式・方程式':['文字式・方程式'],'比例・反比例':['比例・反比例']},'中2':{'文字式・方程式':['文字式・方程式']},'中3':{'文字式・方程式':['文字式・方程式']}}\n  }\n};\n\nlet currentQuestions = [];\nlet showAnswers = false;\nconst STORAGE_KEY = 'mikamijuku_problem_app_v3';\n\nfunction captureState() {\n  return {\n    subject: el('subjectSelect')?.value || '英語',\n    grade: el('gradeSelect')?.value || '中1',\n    textbook: el('textbookSelect')?.value || '',\n    section: el('sectionSelect')?.value || '',\n    questionCount: el('questionCount')?.value || '10',\n    vocabMode: el('vocabMode')?.value || 'on',\n    pickMode: el('pickMode')?.value || 'random',\n    stages: selectedValues('input[data-stage]'),\n    types: selectedValues('input[data-type]'),\n    listSubject: el('listSubject')?.value || '英語',\n    listGrade: el('listGrade')?.value || 'all',\n    listLimit: el('listLimit')?.value || '50',\n    listKeyword: el('listKeyword')?.value || '',\n    listVocabMode: el('listVocabMode')?.value || 'same',\n    currentQuestionIds: currentQuestions.map(q => q.id),\n    showAnswers\n  };\n}\n\nfunction saveState(silent = true) {\n  try {\n    localStorage.setItem(STORAGE_KEY, JSON.stringify(captureState()));\n    if (!silent && el('globalSaveStatus')) {\n      el('globalSaveStatus').textContent = 'この端末に保存しました。';\n    }\n  } catch (e) {\n    if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存に失敗しました。';\n  }\n}\n\nfunction restoreQuestionSetByIds(ids) {\n  const map = new Map(qb.map(q => [q.id, q]));\n  const restored = (ids || []).map(id => map.get(id)).filter(Boolean);\n  if (restored.length) {\n    currentQuestions = restored;\n    el('questionPreview').classList.toggle('showAnswers', showAnswers);\n    el('questionPreview').innerHTML = restored.map((q, i) => `\n      <div class=\"q\">\n        <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n        <div>${q.q}</div>\n        <div class=\"ans\">答え: ${q.a}</div>\n      </div>\n    `).join('');\n    el('builderStatus').textContent = '保存されていた問題セットを復元しました。';\n    el('builderStatus').classList.add('good');\n  }\n}\n\nfunction safeSetValue(id, value) {\n  const node = el(id);\n  if (!node || value == null) return;\n  const has = Array.from(node.options || []).some(op => op.value === value || op.textContent === value);\n  if (has) node.value = value;\n}\n\nfunction setCheckedValues(selector, values) {\n  const set = new Set(values || []);\n  document.querySelectorAll(selector).forEach(ch => {\n    ch.checked = set.size ? set.has(ch.value) : true;\n  });\n}\n\nfunction applyState(state) {\n  if (!state) return;\n  safeSetValue('subjectSelect', state.subject);\n  safeSetValue('gradeSelect', state.grade);\n  renderSectionOptions();\n  renderStageOptions();\n  renderTypeOptions();\n  safeSetValue('textbookSelect', state.textbook);\n  renderSectionOptions();\n  safeSetValue('sectionSelect', state.section);\n  safeSetValue('questionCount', state.questionCount);\n  safeSetValue('vocabMode', state.vocabMode);\n  safeSetValue('pickMode', state.pickMode);\n  setCheckedValues('input[data-stage]', state.stages);\n  setCheckedValues('input[data-type]', state.types);\n  syncListSelectors();\n  safeSetValue('listSubject', state.listSubject);\n  safeSetValue('listGrade', state.listGrade);\n  safeSetValue('listLimit', state.listLimit);\n  safeSetValue('listVocabMode', state.listVocabMode);\n  if (el('listKeyword')) el('listKeyword').value = state.listKeyword || '';\n  showAnswers = !!state.showAnswers;\n  restoreQuestionSetByIds(state.currentQuestionIds || []);\n  updateBuilderStatus(baseFiltered());\n}\n\nfunction exportStateFile() {\n  const blob = new Blob([JSON.stringify(captureState(), null, 2)], {type:'application/json'});\n  const url = URL.createObjectURL(blob);\n  const a = document.createElement('a');\n  a.href = url;\n  a.download = 'mikamijuku_app_state_latest.json';\n  document.body.appendChild(a);\n  a.click();\n  a.remove();\n  setTimeout(() => URL.revokeObjectURL(url), 3000);\n  if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを書き出しました。';\n}\n\nfunction importStateFile(file) {\n  const fr = new FileReader();\n  fr.onload = () => {\n    try {\n      const state = JSON.parse(fr.result);\n      applyState(state);\n      saveState(true);\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを読み込みました。';\n    } catch (e) {\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データの読み込みに失敗しました。';\n    }\n  };\n  fr.readAsText(file, 'utf-8');\n}\n\nconst currentSubject = () => el('subjectSelect').value;\nconst currentGrade = () => el('gradeSelect').value;\nconst currentTextbook = () => el('textbookSelect').value;\nconst currentSectionIndex = () => Math.max(0, el('sectionSelect').selectedIndex);\nconst useVocabGate = () => el('vocabMode').value === 'on';\n\nfunction renderTabs() {\n  document.querySelectorAll('.tab').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.tab').forEach(x => x.classList.remove('active'));\n      document.querySelectorAll('.screen').forEach(x => x.classList.remove('active'));\n      btn.classList.add('active');\n      const target = document.getElementById('screen-' + btn.dataset.screen);\n      if (target) target.classList.add('active');\n    });\n  });\n}\n\nfunction renderTextbookOptions() {\n  el('textbookSelect').innerHTML = meta.textbooks.map(tb => `<option>${tb}</option>`).join('');\n}\n\nfunction renderSectionOptions() {\n  const subject = currentSubject();\n  const grade = currentGrade().replace('中','');\n  if (subject !== '英語') {\n    el('textbookWrap').classList.add('hidden');\n    el('sectionWrap').classList.add('hidden');\n    return;\n  }\n  el('textbookWrap').classList.remove('hidden');\n  el('sectionWrap').classList.remove('hidden');\n  const tb = currentTextbook() || meta.textbooks[0];\n  const sections = ((meta.sections[tb] || {})[grade] || []);\n  el('sectionSelect').innerHTML = sections.map(sec => `<option>${sec}</option>`).join('');\n}\n\nfunction renderStageOptions() {\n  const stages = subjectConfig[currentSubject()].stagesByGrade[currentGrade()] || [];\n  el('stageArea').innerHTML = stages.map(s => `<label class=\"ck\"><input type=\"checkbox\" data-stage value=\"${s}\" checked> <span>${s}</span></label>`).join('');\n}\n\nfunction renderTypeOptions() {\n  const types = subjectConfig[currentSubject()].types;\n  el('typeArea').innerHTML = types.map(t => `<label class=\"ck\"><input type=\"checkbox\" data-type value=\"${t}\" checked> <span>${t}</span></label>`).join('');\n}\n\nfunction selectedValues(selector) {\n  return Array.from(document.querySelectorAll(selector + ':checked')).map(x => x.value);\n}\n\nfunction selectedCategories() {\n  const stages = selectedValues('input[data-stage]');\n  if (!stages.length) return [];\n  const map = subjectConfig[currentSubject()].stageMap[currentGrade()] || {};\n  const set = new Set();\n  stages.forEach(stage => (map[stage] || [stage]).forEach(c => set.add(c)));\n  return Array.from(set);\n}\n\nfunction passesVocab(item, overrideMode) {\n  if (item.subject !== '英語') return true;\n  const mode = overrideMode || (useVocabGate() ? 'on' : 'off');\n  if (mode === 'off') return true;\n  if (item.grade !== currentGrade()) return true;\n  const rec = meta.passMeta[item.id] || {};\n  const minIdx = rec[currentTextbook()];\n  if (meta.vocabCoordinateVersion !== 'v7-2026-08-18-1based') return false;\n  if (minIdx === -2) return true;\n  if (!Number.isInteger(minIdx) || minIdx <= 0) return false;\n  const selectedOrdinal = currentSectionIndex() + 1;\n  return minIdx <= selectedOrdinal;\n}\n\n// ---- Mikami prerequisite grammar + runtime quality gates ----\nfunction selectedStageCeiling() {\n  if (currentSubject() !== '英語') return Number.MAX_SAFE_INTEGER;\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  const selected = selectedValues('input[data-stage]');\n  if (!selected.length) return -1;\n  return selected.reduce((max, s) => Math.max(max, stages.indexOf(s)), -1);\n}\n\nfunction stageIndexByNameContains(...needles) {\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  for (let i = 0; i < stages.length; i++) {\n    const s = stages[i];\n    if (needles.some(n => s.includes(n))) return i;\n  }\n  return -1;\n}\n\nfunction targetStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const cfg = subjectConfig['英語'];\n  const stages = cfg.stagesByGrade[item.grade] || [];\n  const map = cfg.stageMap[item.grade] || {};\n  for (let i = 0; i < stages.length; i++) {\n    const cats = map[stages[i]] || [stages[i]];\n    if (cats.includes(item.category)) return i;\n  }\n  return -1;\n}\n\nfunction requiredGrammarStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const text = String(item.q || '') + ' ' + String(item.a || '');\n  let req = Math.max(0, targetStageIndex(item));\n  const bump = i => { if (i >= 0) req = Math.max(req, i); };\n\n  // Detect only structures that can make an otherwise earlier problem depend on a later grammar unit.\n  if (/\\b(will|won't)\\b|\\b(am|is|are) going to\\b/i.test(text)) bump(stageIndexByNameContains('未来'));\n  if (/\\b(was|were)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('過去進行'));\n  if (/\\b(am|is|are)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('進行形'));\n  if (/\\b(can|must|may|should)\\b/i.test(text)) bump(stageIndexByNameContains('助動詞','can'));\n  if (/\\bto\\s+(?:be|have|do|play|study|read|write|watch|visit|see|eat|drink|go|come|make|use|help|learn|practice|speak|listen|buy|call|join|take|work|live|want|need|try|start|finish)\\b/i.test(text)) bump(stageIndexByNameContains('不定詞①','不定詞②','不定詞'));\n  if (/\\b(?:enjoy|enjoys|enjoyed|finish|finishes|finished|stop|stops|stopped)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('動名詞','不定詞①，動名詞'));\n  if (/\\b(?:than|as\\s+\\w+\\s+as|more\\s+\\w+\\s+than|most\\s+\\w+)\\b/i.test(text)) bump(stageIndexByNameContains('比較'));\n  if (/\\b(am|is|are|was|were)\\s+(?:made|used|written|spoken|known|called|played|studied|visited|built|seen|loved)\\b/i.test(text)) bump(stageIndexByNameContains('受動態'));\n  if (/\\b(?:have|has)\\s+(?:ever\\s+|never\\s+|already\\s+|just\\s+)?(?:been|visited|finished|lost|lived|studied|seen|done|written|read|known|used|played|gon"
    },
    {
      "needle": "questionCount",
      "index": 2590612,
      "context": "both)) return false;\n  if (/Do you have (?:visited|been|finished|lost|lived|studied|seen|done)/i.test(both)) return false;\n  if (/Did You ne\\b|Do You\\b|Do They\\b|Does She\\b|Does He\\b/.test(both)) return false;\n  if (/否定文または疑問文/.test(q)) return false;\n  if (/日本語で説明しなさい/.test(q) && /^[A-Za-z]/.test(a)) return false;\n  if (/\\(\\s*she\\s*\\/\\s*her\\s*\\/\\s*her\\s*\\)/i.test(q)) return false;\n  if (/\\(\\s*he\\s*\\/\\s*him\\s*\\/\\s*him\\s*\\)/i.test(q)) return false;\n  if (String(item.id || '').startsWith('M2-INF2-')) {\n    if (item.type === '空所補充' && /^to$/i.test(a) && /\\bto\\s*\\(\\s*\\)\\s*(?=[A-Za-z])/i.test(q)) return false;\n    if (/^(?:He|She|Yuki|Mika|Takumi|Ken|Emi|Tom|The student|The teacher|This boy|This girl|My mother|My father|My brother|My sister|My friend) (?:want|need|like) to\\b/i.test(a)) return false;\n  }\n\n  // Target-grammar evidence: do not let a mislabeled vocabulary-only item through.\n  if (item.category === '動名詞' && !/[A-Za-z]+ing\\b/.test(both)) return false;\n  if (item.category === '比較' && !/\\bthan\\b|\\bas\\s+\\w+\\s+as\\b|\\bmost\\b|\\w+est\\b/i.test(both)) return false;\n  const infinitiveBlank = item.type === '空所補充' && /^to$/i.test(a) && /\\(\\s*\\)\\s*[A-Za-z]+\\b/.test(q);\n  if ((item.category === '不定詞' || item.category === '不定詞①' || item.category === '不定詞②') && !/\\bto\\s+[A-Za-z]+\\b/i.test(both) && !infinitiveBlank) return false;\n  if (String(item.category).startsWith('現在完了形') && !/\\b(?:have|has)\\b/i.test(both)) return false;\n  if (item.category === '関係代名詞') {\n    if (!/\\b(?:who|which|that)\\b/i.test(both)) return false;\n    if (item.type === '空所補充' && /\\(\\s*\\)/.test(q)) {\n      if (!/who\\s*\\/\\s*which/i.test(q)) return false;\n      if (!/^(?:who|which)$/i.test(a)) return false;\n    }\n  }\n  return true;\n}\n\nfunction baseFiltered(overrideMode) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const categories = selectedCategories();\n  const types = selectedValues('input[data-type]');\n  return qb.filter(item => {\n    if (item.subject !== subj) return false;\n    if (item.grade !== grade) return false;\n    if (categories.length && !categories.includes(item.category)) return false;\n    if (types.length && !types.includes(item.type)) return false;\n    if (!passesVocab(item, overrideMode)) return false;\n    if (!passesPrereqGrammar(item)) return false;\n    if (!passesQualityGate(item)) return false;\n    return true;\n  });\n}\n\nfunction shuffle(arr) {\n  const a = arr.slice();\n  for (let i=a.length-1;i>0;i--) {\n    const j = Math.floor(Math.random() * (i+1));\n    const tmp = a[i]; a[i] = a[j]; a[j] = tmp;\n  }\n  return a;\n}\n\nfunction updateBuilderStatus(candidates) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const sameGradeCount = qb.filter(x => x.subject === subj && x.grade === grade).length;\n  let msg = '全問題 ' + meta.counts.all + '件 / ' + grade + 'の' + subj + '元データ ' + sameGradeCount + '件 / この条件で使える候補 ' + candidates.length + '件';\n  if (subj === '英語') {\n    msg += ' / 教科書: ' + currentTextbook() + ' / 単語はここまで: ' + (el('sectionSelect').value || '未選択');\n  }\n  el('builderStatus').textContent = msg;\n  el('builderStatus').classList.toggle('good', candidates.length > 0);\n}\n\nfunction renderQuestions() {\n  const count = parseInt(el('questionCount').value, 10);\n  const pickMode = el('pickMode').value;\n  const candidates = baseFiltered();\n  updateBuilderStatus(candidates);\n  if (!candidates.length) {\n    currentQuestions = [];\n    el('questionPreview').innerHTML = 'この条件では問題が見つかりませんでした。単語制限を外すか、範囲や問題タイプを広げてください。';\n    return;\n  }\n  const picked = (pickMode === 'random' ? shuffle(candidates) : candidates).slice(0, count);\n  currentQuestions = picked;\n  el('questionPreview').classList.toggle('showAnswers', showAnswers);\n  el('questionPreview').innerHTML = picked.map((q, i) => `\n    <div class=\"q\">\n      <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n      <div>${q.q}</div>\n      <div class=\"ans\">答え: ${q.a}</div>\n    </div>\n  `).join('');\n}\n\nfunction printQuestions() {\n  if (!currentQuestions.length) return;\n  const escapeHtml = value => String(value ?? '').replace(/[&<>\"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[char]));\n  const renderQuestion = (q, index) => `<div class=\"q\"><div class=\"n\">Q${index + 1}　${escapeHtml(q.id)}</div><div class=\"body\">${escapeHtml(q.q)}</div>${showAnswers ? `<div class=\"a\">答え: ${escapeHtml(q.a)}</div>` : '<div class=\"answer-space\" aria-label=\"解答欄\"></div>'}</div>`;\n  const pages = [];\n  for (let startIndex = 0; startIndex < currentQuestions.length; startIndex += 10) {\n    const pageQuestions = currentQuestions.slice(startIndex, startIndex + 10);\n    pages.push({\n      html: pageQuestions.map((q, offset) => renderQuestion(q, startIndex + offset)).join(''),\n      rows: Math.ceil(pageQuestions.length / 2)\n    });\n  }\n  const printHtml = `<!doctype html><html lang=\"ja\"><head><meta charset=\"utf-8\"><title>問題プリント</title><style>\n  @page{size:A4 portrait;margin:8mm}\n  *{box-sizing:border-box}\n  body{font-family:-apple-system,BlinkMacSystemFont,\"Segoe UI\",\"Yu Gothic\",sans-serif;margin:0;color:#111;font-size:9pt;line-height:1.3}\n  .page{height:281mm;display:flex;flex-direction:column;overflow:hidden;page-break-after:always;break-after:page}\n  .page:last-child{page-break-after:auto;break-after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(saved));\n} catch (e) {}\nupdateBuilderStatus(baseFil"
    },
    {
      "needle": "useVocabGate",
      "index": 2580745,
      "context": "(ids || []).map(id => map.get(id)).filter(Boolean);\n  if (restored.length) {\n    currentQuestions = restored;\n    el('questionPreview').classList.toggle('showAnswers', showAnswers);\n    el('questionPreview').innerHTML = restored.map((q, i) => `\n      <div class=\"q\">\n        <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n        <div>${q.q}</div>\n        <div class=\"ans\">答え: ${q.a}</div>\n      </div>\n    `).join('');\n    el('builderStatus').textContent = '保存されていた問題セットを復元しました。';\n    el('builderStatus').classList.add('good');\n  }\n}\n\nfunction safeSetValue(id, value) {\n  const node = el(id);\n  if (!node || value == null) return;\n  const has = Array.from(node.options || []).some(op => op.value === value || op.textContent === value);\n  if (has) node.value = value;\n}\n\nfunction setCheckedValues(selector, values) {\n  const set = new Set(values || []);\n  document.querySelectorAll(selector).forEach(ch => {\n    ch.checked = set.size ? set.has(ch.value) : true;\n  });\n}\n\nfunction applyState(state) {\n  if (!state) return;\n  safeSetValue('subjectSelect', state.subject);\n  safeSetValue('gradeSelect', state.grade);\n  renderSectionOptions();\n  renderStageOptions();\n  renderTypeOptions();\n  safeSetValue('textbookSelect', state.textbook);\n  renderSectionOptions();\n  safeSetValue('sectionSelect', state.section);\n  safeSetValue('questionCount', state.questionCount);\n  safeSetValue('vocabMode', state.vocabMode);\n  safeSetValue('pickMode', state.pickMode);\n  setCheckedValues('input[data-stage]', state.stages);\n  setCheckedValues('input[data-type]', state.types);\n  syncListSelectors();\n  safeSetValue('listSubject', state.listSubject);\n  safeSetValue('listGrade', state.listGrade);\n  safeSetValue('listLimit', state.listLimit);\n  safeSetValue('listVocabMode', state.listVocabMode);\n  if (el('listKeyword')) el('listKeyword').value = state.listKeyword || '';\n  showAnswers = !!state.showAnswers;\n  restoreQuestionSetByIds(state.currentQuestionIds || []);\n  updateBuilderStatus(baseFiltered());\n}\n\nfunction exportStateFile() {\n  const blob = new Blob([JSON.stringify(captureState(), null, 2)], {type:'application/json'});\n  const url = URL.createObjectURL(blob);\n  const a = document.createElement('a');\n  a.href = url;\n  a.download = 'mikamijuku_app_state_latest.json';\n  document.body.appendChild(a);\n  a.click();\n  a.remove();\n  setTimeout(() => URL.revokeObjectURL(url), 3000);\n  if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを書き出しました。';\n}\n\nfunction importStateFile(file) {\n  const fr = new FileReader();\n  fr.onload = () => {\n    try {\n      const state = JSON.parse(fr.result);\n      applyState(state);\n      saveState(true);\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを読み込みました。';\n    } catch (e) {\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データの読み込みに失敗しました。';\n    }\n  };\n  fr.readAsText(file, 'utf-8');\n}\n\nconst currentSubject = () => el('subjectSelect').value;\nconst currentGrade = () => el('gradeSelect').value;\nconst currentTextbook = () => el('textbookSelect').value;\nconst currentSectionIndex = () => Math.max(0, el('sectionSelect').selectedIndex);\nconst useVocabGate = () => el('vocabMode').value === 'on';\n\nfunction renderTabs() {\n  document.querySelectorAll('.tab').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.tab').forEach(x => x.classList.remove('active'));\n      document.querySelectorAll('.screen').forEach(x => x.classList.remove('active'));\n      btn.classList.add('active');\n      const target = document.getElementById('screen-' + btn.dataset.screen);\n      if (target) target.classList.add('active');\n    });\n  });\n}\n\nfunction renderTextbookOptions() {\n  el('textbookSelect').innerHTML = meta.textbooks.map(tb => `<option>${tb}</option>`).join('');\n}\n\nfunction renderSectionOptions() {\n  const subject = currentSubject();\n  const grade = currentGrade().replace('中','');\n  if (subject !== '英語') {\n    el('textbookWrap').classList.add('hidden');\n    el('sectionWrap').classList.add('hidden');\n    return;\n  }\n  el('textbookWrap').classList.remove('hidden');\n  el('sectionWrap').classList.remove('hidden');\n  const tb = currentTextbook() || meta.textbooks[0];\n  const sections = ((meta.sections[tb] || {})[grade] || []);\n  el('sectionSelect').innerHTML = sections.map(sec => `<option>${sec}</option>`).join('');\n}\n\nfunction renderStageOptions() {\n  const stages = subjectConfig[currentSubject()].stagesByGrade[currentGrade()] || [];\n  el('stageArea').innerHTML = stages.map(s => `<label class=\"ck\"><input type=\"checkbox\" data-stage value=\"${s}\" checked> <span>${s}</span></label>`).join('');\n}\n\nfunction renderTypeOptions() {\n  const types = subjectConfig[currentSubject()].types;\n  el('typeArea').innerHTML = types.map(t => `<label class=\"ck\"><input type=\"checkbox\" data-type value=\"${t}\" checked> <span>${t}</span></label>`).join('');\n}\n\nfunction selectedValues(selector) {\n  return Array.from(document.querySelectorAll(selector + ':checked')).map(x => x.value);\n}\n\nfunction selectedCategories() {\n  const stages = selectedValues('input[data-stage]');\n  if (!stages.length) return [];\n  const map = subjectConfig[currentSubject()].stageMap[currentGrade()] || {};\n  const set = new Set();\n  stages.forEach(stage => (map[stage] || [stage]).forEach(c => set.add(c)));\n  return Array.from(set);\n}\n\nfunction passesVocab(item, overrideMode) {\n  if (item.subject !== '英語') return true;\n  const mode = overrideMode || (useVocabGate() ? 'on' : 'off');\n  if (mode === 'off') return true;\n  if (item.grade !== currentGrade()) return true;\n  const rec = meta.passMeta[item.id] || {};\n  const minIdx = rec[currentTextbook()];\n  if (meta.vocabCoordinateVersion !== 'v7-2026-08-18-1based') return false;\n  if (minIdx === -2) return true;\n  if (!Number.isInteger(minIdx) || minIdx <= 0) return false;\n  const selectedOrdinal = currentSectionIndex() + 1;\n  return minIdx <= selectedOrdinal;\n}\n\n// ---- Mikami prerequisite grammar + runtime quality gates ----\nfunction selectedStageCeiling() {\n  if (currentSubject() !== '英語') return Number.MAX_SAFE_INTEGER;\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  const selected = selectedValues('input[data-stage]');\n  if (!selected.length) return -1;\n  return selected.reduce((max, s) => Math.max(max, stages.indexOf(s)), -1);\n}\n\nfunction stageIndexByNameContains(...needles) {\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  for (let i = 0; i < stages.length; i++) {\n    const s = stages[i];\n    if (needles.some(n => s.includes(n))) return i;\n  }\n  return -1;\n}\n\nfunction targetStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const cfg = subjectConfig['英語'];\n  const stages = cfg.stagesByGrade[item.grade] || [];\n  const map = cfg.stageMap[item.grade] || {};\n  for (let i = 0; i < stages.length; i++) {\n    const cats = map[stages[i]] || [stages[i]];\n    if (cats.includes(item.category)) return i;\n  }\n  return -1;\n}\n\nfunction requiredGrammarStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const text = String(item.q || '') + ' ' + String(item.a || '');\n  let req = Math.max(0, targetStageIndex(item));\n  const bump = i => { if (i >= 0) req = Math.max(req, i); };\n\n  // Detect only structures that can make an otherwise earlier problem depend on a later grammar unit.\n  if (/\\b(will|won't)\\b|\\b(am|is|are) going to\\b/i.test(text)) bump(stageIndexByNameContains('未来'));\n  if (/\\b(was|were)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('過去進行'));\n  if (/\\b(am|is|are)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('進行形'));\n  if (/\\b(can|must|may|should)\\b/i.test(text)) bump(stageIndexByNameContains('助動詞','can'));\n  if (/\\bto\\s+(?:be|have|do|play|study|read|write|watch|visit|see|eat|drink|go|come|make|use|help|learn|practice|speak|listen|buy|call|join|take|work|live|want|need|try|start|finish)\\b/i.test(text)) bump(stageIndexByNameContains('不定詞①','不定詞②','不定詞'));\n  if (/\\b(?:enjoy|enjoys|enjoyed|finish|finishes|finished|stop|stops|stopped)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('動名詞','不定詞①，動名詞'));\n  if (/\\b(?:than|as\\s+\\w+\\s+as|more\\s+\\w+\\s+than|most\\s+\\w+)\\b/i.test(text)) bump(stageIndexByNameContains('比較'));\n  if (/\\b(am|is|are|was|were)\\s+(?:made|used|written|spoken|known|called|played|studied|visited|built|seen|loved)\\b/i.test(text)) bump(stageIndexByNameContains('受動態'));\n  if (/\\b(?:have|has)\\s+(?:ever\\s+|never\\s+|already\\s+|just\\s+)?(?:been|visited|finished|lost|lived|studied|seen|done|written|read|known|used|played|gone|made|eaten|had)\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形'));\n  if (/\\b(?:have|has) been [A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形（継続）'));\n  if (/\\b(?:who|which|that)\\s+(?:is|are|was|were|has|have|can|will|[A-Za-z]+s?)(?:\\s|\\?)/i.test(text) && /\\b(?:boy|girl|man|woman|teacher|student|person|book|bag|picture|pen|flower|thing)\\b/i.test(text)) bump(stageIndexByNameContains('関係代名詞'));\n  if (/\\bif\\b[\\s\\S]*\\bwould\\b|\\bI wish\\b/i.test(text)) bump(stageIndexByNameContains('仮定法'));\n  return req;\n}\n\nfunction passesPrereqGrammar(item) {\n  if (item.subject !== '英語') return true;\n  const ceiling = selectedStageCeiling();\n  if (ceiling < 0) return false;\n  const required = requiredGrammarStageIndex(item);\n  return required < 0 || required <= ceiling;\n}\n\nfunction passesQualityGate(item) {\n  if (!item || !String(item.q || '').trim() || !String(item.a || '').trim()) return false;\n  if (item.subject !== '英語') return true;\n  const q = String(item.q || '');\n  const a = String(item.a || '');\n  const both = q + ' ' + a;\n\n  // Known invalid / non-unique generation families.\n  if (/This is (?:he|she|we)\\b/i.test(both)) return false;\n  if (/\\bthan He\\b/.test(both)) return false;\n  if (/\\b(?:Mika|She) (?:love|begin|stop)\\b/.test(both)) return false;\n  if (/Do you have (?:visited|been|finished|lost|lived|studied|seen|done)/i.test(both)) return false;\n  if (/Did You ne\\b|Do You\\b|Do They\\b|Does She\\b|Does He\\b/.test(both)) return false;\n  if (/否定文または疑問文/.test(q)) return false;\n  if (/日本語で説明しなさい/.test(q) && /^[A-Za-z]/.test(a)) return false;\n  if (/\\(\\s*she\\s*\\/\\s*her\\s*\\/\\s*her\\s*\\)/i.test(q)) return false;\n  if (/\\(\\s*he\\s*\\/\\s*him\\s*\\/\\s*him\\s*\\)/i.test(q)) return false;\n  if (String(item.id || '').startsWith('M2-INF2-')) {\n    if (item.type === '空所補充' && /^to$/i."
    },
    {
      "needle": "useVocabGate",
      "index": 2583061,
      "context": "document.body.appendChild(a);\n  a.click();\n  a.remove();\n  setTimeout(() => URL.revokeObjectURL(url), 3000);\n  if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを書き出しました。';\n}\n\nfunction importStateFile(file) {\n  const fr = new FileReader();\n  fr.onload = () => {\n    try {\n      const state = JSON.parse(fr.result);\n      applyState(state);\n      saveState(true);\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを読み込みました。';\n    } catch (e) {\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データの読み込みに失敗しました。';\n    }\n  };\n  fr.readAsText(file, 'utf-8');\n}\n\nconst currentSubject = () => el('subjectSelect').value;\nconst currentGrade = () => el('gradeSelect').value;\nconst currentTextbook = () => el('textbookSelect').value;\nconst currentSectionIndex = () => Math.max(0, el('sectionSelect').selectedIndex);\nconst useVocabGate = () => el('vocabMode').value === 'on';\n\nfunction renderTabs() {\n  document.querySelectorAll('.tab').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.tab').forEach(x => x.classList.remove('active'));\n      document.querySelectorAll('.screen').forEach(x => x.classList.remove('active'));\n      btn.classList.add('active');\n      const target = document.getElementById('screen-' + btn.dataset.screen);\n      if (target) target.classList.add('active');\n    });\n  });\n}\n\nfunction renderTextbookOptions() {\n  el('textbookSelect').innerHTML = meta.textbooks.map(tb => `<option>${tb}</option>`).join('');\n}\n\nfunction renderSectionOptions() {\n  const subject = currentSubject();\n  const grade = currentGrade().replace('中','');\n  if (subject !== '英語') {\n    el('textbookWrap').classList.add('hidden');\n    el('sectionWrap').classList.add('hidden');\n    return;\n  }\n  el('textbookWrap').classList.remove('hidden');\n  el('sectionWrap').classList.remove('hidden');\n  const tb = currentTextbook() || meta.textbooks[0];\n  const sections = ((meta.sections[tb] || {})[grade] || []);\n  el('sectionSelect').innerHTML = sections.map(sec => `<option>${sec}</option>`).join('');\n}\n\nfunction renderStageOptions() {\n  const stages = subjectConfig[currentSubject()].stagesByGrade[currentGrade()] || [];\n  el('stageArea').innerHTML = stages.map(s => `<label class=\"ck\"><input type=\"checkbox\" data-stage value=\"${s}\" checked> <span>${s}</span></label>`).join('');\n}\n\nfunction renderTypeOptions() {\n  const types = subjectConfig[currentSubject()].types;\n  el('typeArea').innerHTML = types.map(t => `<label class=\"ck\"><input type=\"checkbox\" data-type value=\"${t}\" checked> <span>${t}</span></label>`).join('');\n}\n\nfunction selectedValues(selector) {\n  return Array.from(document.querySelectorAll(selector + ':checked')).map(x => x.value);\n}\n\nfunction selectedCategories() {\n  const stages = selectedValues('input[data-stage]');\n  if (!stages.length) return [];\n  const map = subjectConfig[currentSubject()].stageMap[currentGrade()] || {};\n  const set = new Set();\n  stages.forEach(stage => (map[stage] || [stage]).forEach(c => set.add(c)));\n  return Array.from(set);\n}\n\nfunction passesVocab(item, overrideMode) {\n  if (item.subject !== '英語') return true;\n  const mode = overrideMode || (useVocabGate() ? 'on' : 'off');\n  if (mode === 'off') return true;\n  if (item.grade !== currentGrade()) return true;\n  const rec = meta.passMeta[item.id] || {};\n  const minIdx = rec[currentTextbook()];\n  if (meta.vocabCoordinateVersion !== 'v7-2026-08-18-1based') return false;\n  if (minIdx === -2) return true;\n  if (!Number.isInteger(minIdx) || minIdx <= 0) return false;\n  const selectedOrdinal = currentSectionIndex() + 1;\n  return minIdx <= selectedOrdinal;\n}\n\n// ---- Mikami prerequisite grammar + runtime quality gates ----\nfunction selectedStageCeiling() {\n  if (currentSubject() !== '英語') return Number.MAX_SAFE_INTEGER;\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  const selected = selectedValues('input[data-stage]');\n  if (!selected.length) return -1;\n  return selected.reduce((max, s) => Math.max(max, stages.indexOf(s)), -1);\n}\n\nfunction stageIndexByNameContains(...needles) {\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  for (let i = 0; i < stages.length; i++) {\n    const s = stages[i];\n    if (needles.some(n => s.includes(n))) return i;\n  }\n  return -1;\n}\n\nfunction targetStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const cfg = subjectConfig['英語'];\n  const stages = cfg.stagesByGrade[item.grade] || [];\n  const map = cfg.stageMap[item.grade] || {};\n  for (let i = 0; i < stages.length; i++) {\n    const cats = map[stages[i]] || [stages[i]];\n    if (cats.includes(item.category)) return i;\n  }\n  return -1;\n}\n\nfunction requiredGrammarStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const text = String(item.q || '') + ' ' + String(item.a || '');\n  let req = Math.max(0, targetStageIndex(item));\n  const bump = i => { if (i >= 0) req = Math.max(req, i); };\n\n  // Detect only structures that can make an otherwise earlier problem depend on a later grammar unit.\n  if (/\\b(will|won't)\\b|\\b(am|is|are) going to\\b/i.test(text)) bump(stageIndexByNameContains('未来'));\n  if (/\\b(was|were)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('過去進行'));\n  if (/\\b(am|is|are)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('進行形'));\n  if (/\\b(can|must|may|should)\\b/i.test(text)) bump(stageIndexByNameContains('助動詞','can'));\n  if (/\\bto\\s+(?:be|have|do|play|study|read|write|watch|visit|see|eat|drink|go|come|make|use|help|learn|practice|speak|listen|buy|call|join|take|work|live|want|need|try|start|finish)\\b/i.test(text)) bump(stageIndexByNameContains('不定詞①','不定詞②','不定詞'));\n  if (/\\b(?:enjoy|enjoys|enjoyed|finish|finishes|finished|stop|stops|stopped)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('動名詞','不定詞①，動名詞'));\n  if (/\\b(?:than|as\\s+\\w+\\s+as|more\\s+\\w+\\s+than|most\\s+\\w+)\\b/i.test(text)) bump(stageIndexByNameContains('比較'));\n  if (/\\b(am|is|are|was|were)\\s+(?:made|used|written|spoken|known|called|played|studied|visited|built|seen|loved)\\b/i.test(text)) bump(stageIndexByNameContains('受動態'));\n  if (/\\b(?:have|has)\\s+(?:ever\\s+|never\\s+|already\\s+|just\\s+)?(?:been|visited|finished|lost|lived|studied|seen|done|written|read|known|used|played|gone|made|eaten|had)\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形'));\n  if (/\\b(?:have|has) been [A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形（継続）'));\n  if (/\\b(?:who|which|that)\\s+(?:is|are|was|were|has|have|can|will|[A-Za-z]+s?)(?:\\s|\\?)/i.test(text) && /\\b(?:boy|girl|man|woman|teacher|student|person|book|bag|picture|pen|flower|thing)\\b/i.test(text)) bump(stageIndexByNameContains('関係代名詞'));\n  if (/\\bif\\b[\\s\\S]*\\bwould\\b|\\bI wish\\b/i.test(text)) bump(stageIndexByNameContains('仮定法'));\n  return req;\n}\n\nfunction passesPrereqGrammar(item) {\n  if (item.subject !== '英語') return true;\n  const ceiling = selectedStageCeiling();\n  if (ceiling < 0) return false;\n  const required = requiredGrammarStageIndex(item);\n  return required < 0 || required <= ceiling;\n}\n\nfunction passesQualityGate(item) {\n  if (!item || !String(item.q || '').trim() || !String(item.a || '').trim()) return false;\n  if (item.subject !== '英語') return true;\n  const q = String(item.q || '');\n  const a = String(item.a || '');\n  const both = q + ' ' + a;\n\n  // Known invalid / non-unique generation families.\n  if (/This is (?:he|she|we)\\b/i.test(both)) return false;\n  if (/\\bthan He\\b/.test(both)) return false;\n  if (/\\b(?:Mika|She) (?:love|begin|stop)\\b/.test(both)) return false;\n  if (/Do you have (?:visited|been|finished|lost|lived|studied|seen|done)/i.test(both)) return false;\n  if (/Did You ne\\b|Do You\\b|Do They\\b|Does She\\b|Does He\\b/.test(both)) return false;\n  if (/否定文または疑問文/.test(q)) return false;\n  if (/日本語で説明しなさい/.test(q) && /^[A-Za-z]/.test(a)) return false;\n  if (/\\(\\s*she\\s*\\/\\s*her\\s*\\/\\s*her\\s*\\)/i.test(q)) return false;\n  if (/\\(\\s*he\\s*\\/\\s*him\\s*\\/\\s*him\\s*\\)/i.test(q)) return false;\n  if (String(item.id || '').startsWith('M2-INF2-')) {\n    if (item.type === '空所補充' && /^to$/i.test(a) && /\\bto\\s*\\(\\s*\\)\\s*(?=[A-Za-z])/i.test(q)) return false;\n    if (/^(?:He|She|Yuki|Mika|Takumi|Ken|Emi|Tom|The student|The teacher|This boy|This girl|My mother|My father|My brother|My sister|My friend) (?:want|need|like) to\\b/i.test(a)) return false;\n  }\n\n  // Target-grammar evidence: do not let a mislabeled vocabulary-only item through.\n  if (item.category === '動名詞' && !/[A-Za-z]+ing\\b/.test(both)) return false;\n  if (item.category === '比較' && !/\\bthan\\b|\\bas\\s+\\w+\\s+as\\b|\\bmost\\b|\\w+est\\b/i.test(both)) return false;\n  const infinitiveBlank = item.type === '空所補充' && /^to$/i.test(a) && /\\(\\s*\\)\\s*[A-Za-z]+\\b/.test(q);\n  if ((item.category === '不定詞' || item.category === '不定詞①' || item.category === '不定詞②') && !/\\bto\\s+[A-Za-z]+\\b/i.test(both) && !infinitiveBlank) return false;\n  if (String(item.category).startsWith('現在完了形') && !/\\b(?:have|has)\\b/i.test(both)) return false;\n  if (item.category === '関係代名詞') {\n    if (!/\\b(?:who|which|that)\\b/i.test(both)) return false;\n    if (item.type === '空所補充' && /\\(\\s*\\)/.test(q)) {\n      if (!/who\\s*\\/\\s*which/i.test(q)) return false;\n      if (!/^(?:who|which)$/i.test(a)) return false;\n    }\n  }\n  return true;\n}\n\nfunction baseFiltered(overrideMode) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const categories = selectedCategories();\n  const types = selectedValues('input[data-type]');\n  return qb.filter(item => {\n    if (item.subject !== subj) return false;\n    if (item.grade !== grade) return false;\n    if (categories.length && !categories.includes(item.category)) return false;\n    if (types.length && !types.includes(item.type)) return false;\n    if (!passesVocab(item, overrideMode)) return false;\n    if (!passesPrereqGrammar(item)) return false;\n    if (!passesQualityGate(item)) return false;\n    return true;\n  });\n}\n\nfunction shuffle(arr) {\n  const a = arr.slice();\n  for (let i=a.length-1;i>0;i--) {\n    const j = Math.floor(Math.random() * (i+1));\n    const tmp = a[i]; a[i] = a[j]; a[j] = tmp;\n  }\n  return a;\n}\n\nfunction updateBuilderStatus(candidates) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const sameGradeCount = qb.filter(x => x.subject === subj && x.grade === grade).length;\n  let msg = '全問題 ' + meta.counts.all + '件 / ' + grade + 'の' + subj + '元データ ' + sameGradeCount + '件 / "
    },
    {
      "needle": "useVocabGate",
      "index": 2594379,
      "context": "q\">\n      <div class=\"qnum\">Q${String(i+1).padStart(2,'0')}　${q.id}　/　${q.category}　/　${q.type}</div>\n      <div>${q.q}</div>\n      <div class=\"ans\">答え: ${q.a}</div>\n    </div>\n  `).join('');\n}\n\nfunction printQuestions() {\n  if (!currentQuestions.length) return;\n  const escapeHtml = value => String(value ?? '').replace(/[&<>\"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[char]));\n  const renderQuestion = (q, index) => `<div class=\"q\"><div class=\"n\">Q${index + 1}　${escapeHtml(q.id)}</div><div class=\"body\">${escapeHtml(q.q)}</div>${showAnswers ? `<div class=\"a\">答え: ${escapeHtml(q.a)}</div>` : '<div class=\"answer-space\" aria-label=\"解答欄\"></div>'}</div>`;\n  const pages = [];\n  for (let startIndex = 0; startIndex < currentQuestions.length; startIndex += 10) {\n    const pageQuestions = currentQuestions.slice(startIndex, startIndex + 10);\n    pages.push({\n      html: pageQuestions.map((q, offset) => renderQuestion(q, startIndex + offset)).join(''),\n      rows: Math.ceil(pageQuestions.length / 2)\n    });\n  }\n  const printHtml = `<!doctype html><html lang=\"ja\"><head><meta charset=\"utf-8\"><title>問題プリント</title><style>\n  @page{size:A4 portrait;margin:8mm}\n  *{box-sizing:border-box}\n  body{font-family:-apple-system,BlinkMacSystemFont,\"Segoe UI\",\"Yu Gothic\",sans-serif;margin:0;color:#111;font-size:9pt;line-height:1.3}\n  .page{height:281mm;display:flex;flex-direction:column;overflow:hidden;page-break-after:always;break-after:page}\n  .page:last-child{page-break-after:auto;break-after:auto}\n  h2{flex:0 0 auto;font-size:14pt;margin:0 0 2mm}.meta{flex:0 0 auto;font-size:8pt;margin:0 0 4mm;padding-bottom:2mm;border-bottom:0.3mm solid #555}\n  .questions{flex:1 1 auto;min-height:0;display:grid;grid-template-columns:1fr 1fr;gap:2.5mm 5mm;height:auto}\n  .q{display:flex;flex-direction:column;break-inside:avoid;page-break-inside:avoid;border:0.2mm solid #c7c7c7;border-radius:1.5mm;padding:2.5mm;margin:0;min-height:0}\n  .n{font-weight:700;font-size:8pt;margin-bottom:1mm}.body{white-space:pre-wrap}.a{margin-top:2mm;color:#075c4c;font-size:8pt}\n  .answer-space{flex:1;min-height:18mm;margin-top:2mm;background:repeating-linear-gradient(to bottom,transparent 0,transparent 7.2mm,#a8a8a8 7.4mm,transparent 7.6mm)}\n  </style></head><body>\n  ${pages.map((page, pageIndex) => `<section class=\"page\"><h2>${currentGrade()} ${currentSubject()} 問題プリント</h2><div class=\"meta\">教科書: ${currentSubject()==='英語' ? escapeHtml(currentTextbook()) : '—'} / 単語はここまで: ${currentSubject()==='英語' ? escapeHtml(el('sectionSelect').value || '—') : '—'} / ${pageIndex + 1}頁</div><div class=\"questions\" style=\"grid-template-rows:repeat(${page.rows}, minmax(0, 1fr))\">${page.html}</div></section>`).join('')}\n  <script>window.onload=()=>window.print()<\\/script></body></html>`;\n  const w = window.open('', '_blank');\n  if (!w) return;\n  w.document.open();\n  w.document.write(printHtml);\n  w.document.close();\n}\n\nfunction refreshList() {\n  const subj = el('listSubject').value;\n  const grade = el('listGrade').value;\n  const limit = parseInt(el('listLimit').value, 10);\n  const kw = el('listKeyword').value.trim().toLowerCase();\n  const vocabMode = el('listVocabMode').value === 'off' ? 'off' : (useVocabGate() ? 'on' : 'off');\n  let rows = qb.filter(item => {\n    if (subj !== 'all' && item.subject !== subj) return false;\n    if (grade !== 'all' && item.grade !== grade) return false;\n    if (item.subject === '英語' && item.grade === currentGrade() && !passesVocab(item, vocabMode)) return false;\n    if (kw) {\n      const hay = (item.id + ' ' + item.category + ' ' + item.type + ' ' + item.q + ' ' + item.a).toLowerCase();\n      if (!hay.includes(kw)) return false;\n    }\n    return true;\n  });\n  el('listStatus').textContent = '一覧 ' + rows.length + '件';\n  el('listStatus').classList.toggle('good', rows.length > 0);\n  rows = rows.slice(0, limit);\n  el('listBody').innerHTML = rows.map(item => `\n    <tr><td>${item.id}</td><td>${item.subject}</td><td>${item.grade}</td><td>${item.category}</td><td>${item.type}</td><td>${item.q}</td><td>${item.a}</td></tr>\n  `).join('');\n}\n\nfunction syncListSelectors() {\n  el('listSubject').value = currentSubject();\n  el('listGrade').value = currentGrade();\n}\n\nfunction wireEvents() {\n  renderTabs();\n  el('subjectSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('gradeSelect').addEventListener('change', () => { renderSectionOptions(); renderStageOptions(); renderTypeOptions(); syncListSelectors(); updateBuilderStatus(baseFiltered()); });\n  el('textbookSelect').addEventListener('change', () => { renderSectionOptions(); updateBuilderStatus(baseFiltered()); });\n  el('sectionSelect').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  el('vocabMode').addEventListener('change', () => updateBuilderStatus(baseFiltered()));\n  document.addEventListener('change', evt => {\n    if (evt.target.matches('input[data-stage], input[data-type]')) updateBuilderStatus(baseFiltered());\n  });\n  el('generateBtn').addEventListener('click', renderQuestions);\n  el('toggleAnswersBtn').addEventListener('click', () => { showAnswers = !showAnswers; renderQuestions(); });\n  el('printBtn').addEventListener('click', printQuestions);\n  el('toListBtn').addEventListener('click', () => { document.querySelector('.tab[data-screen=\"list\"]').click(); refreshList(); saveState(true); });\n  el('refreshListBtn').addEventListener('click', () => { refreshList(); saveState(true); });\n  const autoSave = () => saveState(true);\n  document.addEventListener('change', evt => {\n    if (evt.target.closest('select,input,textarea')) autoSave();\n  });\n  document.addEventListener('input', evt => {\n    if (evt.target.closest('input[type=text], textarea')) autoSave();\n  });\n  if (el('exportJsonBtn')) el('exportJsonBtn').addEventListener('click', exportStateFile);\n  if (el('importJsonBtn')) el('importJsonBtn').addEventListener('click', () => el('importJsonInput').click());\n  if (el('importJsonInput')) el('importJsonInput').addEventListener('change', evt => {\n    const file = evt.target.files && evt.target.files[0];\n    if (file) importStateFile(file);\n    evt.target.value = '';\n  });\n}\n\n\nfunction esc(s){ return String(s ?? '').replace(/[&<>\"]/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m])); }\n\nrenderTextbookOptions();\nrenderSectionOptions();\nrenderStageOptions();\nrenderTypeOptions();\nsyncListSelectors();\nwireEvents();\ntry {\n  const saved = localStorage.getItem(STORAGE_KEY);\n  if (saved) applyState(JSON.parse(saved));\n} catch (e) {}\nupdateBuilderStatus(baseFiltered());\n})();\n</script>\n\n</body>\n</html>"
    },
    {
      "needle": "passMeta",
      "index": 2141239,
      "context": "e\":\"文法ミス\",\"cause\":\"学校範囲での定着不足\",\"q\":\"If I were rich, I would travel around the world. 問い：この文の意味として最も適切なものを書きなさい。\",\"a\":\"もし私がお金持ちなら、世界中を旅行するのに。\"}]</script>\n<script id=\"meta-data\" type=\"application/json\">{\"textbooks\":[\"サンシャイン\",\"ニューホライズン\"],\"sections\":{\"ニューホライズン\":{\"1\":[\"プレステップ2\",\"プレステップ3\",\"プレステップ4\",\"プレステップ5\",\"プレステップ6\",\"プレステップ7\",\"プレステップ8\",\"プレステップ9\",\"Unit 0\",\"Unit 1-1\",\"Unit 1-2\",\"Unit 1-3\",\"Unit 2-1\",\"Unit 2-2\",\"Unit 2-3\",\"Unit 3-1\",\"Unit 3-2\",\"Unit 3-3\",\"Unit 4-1\",\"Unit 4-2\",\"Unit 4-3\",\"Stage Activity 1\",\"Unit 5-1\",\"Unit 5-2\",\"Unit 5-3\",\"Unit 6-1\",\"Unit 6-2\",\"Unit 6-3\",\"Real Life English 2\",\"Unit 7-1\",\"Unit 7-2\",\"Unit 7-3\",\"Real Life English 3\",\"Unit 8-1・2\",\"Unit 8-3\",\"Real Life English 4\",\"Stage Activity 2\",\"Unit 9-1\",\"Unit 9-2\",\"Real Life English 5\",\"Unit 10-1\",\"Unit 10-2\",\"Unit 10-3\",\"Real Life English 6\",\"Stage Activity 3\",\"Let's Read\"],\"2\":[\"Unit 0\",\"Unit 1-1\",\"Unit 1-2\",\"Unit 1-3\",\"Unit 1-4\",\"Unit 2-1\",\"Unit 2-2\",\"Unit 2-3\",\"Unit 2-4\",\"Real Life English 2\",\"Unit 3-1・2\",\"Unit 3-3\",\"Unit 3-4\",\"Stage Activity 1\",\"Let's Read 1\",\"Unit 4-1\",\"Unit 4-2\",\"Unit 4-3・4\",\"Real Life English 4\",\"Unit 5-1\",\"Unit 5-2\",\"Unit 5-3・4\",\"Unit 6-1\",\"Unit 6-2\",\"Unit 6-3\",\"Unit 6-4\",\"Real Life English 6\",\"Stage Activity 2\",\"Let's Read 2\",\"Unit 7-1・2\",\"Unit 7-3\",\"Unit 7-4\",\"Real Life English 7\",\"Stage Activity 3\",\"Let's Read 3\"],\"3\":[\"Unit 0\",\"Unit 1-1・2\",\"Unit 1-3\",\"Unit 1-4\",\"Unit 2-1\",\"Unit 2-2\",\"Unit 2-3・4\",\"Unit 3-1\",\"Unit 3-2\",\"Unit 3-3・4\",\"Real Life English 3\",\"Stage Activity 1\",\"Let's Read 1-1\",\"Let's Read 1-2\",\"Unit 4-1\",\"Unit 4-2\",\"Unit 4-3\",\"Unit 4-4\",\"Real Life English 4\",\"Unit 5-1\",\"Unit 5-2\",\"Unit 5-3\",\"Unit 5-4\",\"Real Life English 5\",\"Stage Activity 2\",\"Unit 6-1\",\"Unit 6-2・3\",\"Unit 6-4\",\"Stage Activity 3\",\"Let's Read 2\"]},\"サンシャイン\":{\"1\":[\"プレステップ2\",\"プレステップ3\",\"プレステップ4\",\"プレステップ5\",\"プレステップ6\",\"プレステップ7\",\"プレステップ8\",\"プレステップ9\",\"Get Ready 3\",\"PROGRAM 1-1\",\"PROGRAM 1-2\",\"PROGRAM 2-1\",\"PROGRAM 2-2\",\"アクションコーナー\",\"PROGRAM 3-1\",\"PROGRAM 3-2\",\"Our Project 1\",\"Power-Up 1\",\"PROGRAM 4-1\",\"PROGRAM 4-2\",\"PROGRAM 5-1\",\"PROGRAM 5-2\",\"Power-Up 2\",\"PROGRAM 6-1\",\"PROGRAM 6-2\",\"PROGRAM 7-1\",\"PROGRAM 7-2\",\"疑問詞のまとめ\",\"Power-Up 3\",\"Our Project 2\",\"PROGRAM 8-1\",\"PROGRAM 8-2\",\"Power-Up 4\",\"PROGRAM 9-1\",\"PROGRAM 9-2・3\",\"PROGRAM 10-1・2\",\"PROGRAM 10-3\",\"Our Project 3\",\"Power-Up 6\"],\"2\":[\"PROGRAM 1-1\",\"PROGRAM 1-2\",\"PROGRAM 1-3\",\"PROGRAM 2-1\",\"PROGRAM 2-2\",\"PROGRAM 2-3\",\"Power-Up 1 / Step 2\",\"PROGRAM 3-1\",\"PROGRAM 3-2\",\"PROGRAM 3-3\",\"Our Project 4\",\"Reading 1\",\"Power-Up 2\",\"PROGRAM 4-1\",\"PROGRAM 4-2\",\"PROGRAM 4-3\",\"Power-Up 3 / Step 3\",\"PROGRAM 5-1\",\"PROGRAM 5-2\",\"PROGRAM 5-3\",\"PROGRAM 6-1\",\"PROGRAM 6-2\",\"PROGRAM 6-3\",\"Power-Up 4\",\"Step 4 / Our Project 5\",\"Reading 2\",\"PROGRAM 7-1・2\",\"PROGRAM 7-3\",\"PROGRAM 8-1\",\"PROGRAM 8-2・3\",\"Power-Up 5 / Step 5\",\"Our Project 6\",\"Reading 3\"],\"3\":[\"PROGRAM 1-1\",\"PROGRAM 1-2\",\"PROGRAM 1-3\",\"PROGRAM 2-1\",\"PROGRAM 2-2\",\"PROGRAM 2-3\",\"PROGRAM 3-1\",\"PROGRAM 3-2\",\"PROGRAM 3-3\",\"Power-Up 1 / Our Project 7\",\"Reading 1\",\"PROGRAM 4-1\",\"PROGRAM 4-2\",\"PROGRAM 4-3\",\"PROGRAM 5-1\",\"PROGRAM 5-2\",\"PROGRAM 5-3\",\"Power-Up 2 / Step 4\",\"PROGRAM 6-1\",\"PROGRAM 6-2\",\"PROGRAM 6-3\",\"Our Project 8 / Power-Up 3\",\"PROGRAM 7-1・2\",\"PROGRAM 7-3\",\"Power-Up 4\",\"Reading 2\",\"Special Project\"]}},\"passMeta\":{\"E1-BQ-001\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"E1-BQ-002\":{\"サンシャイン\":31,\"ニューホライズン\":35},\"E1-BQ-003\":{\"サンシャイン\":10,\"ニューホライズン\":22},\"E1-BQ-004\":{\"サンシャイン\":10,\"ニューホライズン\":16},\"E1-BQ-005\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"E1-BQ-006\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"E1-BQ-007\":{\"サンシャイン\":10,\"ニューホライズン\":13},\"E1-BQ-008\":{\"サンシャイン\":10,\"ニューホライズン\":19},\"E1-BQ-009\":{\"サンシャイン\":10,\"ニューホライズン\":22},\"E1-BQ-010\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"E1-BQ-011\":{\"サンシャイン\":11,\"ニューホライズン\":13},\"E1-BQ-012\":{\"サンシャイン\":-1,\"ニューホライズン\":-1},\"E1-BQ-013\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"E1-BQ-014\":{\"サンシャイン\":22,\"ニューホライズン\":22},\"E1-BQ-015\":{\"サンシャイン\":10,\"ニューホライズン\":19},\"E1-DQ-001\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-DQ-002\":{\"サンシャイン\":14,\"ニューホライズン\":17},\"E1-DQ-003\":{\"サンシャイン\":-1,\"ニューホライズン\":37},\"E1-DQ-004\":{\"サンシャイン\":7,\"ニューホライズン\":46},\"E1-DQ-005\":{\"サンシャイン\":12,\"ニューホライズン\":46},\"E1-DQ-006\":{\"サンシャイン\":-1,\"ニューホライズン\":-1},\"E1-DQ-007\":{\"サンシャイン\":14,\"ニューホライズン\":17},\"E1-DQ-008\":{\"サンシャイン\":7,\"ニューホライズン\":46},\"E1-DQ-009\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-DQ-010\":{\"サンシャイン\":-1,\"ニューホライズン\":37},\"E1-DQ-011\":{\"サンシャイン\":12,\"ニューホライズン\":46},\"E1-DQ-012\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-DQ-013\":{\"サンシャイン\":14,\"ニューホライズン\":22},\"E1-DQ-014\":{\"サンシャイン\":1,\"ニューホライズン\":46},\"E1-DQ-015\":{\"サンシャイン\":7,\"ニューホライズン\":7},\"E1-BD-001\":{\"サンシャイン\":10,\"ニューホライズン\":16},\"E1-BD-002\":{\"サンシャイン\":7,\"ニューホライズン\":7},\"E1-BD-003\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-BD-004\":{\"サンシャイン\":7,\"ニューホライズン\":46},\"E1-BD-005\":{\"サンシャイン\":10,\"ニューホライズン\":19},\"E1-BD-006\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-BD-007\":{\"サンシャイン\":10,\"ニューホライズン\":16},\"E1-BD-008\":{\"サンシャイン\":1,\"ニューホライズン\":46},\"E1-BD-009\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"E1-BD-010\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-BD-011\":{\"サンシャイン\":-1,\"ニューホライズン\":37},\"E1-BD-012\":{\"サンシャイン\":-1,\"ニューホライズン\":-1},\"E1-BD-013\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-BD-014\":{\"サンシャイン\":10,\"ニューホライズン\":22},\"E1-BD-015\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-WH-001\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-WH-002\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-WH-003\":{\"サンシャイン\":-1,\"ニューホライズン\":-1},\"E1-WH-004\":{\"サンシャイン\":9,\"ニューホライズン\":1},\"E1-WH-005\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-WH-006\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-WH-007\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-WH-008\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-WH-009\":{\"サンシャイン\":1,\"ニューホライズン\":17},\"E1-WH-010\":{\"サンシャイン\":9,\"ニューホライズン\":1},\"E1-WH-011\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-WH-012\":{\"サンシャイン\":37,\"ニューホライズン\":-1},\"E1-3S-001\":{\"サンシャイン\":7,\"ニューホライズン\":46},\"E1-3S-002\":{\"サンシャイン\":-1,\"ニューホライズン\":37},\"E1-3S-003\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-3S-004\":{\"サンシャイン\":21,\"ニューホライズン\":24},\"E1-3S-005\":{\"サンシャイン\":14,\"ニューホライズン\":22},\"E1-3S-006\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-3S-007\":{\"サンシャイン\":14,\"ニューホライズン\":17},\"E1-3S-008\":{\"サンシャイン\":7,\"ニューホライズン\":46},\"E1-3S-009\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-3S-010\":{\"サンシャイン\":-1,\"ニューホライズン\":37},\"E1-3S-011\":{\"サンシャイン\":22,\"ニューホライズン\":23},\"E1-3S-012\":{\"サンシャイン\":35,\"ニューホライズン\":43},\"E1-3S-013\":{\"サンシャイン\":9,\"ニューホライズン\":8},\"E1-3S-014\":{\"サンシャイン\":21,\"ニューホライズン\":46},\"E1-3S-015\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-PR-001\":{\"サンシャイン\":-1,\"ニューホライズン\":9},\"E1-PR-002\":{\"サンシャイン\":-1,\"ニューホライズン\":9},\"E1-PR-003\":{\"サンシャイン\":25,\"ニューホライズン\":18},\"E1-PR-004\":{\"サンシャイン\":12,\"ニューホライズン\":1},\"E1-PR-005\":{\"サンシャイン\":-1,\"ニューホライズン\":-1},\"E1-PR-006\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-PR-007\":{\"サンシャイン\":-1,\"ニューホライズン\":-1},\"E1-PR-008\":{\"サンシャイン\":-1,\"ニューホライズン\":17},\"E1-PR-009\":{\"サンシャイン\":27,\"ニューホライズン\":19},\"E1-PR-010\":{\"サンシャイン\":17,\"ニューホライズン\":18},\"E1-PR-011\":{\"サンシャイン\":12,\"ニューホライズン\":1},\"E1-PR-012\":{\"サンシャイン\":-1,\"ニューホライズン\":9},\"E1-FA-001\":{\"サンシャイン\":25,\"ニューホライズン\":46},\"E1-FA-002\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"E1-FA-003\":{\"サンシャイン\":25,\"ニューホライズン\":34},\"E1-FA-004\":{\"サンシャイン\":25,\"ニューホライズン\":34},\"E1-FA-005\":{\"サンシャイン\":14,\"ニューホライズン\":-1},\"E1-FA-006\":{\"サンシャイン\":16,\"ニューホライズン\":17},\"E1-FA-007\":{\"サンシャイン\":31,\"ニューホライズン\":34},\"E1-FA-008\":{\"サンシャイン\":25,\"ニューホライズン\":46},\"E1-FA-009\":{\"サンシャイン\":31,\"ニューホライズン\":35},\"E1-FA-010\":{\"サンシャイン\":-1,\"ニューホライズン\":-1},\"E1-OR-001\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-OR-002\":{\"サンシャイン\":9,\"ニューホライズン\":1},\"E1-OR-003\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"E1-OR-004\":{\"サンシャイン\":1,\"ニューホライズン\":46},\"E1-OR-005\":{\"サンシャイン\":17,\"ニューホライズン\":18},\"E1-OR-006\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"E1-OR-007\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-OR-008\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"E1-OR-009\":{\"サンシャイン\":-1,\"ニューホライズン\":37},\"E1-OR-010\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-OR-011\":{\"サンシャイン\":14,\"ニューホライズン\":17},\"E1-OR-012\":{\"サンシャイン\":36,\"ニューホライズン\":21},\"E1-OR-013\":{\"サンシャイン\":16,\"ニューホライズン\":11},\"E1-OR-014\":{\"サンシャイン\":-1,\"ニューホライズン\":9},\"E1-OR-015\":{\"サンシャイン\":9,\"ニューホライズン\":35},\"E1-WR-001\":{\"サンシャイン\":-1,\"ニューホライズン\":-1},\"E1-WR-002\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-WR-003\":{\"サンシャイン\":31,\"ニューホライズン\":34},\"E1-WR-004\":{\"サンシャイン\":26,\"ニューホライズン\":33},\"E1-WR-005\":{\"サンシャイン\":10,\"ニューホライズン\":19},\"E1-WR-006\":{\"サンシャイン\":10,\"ニューホライズン\":16},\"E1-WR-007\":{\"サンシャイン\":12,\"ニューホライズン\":46},\"E1-WR-008\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-WR-009\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"E1-WR-010\":{\"サンシャイン\":9,\"ニューホライズン\":1},\"E1-WR-011\":{\"サンシャイン\":9,\"ニューホライズン\":8},\"E1-WR-012\":{\"サンシャイン\":16,\"ニューホライズン\":11},\"E1-RD-001\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-RD-002\":{\"サンシャイン\":4,\"ニューホライズン\":4},\"E1-RD-003\":{\"サンシャイン\":-1,\"ニューホライズン\":37},\"E1-RD-004\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"E1-RD-005\":{\"サンシャイン\":16,\"ニューホライズン\":46},\"E1-RD-006\":{\"サンシャイン\":1,\"ニューホライズン\":1},\"E1-RD-007\":{\"サンシャイン\":23,\"ニューホライズン\":1},\"E1-RD-008\":{\"サンシャイン\":10,\"ニューホライズン\":22},\"E1-RD-009\":{\"サンシャイン\":14,\"ニューホライズン\":-1},\"E1-RD-010\":{\"サンシャイン\":12,\"ニューホライズン\":46},\"E1-PASTBE-001\":{\"サンシャイン\":35,\"ニューホライズン\":42},\"E1-PASTBE-002\":{\"サンシャイン\":25,\"ニューホライズン\":18},\"E1-PASTBE-003\":{\"サンシャイン\":35,\"ニューホライズン\":42},\"E1-PAST-001\":{\"サンシャイン\":35,\"ニューホライズン\":38},\"E1-PAST-002\":{\"サンシャイン\":35,\"ニューホライズン\":42},\"E1-PAST-003\":{\"サンシャイン\":-1,\"ニューホライズン\":-1},\"E1-PASTDID-001\":{\"サンシャイン\":35,\"ニューホライズン\":46},\"E1-PASTDID-002\":{\"サンシャイン\":35,\"ニューホライズン\":42},\"E1-PASTDID-003\":{\"サンシャイン\":35,\"ニューホライズン\":42},\"E1-IRR-001\":{\"サンシャイン\":35,\"ニューホライズン\":38},\"E1-IRR-002\":{\"サンシャイン\":35,\"ニューホライズン\":42},\"E1-IRR-003\":{\"サンシャイン\":37,\"ニューホライズン\":-1},\"E1-TIME-001\":{\"サンシャイン\":35,\"ニューホライズン\":38},\"E1-TIME-002\":{\"サンシャイン\":35,\"ニューホライズン\":43},\"E1-TIME-003\":{\"サンシャイン\":35,\"ニューホライズン\":42},\"E1-PASTBE-004\":{\"サンシャイン\":35,\"ニューホライズン\":42},\"E1-PASTBE-005\":{\"サンシャイン\":35,\"ニューホライズン\":42},\"E1-PASTBE-006\":{\"サンシャイン\":35,\"ニューホライズン\":43},\"E1-PAST-004\":{\"サンシャイン\":35,\"ニューホライズン\":42},\"E1-PAST-005\":{\"サンシャイン\":34,\"ニューホライズン\":-1},\"E1-PAST-006\":{\"サンシャイン\":36,\"ニューホライズン\":42},\"E1-PAST-007\":{\"サンシャイン\":35,\"ニューホライズン\":46},\"E1-PASTDID-004\":{\"サンシャイン\":35,\"ニューホライズン\":43},\"E1-PASTDID-005\":{\"サンシャイン\":35,\"ニューホライズン\":38},\"E1-PASTDID-006\":{\"サンシャイン\":35,\"ニューホライズン\":42},\"E1-PASTDID-007\":{\"サンシャイン\":35,\"ニューホライズン\":46},\"E1-IRR-004\":{\"サンシャイン\":35,\"ニューホライズン\":42},\"E1-IRR-005\":{\"サンシャイン\":35,\"ニューホライズン\":43},\"E1-IRR-006\":{\"サンシャイン\":-1,\"ニューホライズン\":42},\"E1-IRR-007\":{\"サンシャイン\":-1,\"ニューホライズン\":-1},\"E1-IRR-008\":{\"サンシャイン\":36,\"ニューホライズン\":42},\"E1-TIME-004\":{\"サンシャイン\":35,\"ニューホライズン\":38},\"E1-TIME-005\":{\"サンシャイン\":34,\"ニューホライズン\":30},\"E1-TIME-006\":{\"サンシャイン\":35,\"ニューホライズン\":-1},\"E1-TIME-007\":{\"サンシャイン\":35,\"ニューホライズン\":43},\"E1-TIME-008\":{\"サンシャイン\":35,\"ニューホライズン\":38},\"GEN-BE-0001\":{\"サンシャイン\":31,\"ニューホライズン\":35},\"GEN-BE-0002\":{\"サンシャイン\":31,\"ニューホライズン\":35},\"GEN-BE-0003\":{\"サンシャイン\":31,\"ニューホライズン\":35},\"GEN-BE-0004\":{\"サンシャイン\":31,\"ニューホライズン\":35},\"GEN-BE-0005\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"GEN-BE-0006\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"GEN-BE-0007\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"GEN-BE-0008\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"GEN-BE-0010\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"GEN-BE-0011\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"GEN-BE-0012\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"GEN-BE-0013\":{\"サンシャイン\":31,\"ニューホライズン\":30},\"GEN-"
    },
    {
      "needle": "passMeta",
      "index": 2583197,
      "context": ")) el('globalSaveStatus').textContent = '保存データを書き出しました。';\n}\n\nfunction importStateFile(file) {\n  const fr = new FileReader();\n  fr.onload = () => {\n    try {\n      const state = JSON.parse(fr.result);\n      applyState(state);\n      saveState(true);\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データを読み込みました。';\n    } catch (e) {\n      if (el('globalSaveStatus')) el('globalSaveStatus').textContent = '保存データの読み込みに失敗しました。';\n    }\n  };\n  fr.readAsText(file, 'utf-8');\n}\n\nconst currentSubject = () => el('subjectSelect').value;\nconst currentGrade = () => el('gradeSelect').value;\nconst currentTextbook = () => el('textbookSelect').value;\nconst currentSectionIndex = () => Math.max(0, el('sectionSelect').selectedIndex);\nconst useVocabGate = () => el('vocabMode').value === 'on';\n\nfunction renderTabs() {\n  document.querySelectorAll('.tab').forEach(btn => {\n    btn.addEventListener('click', () => {\n      document.querySelectorAll('.tab').forEach(x => x.classList.remove('active'));\n      document.querySelectorAll('.screen').forEach(x => x.classList.remove('active'));\n      btn.classList.add('active');\n      const target = document.getElementById('screen-' + btn.dataset.screen);\n      if (target) target.classList.add('active');\n    });\n  });\n}\n\nfunction renderTextbookOptions() {\n  el('textbookSelect').innerHTML = meta.textbooks.map(tb => `<option>${tb}</option>`).join('');\n}\n\nfunction renderSectionOptions() {\n  const subject = currentSubject();\n  const grade = currentGrade().replace('中','');\n  if (subject !== '英語') {\n    el('textbookWrap').classList.add('hidden');\n    el('sectionWrap').classList.add('hidden');\n    return;\n  }\n  el('textbookWrap').classList.remove('hidden');\n  el('sectionWrap').classList.remove('hidden');\n  const tb = currentTextbook() || meta.textbooks[0];\n  const sections = ((meta.sections[tb] || {})[grade] || []);\n  el('sectionSelect').innerHTML = sections.map(sec => `<option>${sec}</option>`).join('');\n}\n\nfunction renderStageOptions() {\n  const stages = subjectConfig[currentSubject()].stagesByGrade[currentGrade()] || [];\n  el('stageArea').innerHTML = stages.map(s => `<label class=\"ck\"><input type=\"checkbox\" data-stage value=\"${s}\" checked> <span>${s}</span></label>`).join('');\n}\n\nfunction renderTypeOptions() {\n  const types = subjectConfig[currentSubject()].types;\n  el('typeArea').innerHTML = types.map(t => `<label class=\"ck\"><input type=\"checkbox\" data-type value=\"${t}\" checked> <span>${t}</span></label>`).join('');\n}\n\nfunction selectedValues(selector) {\n  return Array.from(document.querySelectorAll(selector + ':checked')).map(x => x.value);\n}\n\nfunction selectedCategories() {\n  const stages = selectedValues('input[data-stage]');\n  if (!stages.length) return [];\n  const map = subjectConfig[currentSubject()].stageMap[currentGrade()] || {};\n  const set = new Set();\n  stages.forEach(stage => (map[stage] || [stage]).forEach(c => set.add(c)));\n  return Array.from(set);\n}\n\nfunction passesVocab(item, overrideMode) {\n  if (item.subject !== '英語') return true;\n  const mode = overrideMode || (useVocabGate() ? 'on' : 'off');\n  if (mode === 'off') return true;\n  if (item.grade !== currentGrade()) return true;\n  const rec = meta.passMeta[item.id] || {};\n  const minIdx = rec[currentTextbook()];\n  if (meta.vocabCoordinateVersion !== 'v7-2026-08-18-1based') return false;\n  if (minIdx === -2) return true;\n  if (!Number.isInteger(minIdx) || minIdx <= 0) return false;\n  const selectedOrdinal = currentSectionIndex() + 1;\n  return minIdx <= selectedOrdinal;\n}\n\n// ---- Mikami prerequisite grammar + runtime quality gates ----\nfunction selectedStageCeiling() {\n  if (currentSubject() !== '英語') return Number.MAX_SAFE_INTEGER;\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  const selected = selectedValues('input[data-stage]');\n  if (!selected.length) return -1;\n  return selected.reduce((max, s) => Math.max(max, stages.indexOf(s)), -1);\n}\n\nfunction stageIndexByNameContains(...needles) {\n  const stages = subjectConfig['英語'].stagesByGrade[currentGrade()] || [];\n  for (let i = 0; i < stages.length; i++) {\n    const s = stages[i];\n    if (needles.some(n => s.includes(n))) return i;\n  }\n  return -1;\n}\n\nfunction targetStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const cfg = subjectConfig['英語'];\n  const stages = cfg.stagesByGrade[item.grade] || [];\n  const map = cfg.stageMap[item.grade] || {};\n  for (let i = 0; i < stages.length; i++) {\n    const cats = map[stages[i]] || [stages[i]];\n    if (cats.includes(item.category)) return i;\n  }\n  return -1;\n}\n\nfunction requiredGrammarStageIndex(item) {\n  if (item.subject !== '英語') return -1;\n  const text = String(item.q || '') + ' ' + String(item.a || '');\n  let req = Math.max(0, targetStageIndex(item));\n  const bump = i => { if (i >= 0) req = Math.max(req, i); };\n\n  // Detect only structures that can make an otherwise earlier problem depend on a later grammar unit.\n  if (/\\b(will|won't)\\b|\\b(am|is|are) going to\\b/i.test(text)) bump(stageIndexByNameContains('未来'));\n  if (/\\b(was|were)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('過去進行'));\n  if (/\\b(am|is|are)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('進行形'));\n  if (/\\b(can|must|may|should)\\b/i.test(text)) bump(stageIndexByNameContains('助動詞','can'));\n  if (/\\bto\\s+(?:be|have|do|play|study|read|write|watch|visit|see|eat|drink|go|come|make|use|help|learn|practice|speak|listen|buy|call|join|take|work|live|want|need|try|start|finish)\\b/i.test(text)) bump(stageIndexByNameContains('不定詞①','不定詞②','不定詞'));\n  if (/\\b(?:enjoy|enjoys|enjoyed|finish|finishes|finished|stop|stops|stopped)\\s+[A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('動名詞','不定詞①，動名詞'));\n  if (/\\b(?:than|as\\s+\\w+\\s+as|more\\s+\\w+\\s+than|most\\s+\\w+)\\b/i.test(text)) bump(stageIndexByNameContains('比較'));\n  if (/\\b(am|is|are|was|were)\\s+(?:made|used|written|spoken|known|called|played|studied|visited|built|seen|loved)\\b/i.test(text)) bump(stageIndexByNameContains('受動態'));\n  if (/\\b(?:have|has)\\s+(?:ever\\s+|never\\s+|already\\s+|just\\s+)?(?:been|visited|finished|lost|lived|studied|seen|done|written|read|known|used|played|gone|made|eaten|had)\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形'));\n  if (/\\b(?:have|has) been [A-Za-z]+ing\\b/i.test(text)) bump(stageIndexByNameContains('現在完了形（継続）'));\n  if (/\\b(?:who|which|that)\\s+(?:is|are|was|were|has|have|can|will|[A-Za-z]+s?)(?:\\s|\\?)/i.test(text) && /\\b(?:boy|girl|man|woman|teacher|student|person|book|bag|picture|pen|flower|thing)\\b/i.test(text)) bump(stageIndexByNameContains('関係代名詞'));\n  if (/\\bif\\b[\\s\\S]*\\bwould\\b|\\bI wish\\b/i.test(text)) bump(stageIndexByNameContains('仮定法'));\n  return req;\n}\n\nfunction passesPrereqGrammar(item) {\n  if (item.subject !== '英語') return true;\n  const ceiling = selectedStageCeiling();\n  if (ceiling < 0) return false;\n  const required = requiredGrammarStageIndex(item);\n  return required < 0 || required <= ceiling;\n}\n\nfunction passesQualityGate(item) {\n  if (!item || !String(item.q || '').trim() || !String(item.a || '').trim()) return false;\n  if (item.subject !== '英語') return true;\n  const q = String(item.q || '');\n  const a = String(item.a || '');\n  const both = q + ' ' + a;\n\n  // Known invalid / non-unique generation families.\n  if (/This is (?:he|she|we)\\b/i.test(both)) return false;\n  if (/\\bthan He\\b/.test(both)) return false;\n  if (/\\b(?:Mika|She) (?:love|begin|stop)\\b/.test(both)) return false;\n  if (/Do you have (?:visited|been|finished|lost|lived|studied|seen|done)/i.test(both)) return false;\n  if (/Did You ne\\b|Do You\\b|Do They\\b|Does She\\b|Does He\\b/.test(both)) return false;\n  if (/否定文または疑問文/.test(q)) return false;\n  if (/日本語で説明しなさい/.test(q) && /^[A-Za-z]/.test(a)) return false;\n  if (/\\(\\s*she\\s*\\/\\s*her\\s*\\/\\s*her\\s*\\)/i.test(q)) return false;\n  if (/\\(\\s*he\\s*\\/\\s*him\\s*\\/\\s*him\\s*\\)/i.test(q)) return false;\n  if (String(item.id || '').startsWith('M2-INF2-')) {\n    if (item.type === '空所補充' && /^to$/i.test(a) && /\\bto\\s*\\(\\s*\\)\\s*(?=[A-Za-z])/i.test(q)) return false;\n    if (/^(?:He|She|Yuki|Mika|Takumi|Ken|Emi|Tom|The student|The teacher|This boy|This girl|My mother|My father|My brother|My sister|My friend) (?:want|need|like) to\\b/i.test(a)) return false;\n  }\n\n  // Target-grammar evidence: do not let a mislabeled vocabulary-only item through.\n  if (item.category === '動名詞' && !/[A-Za-z]+ing\\b/.test(both)) return false;\n  if (item.category === '比較' && !/\\bthan\\b|\\bas\\s+\\w+\\s+as\\b|\\bmost\\b|\\w+est\\b/i.test(both)) return false;\n  const infinitiveBlank = item.type === '空所補充' && /^to$/i.test(a) && /\\(\\s*\\)\\s*[A-Za-z]+\\b/.test(q);\n  if ((item.category === '不定詞' || item.category === '不定詞①' || item.category === '不定詞②') && !/\\bto\\s+[A-Za-z]+\\b/i.test(both) && !infinitiveBlank) return false;\n  if (String(item.category).startsWith('現在完了形') && !/\\b(?:have|has)\\b/i.test(both)) return false;\n  if (item.category === '関係代名詞') {\n    if (!/\\b(?:who|which|that)\\b/i.test(both)) return false;\n    if (item.type === '空所補充' && /\\(\\s*\\)/.test(q)) {\n      if (!/who\\s*\\/\\s*which/i.test(q)) return false;\n      if (!/^(?:who|which)$/i.test(a)) return false;\n    }\n  }\n  return true;\n}\n\nfunction baseFiltered(overrideMode) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const categories = selectedCategories();\n  const types = selectedValues('input[data-type]');\n  return qb.filter(item => {\n    if (item.subject !== subj) return false;\n    if (item.grade !== grade) return false;\n    if (categories.length && !categories.includes(item.category)) return false;\n    if (types.length && !types.includes(item.type)) return false;\n    if (!passesVocab(item, overrideMode)) return false;\n    if (!passesPrereqGrammar(item)) return false;\n    if (!passesQualityGate(item)) return false;\n    return true;\n  });\n}\n\nfunction shuffle(arr) {\n  const a = arr.slice();\n  for (let i=a.length-1;i>0;i--) {\n    const j = Math.floor(Math.random() * (i+1));\n    const tmp = a[i]; a[i] = a[j]; a[j] = tmp;\n  }\n  return a;\n}\n\nfunction updateBuilderStatus(candidates) {\n  const subj = currentSubject();\n  const grade = currentGrade();\n  const sameGradeCount = qb.filter(x => x.subject === subj && x.grade === grade).length;\n  let msg = '全問題 ' + meta.counts.all + '件 / ' + grade + 'の' + subj + '元データ ' + sameGradeCount + '件 / この条件で使える候補 ' + candidates.length + '件';\n  if (subj === '英語') {\n    msg += ' / 教科書: ' + currentTextbook() + ' / 単語はここまで: ' + (el('sec"
    }
  ]
}
