# Trigger passMeta morphology-column repair

Run the generic repair that makes the safe passMeta builder consume the authoritative v7 `英語`, `検索用基本形`, and `変化形・別表記` columns. No problem-ID, unit, or count hard-code.
